# Part 3 Execution Plan: Independent Review, Implementer Fix, Docker Gate

- **Status:** Proposed
- **Date:** 2026-08-16
- **Target branch:** `fix/change-aware-save-mcp-autonomy`
- **Reported starting checkpoints:** parent `2c4c16bb`, nested `0bd67ad5`
- **Architecture source:** `doc/part3-gui-collaboration-stress-design.md`
- **Scope:** Part 3 collaboration correctness through Stage B. Stage C remains a separate fresh-session gate.

This execution plan augments the Part 3 architecture decision record. It does not replace the actor, trust, save, recovery, or collaboration contracts already defined there.

---

## 1. Fixed agent and model policy

The orchestration roles are fixed for the entire run:

| Role | Required agent/model | Permissions |
| --- | --- | --- |
| Orchestration | Main Agent | Controls scope, evidence, routing, tests, commits, and pushes. Does not self-approve. |
| Review | **Reviewer subagent** (orchestrator-spawned), operating under the review contract of §5 | Read-only review. Produces findings and verdicts. Does not edit files. |
| Implementation | **Implementer subagent** (orchestrator-spawned), operating under the fix contract of §7 | Implements approved work and fixes findings. Does not approve its own work. |
| Validation | Docker and required native platform gates | Executes exact commands and records machine-readable evidence. |

### 1.0 Role-execution amendment (2026-08-18)

Work items P3-WP00..WP09 were executed with the external roles **Grok 4.5 High** (review) and **Composer 2.5** (implementation). From P3-WP10 onward, by explicit user decision, both roles are executed by **orchestrator-spawned subagents** running in this session.

This is a change of *executor*, not of *contract*. Every obligation in §§4–12 continues to bind:

- The implementer subagent receives the §7 fix contract verbatim and returns the §7 packet.
- The reviewer subagent receives the §5 review contract verbatim and returns one §5.2 verdict with §5.2-schema findings.
- The reviewer subagent is a **separate spawn** from the implementer subagent, is read-only, and never reviews a diff it authored. Independent review and the no-self-approval rule are the invariants; the vendor name was only ever the means of enforcing them.
- The Main Agent still performs §6 triage, runs all §8 gates itself, and owns §11/§12.

Historical records for P3-WP00..WP09 are not rewritten. They remain accurate statements of who executed those work items.

### 1.1 Prohibited substitutions

- The model tier named **Fast** is forbidden.
- Automatic model selection is forbidden.
- Automatic fallback to another model or agent is forbidden.
- Low-effort, mini, lightweight, or reduced-reasoning substitutes are forbidden. Subagents are spawned at full reasoning effort.
- The implementer subagent may not review or approve its own diff; review is always a separate read-only spawn.
- The reviewer subagent may not edit files; if it wants a change, it files a §5.2 finding.
- The Main Agent may not act as reviewer for a diff it authored, and may not silently absorb the implementation role to make progress.
- If a required role cannot be spawned, the work item becomes `BLOCKED_MODEL_UNAVAILABLE`. The orchestrator stops rather than silently changing the process.

The executing role, agent type, and mode are recorded in every review and fix packet.

---

## 2. Authoritative loop

The loop runs once per **commit-sized work package**.

```text
[Main Agent: define work package and capture baseline]
                         |
                         v
            [Reviewer subagent: review]
                         |
              +----------+-----------+
              |                      |
         PROBLEMS                  NO ISSUES
              |                      |
              v                      v
 [Main Agent accepts/routes]   [Required tests already
              |                pass for exact diff?]
              v                      |
  [Implementer subagent: fix]    +----+----+
              |                 |         |
              v                NO        YES
 [Docker focused validation]    |         |
              |                 v         v
       +------+------+      [Run tests] [Final gate]
       |             |           |         |
     FAIL           PASS         +----+----+
       |             |                |
       v             v                v
 [Implementer]   [Reviewer]     [Commit]
       ^             |                |
       |       +-----+-----+          v
       |       |           |        [Push]
       |   PROBLEMS    NO ISSUES       |
       |       |           |           v
       +-------+           +-------- [Done]
```

The actual state machine is:

```text
BASELINED
  -> REVIEW_REQUIRED
  -> FIX_REQUIRED | TEST_REQUIRED | FINAL_GATE_REQUIRED
  -> REVIEW_REQUIRED
  -> FINAL_GATE_REQUIRED
  -> COMMIT_READY
  -> PUSH_READY
  -> DONE
```

No state may be skipped. In particular, `Implementer -> Commit` and `Tests -> Commit` are invalid transitions. A final independent review is mandatory after the last code change and after the required validation evidence exists.

---

## 3. Baseline before every work package

The Main Agent records:

- parent branch, HEAD, upstream, ahead/behind, and status;
- nested branch, HEAD, upstream, ahead/behind, and status;
- parent gitlink and nested HEAD consistency;
- exact untracked files and the exclusion list;
- source diff fingerprint;
- relevant generated-contract fingerprints;
- active FreeCAD, FreeCADCmd, Python, test, Ninja, CMake, and Docker processes;
- port 9875 ownership;
- Docker image tag and immutable image ID/digest;
- build configuration and target binaries;
- line-ending classification for every file the work package may edit.

The work stops immediately if:

- `HamaAdapter_v3` could be touched;
- the user's normal profile would be used;
- port 9875 is occupied;
- an unknown FreeCAD/MCP session is running;
- the parent gitlink is inconsistent;
- the repository contains unexplained test artifacts;
- a reported baseline cannot be reproduced.

The baseline is written to:

```text
results/part3-orchestration/<run-id>/<work-item>/baseline.json
```

`results/` remains untracked and is never committed.

---

## 4. Work-package contract

Each work package must be small enough to become one intentional commit. It contains:

```yaml
work_item_id: P3-WP##
title: one concern only
repository: parent | nested | both
objective: observable end state
in_scope:
  - exact files or components
out_of_scope:
  - frozen subsystems
  - unrelated cleanup
architecture_invariants:
  - rules that may not change
acceptance_tests:
  - exact commands or named gates
platforms:
  - docker-linux
  - windows-native, when required
commit_message: proposed message
```

### 4.1 Scope discipline

The implementer may not:

- refactor unrelated code;
- rename unrelated APIs;
- change a frozen subsystem merely to simplify a test;
- weaken assertions, skip tests, add `xfail`, or increase timeouts to obtain green output;
- add arbitrary sleeps for synchronization;
- reintroduce `execute_code` into the acceptance path;
- reintroduce transaction ownership in generated payloads;
- use a document-wide generation as the only semantic conflict fence;
- create a second live-document transaction/commit authority;
- merge or cherry-pick `feature/non-blocking-geometry-scaffolding` wholesale.

If an out-of-scope defect blocks the work package, the implementer reports it as `BLOCKED_OUT_OF_SCOPE`; the Main Agent creates a new work package rather than smuggling the change into the current one.

---

## 5. Reviewer review contract

Executed by Grok 4.5 High for P3-WP00..WP09 and by the reviewer subagent from P3-WP10 onward (§1.0). The contract below is unchanged by that substitution.

The reviewer performs a read-only review of the complete current diff and the relevant surrounding code. It reviews the work package against the architecture, not merely against compiler success.

### 5.1 Required review areas

1. **Actor boundaries**
   - local GUI actions remain local;
   - remote actions use typed JSON-RPC;
   - the coordinator does not execute arbitrary Python in FreeCAD.

2. **Mutation ownership**
   - one native live-document commit owner;
   - no nested transaction control;
   - explicit recompute policy;
   - local and remote model edits publish compatible semantic revisions.

3. **Identity and lifecycle**
   - document UID/instance/lifecycle identity is used where stale sessions or pointer/name reuse matter;
   - document names are diagnostic, not sufficient authority;
   - close/reopen invalidates old sessions and results.

4. **Conflict precision**
   - expected revisions bind to semantic read/write sets;
   - personal view activity cannot conflict;
   - unrelated model changes remain eligible;
   - structural changes invalidate dependent operations.

5. **Exactly-once behavior**
   - mutating RPCs use server-enforced operation identity where retries are possible;
   - duplicate operation IDs with equal payload return the stored result;
   - duplicate operation IDs with different payload fail explicitly.

6. **Undo/redo safety**
   - remote history actions cannot silently undo a user's intervening transaction;
   - any allowed history action is bound to an expected history head or operation identity.

7. **Cross-platform behavior**
   - Windows thread/module boundaries;
   - Windows lock/export rules;
   - 9p and persistent lock-anchor contracts;
   - CRLF preservation.

8. **Acceptance honesty**
   - no fallback from the real GUI pause checkbox in acceptance;
   - no forced shutdown reported as success;
   - no test-only bypass pretending to be a real GUI path;
   - test evidence covers the changed behavior rather than neighboring plumbing.

9. **Future non-blocking geometry compatibility**
   - detached geometry may become an execution backend later;
   - `DocumentCommitCoordinator` remains the sole final live commit owner;
   - future geometry results must be validated with semantic read-set revisions, not only global `modelGeneration`;
   - the exported out-of-line `MainThreadSignal` hook storage must not regress.

### 5.2 Finding format

The reviewer returns exactly one verdict:

```text
PROBLEMS
NO_ISSUES
BLOCKED_INSUFFICIENT_EVIDENCE
```

Every blocking finding has this schema:

```yaml
id: GRK-P3-###
severity: critical | high | medium | low
category: architecture | correctness | concurrency | lifecycle | security | test | portability | maintainability
file: path
lines: start-end
claim: one precise defect
evidence: concrete code path or failed invariant
impact: observable consequence
required_fix: bounded outcome, not a vague suggestion
required_test: exact regression proof
```

Rules:

- No finding may consist only of style preference.
- No finding may demand unrelated cleanup.
- “Consider” and “maybe” are non-blocking notes, not blocking findings.
- A `NO_ISSUES` verdict must list the exact diff fingerprint reviewed and the test packet fingerprint considered.
- Re-review examines the **entire current diff**, not only previously reported lines.

Review output is stored under:

```text
results/part3-orchestration/<run-id>/<work-item>/iteration-###/review.yaml
```

---

## 6. Main Agent finding triage

The orchestrator does not rewrite findings into softer language and does not silently discard them.

Each finding becomes one of:

```text
ACCEPTED
DUPLICATE_OF:<id>
DISPUTED_WITH_EVIDENCE
OUT_OF_SCOPE_BLOCKER
```

For `DISPUTED_WITH_EVIDENCE`, the Main Agent sends the exact source/test evidence back to the reviewer. The reviewer must resolve it as withdrawn, revised, or still blocking. The orchestrator cannot overrule an unresolved critical/high finding and proceed to commit.

Accepted findings are passed to the implementer unchanged, together with the work-package contract and relevant source context.

---

## 7. Implementer fix contract

Executed by Composer 2.5 for P3-WP00..WP09 and by the implementer subagent from P3-WP10 onward (§1.0). The contract below is unchanged by that substitution.

The implementer receives:

- the work-package definition;
- baseline fingerprints;
- accepted reviewer finding IDs;
- exact failure logs from prior validation;
- frozen-layer and safety constraints;
- the required test list.

The implementer must:

1. reproduce or reason from the supplied evidence before editing;
2. implement the smallest coherent fix;
3. add or update a regression test for every correctness finding;
4. preserve file line endings;
5. update generated fixtures only through the authoritative generator;
6. avoid changing the test to match broken production behavior;
7. report every edited file and why it changed;
8. map each finding ID to its resolution;
9. report any unresolved finding as unresolved rather than claiming completion.

The implementer returns:

```yaml
role: implementer
executor: Composer 2.5 (WP00..WP09) | implementer subagent (WP10+)
resolved_findings:
  GRK-P3-001: explanation
unresolved_findings: []
files_changed:
  - path: reason
tests_added_or_changed:
  - test: invariant
generated_outputs:
  - path: generator command
known_limitations: []
```

The implementer does not commit, push, or declare the work package approved.

---

## 8. Validation after every implementer change

Every implementer edit is followed by Docker validation. Docker success is necessary but not sufficient for Windows-specific work.

### 8.1 Iteration gate

Run the narrowest authoritative set that covers the changed concern:

1. compile/configuration checks for affected targets;
2. focused unit/integration tests named by reviewer findings;
3. architecture/static policy tests;
4. generated-contract equality tests where applicable;
5. Ruff/lint/compileall for changed Python surfaces;
6. `git diff --check` and line-ending validation.

Use a fresh container process and an isolated working directory. Compiler caches may be retained, but build outputs are keyed by:

```text
source HEADs + git diff fingerprint + build configuration + toolchain fingerprint
```

When App ABI, Gui ABI, or shared headers change, rebuild every dependent target. A rebuilt `App_tests_run` with a stale `FreeCADGui` is not evidence.

### 8.2 Native platform gate

Run native Windows validation in the same loop when the work package touches:

- GUI owner-thread behavior;
- DLL exports or cross-module storage;
- Win32 file sharing, rename, recovery, or lock behavior;
- launcher/session behavior on Windows;
- graceful GUI/process shutdown.

Docker may not certify those properties by proxy. Computers are literal enough without us asking Linux to vouch for Windows kernel semantics.

### 8.3 Failure packet

On failure, the Main Agent sends the implementer:

```yaml
command: exact command
exit_code: value
source_fingerprint: value
diff_fingerprint: value
container_image_id: value
platform: value
failed_tests:
  - exact name
log_path: path
first_relevant_error: verbatim excerpt
full_log_available: true
```

Do not paraphrase away the useful part of a compiler or test failure.

### 8.4 Pass packet

On success, record:

```yaml
verdict: PASSED
commands:
  - exact command and exit code
test_counts: run/passed/skipped/failed
source_fingerprint: value
diff_fingerprint: value
binary_fingerprints: relevant outputs
artifacts: paths
```

The pass packet is supplied to the reviewer for re-review.

---

## 9. Re-review loop

After validation passes, the reviewer reviews:

- the complete current diff;
- all previously accepted findings;
- the implementer's resolution map;
- test changes;
- the exact validation packet;
- any generated outputs;
- the final architecture impact.

Possible outcomes:

```text
PROBLEMS
  -> route accepted findings to the implementer

BLOCKED_INSUFFICIENT_EVIDENCE
  -> Main Agent obtains the missing test/evidence

NO_ISSUES
  -> proceed only if all work-package tests passed for the same diff fingerprint
```

A new code change after `NO_ISSUES` invalidates the verdict and restarts review.

---

## 10. Loop-breaker rules

The loop must make measurable progress rather than orbiting the same defect until everyone forgets why it mattered.

- Every iteration must reduce unresolved findings, add a reproducer, or produce new evidence.
- If the same finding reopens twice, enter **root-cause mode**:
  1. The reviewer writes a causal hypothesis and minimal reproduction contract.
  2. The implementer first adds the failing regression test or probe.
  3. The failure is observed.
  4. Only then is production code changed.
- If three consecutive implementer attempts fail the same test, stop normal patching and create a dedicated blocker work package.
- Timeout increases, retries, sleeps, skips, assertion weakening, and forced process termination do not count as progress.
- A flaky test is a defect to diagnose, not a coin to keep tossing until it lands green.

---

## 11. Final gate for a work package

A work package is `COMMIT_READY` only when all of these are true:

- reviewer verdict is `NO_ISSUES`;
- every accepted finding is resolved;
- iteration Docker gate passed;
- required native platform gate passed;
- required repository/static gates passed;
- source and diff fingerprints in review and test evidence are identical;
- no file changed after final review;
- no unexplained artifacts exist;
- parent/nested/gitlink state is still consistent;
- commit contents match the declared work-package scope.

The Main Agent computes and records:

```text
git diff --binary | SHA-256
```

That fingerprint binds the final review and tests to the exact commit candidate.

---

## 12. Commit and push protocol

The loop ends in one intentional commit per work package.

### 12.1 Nested repository work

1. Verify nested final gate.
2. Stage explicit paths only.
3. Commit with the declared message.
4. Verify the commit tree contains no results, logs, user files, `tests/lib`, or research artifacts.
5. Push the nested branch without force.
6. Verify the remote branch resolves to the pushed SHA.
7. Update the parent gitlink in a separate parent work package or the explicitly paired parent package.

### 12.2 Parent repository work

1. Verify parent final gate against the exact nested SHA.
2. Stage explicit paths only.
3. Commit.
4. Push without force.
5. Verify the remote parent SHA and the recorded gitlink.

No amend, rebase, force push, or pull request.

If push fails, do not rewrite a reviewed commit merely to make transport convenient. Resolve the repository state explicitly and repeat the necessary verification if the commit candidate changes.

---

## 13. Updated Part 3 work-package order

The following order minimizes expensive cross-platform repetition and prevents the harness from being built on unresolved product semantics.

### P3-WP00 — ADR corrections and execution contract

- Add this orchestration loop to the Part 3 documentation.
- Replace “three trust domains” with precise actor and credential boundaries.
- State that the RemoteAgentDriver runs in a child process that never receives the local control token.
- Bind actor identity to authenticated session state, not a caller-provided string.
- Add the non-blocking geometry compatibility section without merging that branch.
- Prohibit an acceptance fallback from the real Pause Agent Writes checkbox.
- Define stable document identity and lifecycle invalidation.
- Define exactly-once and undo/redo semantics before implementation.

**Gate:** documentation review plus architecture consistency checks.

### P3-WP01 — Generic identity-bound mutation preconditions

- Avoid a property-test-specific final product API.
- Use a stable document selector containing instance identity/lifecycle data; name remains diagnostic.
- Provide generic expected semantic revisions and typed mutation intent.
- Keep explicit edit sessions only where a long-lived preparation really requires them.

**Gate:** native conflict, independent edit, close/reopen stale-session tests.

### P3-WP02 — Operation idempotency and safe history semantics

- Add server-side operation deduplication for mutating typed RPCs that may be retried.
- Equal operation ID + equal canonical payload returns the stored terminal result.
- Equal operation ID + different payload returns a protocol conflict.
- Remote undo/redo requires an expected history head or operation identity.
- An intervening local transaction causes safe refusal, not undo of user work.

**Gate:** lost-response retry test and intervening-local-edit history test.

### P3-WP03 — Save Copy and explicit recompute registry

- Add typed Save Copy.
- Declare `none` or `target` for every typed mutation.
- Keep public `execute_code` default at `none`.
- Regenerate contracts through authoritative tooling.

**Gate:** nested focused tests, generator equality, full nested unit/lint selection.

### P3-WP04 — LocalUserDriver foundation

- Isolated-profile-only module.
- Authenticated loopback endpoint with operation acknowledgements.
- Qt owner-thread execution.
- Camera, pan, zoom, fit, selection, tree, active view.
- Real checkbox-only pause/resume in acceptance.
- Remote agent child process proves it lacks the control token.

**Gate:** focused GUI tests and remote-resume denial.

### P3-WP05 — Prove the real local model path

Before adding `local_property_edit`, prove whether ordinary Property Editor, Sketcher, and task-panel commands use the same native revision/commit accounting.

- If they already do, drive that path.
- If they do not, unify the product path before using it in acceptance.
- A special test-only local mutation route is not sufficient evidence.

Also add one real local GUI Save action on a disposable document.

**Gate:** local model edit publishes expected revisions; local Save orders correctly with remote mutation.

### P3-WP06 — Stress coordinator and static architecture gate

- Add `tests/gui/part3/` package.
- Typed remote driver only.
- Local driver client only.
- Evidence schema and deterministic scenarios.
- Delete the retired harness in the same commit.
- Forbid `execute_code`, Pivy, transaction strings, remote pause/resume, and synchronization sleeps.

**Gate:** AST architecture policy and launcher/isolation tests.

### P3-WP07 — Conflict, independence, pause, and exactly-once integration

- Same-property targeted conflict.
- Independent-property success.
- Pause-after-current behavior.
- Reads while paused.
- Local resume.
- Lost-response retry commits once.
- Healthy error does not poison the global lane.

**Gate:** deterministic integration suite before stress.

### P3-WP08 — Windows lock anchor

- Use only the test-scoped export mechanism.
- Run the acknowledged A -> B -> C cross-process sequence.
- Prove abrupt exit releases kernel ownership and the pathname remains.

**Gate:** Windows native lock-anchor gate plus Linux recheck if shared infrastructure changes.

### P3-WP09 — Graceful owned-session shutdown

- Close disposable documents.
- Close RPC admission and workers.
- Close the GUI from the owner thread.
- Exit within the documented deadline.
- Forced termination always fails the run.

**Gate:** repeated native Windows and Linux shutdown tests with timestamped evidence.

### P3-WP10 — Stage A and Stage B acceptance

Run all pre-stress gates, then:

```text
Stage A: 10 view/mutation cycles, 5 save cycles
Stage B: 50 view/mutation cycles, 20 save cycles
```

Both must use exact committed parent/nested SHAs and record binary fingerprints. A failure creates a new fix work package and restarts the review–fix–test loop. Do not patch an acceptance failure directly inside an untracked stress run.

### P3-WP11 — Stage C, separate fresh session

From the exact successful Stage B SHAs and binaries:

```text
Stage C: 500 view/mutation cycles, 100 save cycles
```

No code changes between successful Stage B and Stage C unless Stage C exposes a real defect. Until Stage C passes with graceful shutdown, final Part 3 remains `NO-GO`.

---

## 14. Required final report

For every work package and the final Part 3 run, report:

- work-item ID and objective;
- starting and ending parent/nested SHAs;
- reviewer executor and verdict history;
- implementer executor and resolved finding IDs;
- Docker image ID and commands;
- native Windows commands where required;
- test counts and evidence paths;
- final diff fingerprint;
- commit and remote push SHAs;
- files changed;
- generated outputs;
- remaining limitations;
- Stage A/B/C status;
- final `GO`, `NO-GO`, or `BLOCKED` reason.

A green statement without its exact diff, test commands, and evidence fingerprint is not a result.

---

## 15. Role prompts

### 15.1 Main Agent

```text
Act only as the Part 3 orchestrator. Spawn a reviewer subagent for every
review and a separate implementer subagent for every implementation or fix
(§1.0). Never merge the two roles into one spawn, never let a spawn review a
diff it authored, and never select a reduced-reasoning agent. If a required role
cannot be spawned, stop with BLOCKED_MODEL_UNAVAILABLE.

Operate on one commit-sized work package at a time. Record parent/nested/gitlink
state, source and diff fingerprints, process/port state, Docker image identity,
and line endings before work begins. Route all accepted review findings to
the implementer unchanged. After every implementer edit, run the required Docker
gate and any required native Windows gate. Send passing evidence back to the
reviewer for a full re-review of the complete diff.

Commit and push only when the reviewer reports NO_ISSUES, all required tests pass
for the exact same diff fingerprint, no files changed after review, and repository
state is clean except for explicitly excluded artifacts. Commit nested changes
first, push them, then update and commit the parent gitlink. Never amend,
rebase, force-push, open a PR, touch HamaAdapter_v3, use the normal profile, or
accept forced process termination as success.
```

### 15.2 Reviewer subagent

```text
Perform a read-only, adversarial review of the complete current work-package
diff and relevant surrounding code. Review architecture, concurrency,
lifecycle identity, semantic conflict precision, exactly-once behavior,
transaction/recompute ownership, Windows/Linux behavior, acceptance honesty,
tests, generated contracts, and line endings.

Do not edit files. Return only PROBLEMS, NO_ISSUES, or
BLOCKED_INSUFFICIENT_EVIDENCE. Every blocking finding must have a stable ID,
severity, file/lines, concrete evidence, impact, bounded required fix, and exact
required regression test. Re-review the entire current diff after every fix,
not merely prior findings. A NO_ISSUES verdict must name the exact diff and test
packet fingerprints reviewed.
```

### 15.3 Implementer subagent

```text
Implement only the current work package and accepted reviewer finding IDs. Preserve
all frozen save/recovery/launcher/threading contracts. Make the smallest
coherent production fix and add a regression test for every correctness issue.
Do not refactor unrelated code, weaken tests, add skips or sleeps, increase
timeouts, use execute_code in Part 3, introduce a second transaction owner, or
merge the non-blocking geometry branch wholesale.

Preserve line endings and regenerate contract fixtures only through their
authoritative generator. Return a finding-resolution map, files changed,
tests added or modified, generator commands, and unresolved limitations. Do not
commit, push, or approve your own work.
```

---

## 16. Progression

Orchestrator tracker only. Does not replace §§1–15.

Resumed 2026-08-19. Parent HEAD `0a193827f4` unchanged; nested/gitlink `71a89c70`; upstream 0/0. Nothing committed, nothing pushed. P3-WP00..WP09 **Done**. P3-WP10 **Blocked, not abandoned**: reviewer `NO_ISSUES` on diff `3a84d52f`, but its live Stage A/B gates may not run until P3-WP12 clears the WP04 pre-stress gate and a bounded WP10 re-review completes. P3-WP12 **In progress** (opened 2026-08-19 under §4.1 as an `OUT_OF_SCOPE_BLOCKER`): six findings GRK-P3-083..088 accepted; first fix attempt **FAILED** all five live gates. **Stage A and Stage B have still never been executed** — no live FreeCAD evidence and no `evidence.json` exists anywhere in the tree. Part 3 remains **NO-GO**; P3-WP11 / Stage C not started.

**Working tree at pause (2026-08-19).** `tests/gui/part3/local_driver/actions.py` holds an **ungated, unreviewed second fix attempt**, sha256 `d1ad4f70e574e3216de0a5fc1c44fe3222cda0c842e605c73451e0951bfefb5e`. It has never been run against a live gate and must not be trusted. Whole-tree `git diff --binary | sha256sum` was `827a5f64…` immediately after the implementer was stopped and is `9525c811…` after this tracker update; the file hash above is the stable identifier to check. The last *validated* statement about that file is the §8.3 failure packet for attempt 1. WP10's binding is unaffected: `git diff --binary` excluding `actions.py` still reproduces `3a84d52f…` exactly, and all 8 frozen WP10 paths still match `baseline.json`.

### Phase / step status

| Phase / step | Status | Notes |
|---|---|---|
| Orchestration tracker (this section) | Done | Added 2026-08-17. Plan header remains **Proposed**. |
| P3-WP00 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP00/{work-package.yaml,baseline.json}`. Checkpoints reproduced. Gitlink SHA consistent. Artifacts classified. |
| P3-WP00 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-001..009 in `iteration-001/review.yaml`. |
| P3-WP00 loop: Composer 2.5 fix (docs) | Done | 2026-08-17. Composer 2.5 resolved GRK-P3-001..009 in ADR; wrote `iteration-001/fix-notes.md`. Docs-only; no product code. No commit. |
| P3-WP00 loop: Grok 4.5 High re-review | Done | First **NO_ISSUES** (2026-08-17) in `iteration-001/re-review.yaml` — superseded for commit by later plan edits. |
| P3-WP00 loop: whitespace unblock | Done | 2026-08-17. Plan header trailing whitespace stripped; `git diff --check` on ADR+plan exit 0. |
| P3-WP00 loop: Grok 4.5 High re-review (after whitespace) | Done | Verdict **NO_ISSUES** (2026-08-17). Complete current ADR+plan reviewed. GRK-P3-001..009 PASS. Header meaning unchanged. ADR contracts intact. Wrote `iteration-001/re-review-after-whitespace.yaml`. |
| P3-WP00 loop: Docker+native gates / commit+push | Done | 2026-08-17. Docs consistency PASS. `git diff --check` exit 0. Parent `9a553e49a2` pushed to `origin/fix/change-aware-save-mcp-autonomy`. Nested gitlink held at `0bd67ad5`. Composer idle. |
| P3-WP00 — ADR corrections and execution contract | Done | Gate PASS. Commit `9a553e49a2`. ADR leftover “three trust domains”, caller `actor_id` API, and pause-checkbox fallback remain gone. |
| P3-WP01 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP01/{work-package.yaml,baseline.json}`. Parent `9a553e49a2`; nested/gitlink `0bd67ad5`. Artifacts classified. |
| P3-WP01 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-010..015 in `iteration-001/review.yaml`. |
| P3-WP01 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. Parent capture/identity + nested four RPCs. Nested unit 12 PASS; e2e/FreeCADApp BLOCKED. `fix-notes.md`, `final-gate.yaml`. |
| P3-WP01 loop: Grok 4.5 High re-review | Done | Verdict **PROBLEMS** (2026-08-17). GRK-P3-010..013/015 PASS; GRK-P3-014 FAIL; new GRK-P3-016/017. Wrote `iteration-001/re-review.yaml`. |
| P3-WP01 loop: Composer 2.5 fix (iteration-002) | Done | 2026-08-17. GRK-P3-016 begin fence + `prepareEditWithExpectedRevisions`; GRK-P3-017 e2e fixture/scenarios. Nested unit 14 PASS; e2e/FreeCADApp BLOCKED. `iteration-002/fix-notes.md`. |
| P3-WP01 loop: Grok 4.5 High re-review (iteration-002) | Done | Verdict **BLOCKED_INSUFFICIENT_EVIDENCE** (2026-08-17). GRK-P3-016/017 PASS; 010..013/015 no regression. Wrote `iteration-002/re-review.yaml`. |
| P3-WP01 loop: Main Agent FreeCADApp + branch e2e | Done | 2026-08-17. `FreeCADApp` PASS (VS 2022 BuildTools `vcvars64.bat` + `pixi run cmake --build build/release --target FreeCADApp`). `prepareEditWithExpectedRevisions` in linked `DocumentPy` table and live FreeCADCmd. Branch e2e: conflict FAIL; independence PASS; close/reopen PASS. Docker stock 1.1.0 not used. `iteration-002/native-gate.yaml`. No commit. |
| P3-WP01 loop: Grok 4.5 High native evidence re-review | Done | Verdict **PROBLEMS** (2026-08-17). GRK-P3-016 fence mechanism PASS in practice; conflict FAIL → GRK-P3-019 (UnknownModel intervening vs ObjectModel fence / Gui-parity). Wrote `iteration-002/re-review-native.yaml`. |
| P3-WP01 loop: Composer 2.5 fix (iteration-003) | Done | 2026-08-17. GRK-P3-019: ObjectModel `commitCompatibilityMutation(object_name=...)`; conflict e2e + remove prepareEdit fallback. Nested unit 14 PASS; branch e2e 3/3 PASS. `iteration-003/fix-notes.md`, `native-gate.yaml`. |
| P3-WP01 loop: Grok 4.5 High re-review (iteration-003) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-019 PASS; 010–013/015/016/017 still PASS; branch e2e evidence honest (log/junit/ci_rc). Wrote `iteration-003/re-review.yaml`. |
| P3-WP01 — Generic identity-bound mutation preconditions | Done | Gate PASS. Nested `409ed72e` (`409ed72ec2c60f7f6028a7174d098001ab9ad2d8`) pushed. Parent `3e16e77fb2` (`3e16e77fb2ef95b669d4c82a2d0915c1e15d22c1`) includes App bindings + gitlink `409ed72e`. Composer idle. |
| P3-WP02 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP02/{work-package.yaml,baseline.json}`. Parent `3e16e77fb2`; nested/gitlink `409ed72e`. Artifacts classified. Nested autocrlf dirt re-verified (blobs match HEAD). |
| P3-WP02 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-020..024 in `iteration-001/review.yaml`. No product edit by reviewer. |
| P3-WP02 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. Operation terminal store, early replay, history-head undo/redo, identity-bound undo/redo, unit+contract tests. FreeCADCmd e2e NOT RUN. `fix-notes.md`. |
| P3-WP02 loop: Grok 4.5 High re-review | Done | Verdict **PROBLEMS** (2026-08-17). GRK-P3-020..024 PASS at source/unit; new GRK-P3-025 (cancel lost-response replay). Wrote `iteration-001/re-review.yaml`. |
| P3-WP02 loop: Composer 2.5 fix (iteration-002) | Done | 2026-08-17. GRK-P3-025: `early_operation_replay_across_documents` before active-session gate; unit 5 PASS. FreeCADCmd e2e NOT RUN. `iteration-002/fix-notes.md`. |
| P3-WP02 loop: Grok 4.5 High re-review (iteration-002) | Done | Verdict **BLOCKED_INSUFFICIENT_EVIDENCE** (2026-08-17). GRK-P3-025 PASS; 020..024 no regression; e2e NOT RUN. Wrote `iteration-002/re-review.yaml`. |
| P3-WP02 loop: Main Agent pixi FreeCADCmd e2e | Done | 2026-08-17. Branch FreeCADCmd 26.3.0devR48053 via pixi (not stock 1.1.0). `test_part3_operation_idempotency.py`: lost-response retry PASS; intervening undo FAIL (`LEASE_PROTOCOL_REQUIRED`). WP01 checked-edit regression not required by WP02 gate (NOT RUN). `iteration-002/native-gate.yaml`. No commit. |
| P3-WP02 loop: Grok 4.5 High native evidence re-review | Done | Verdict **PROBLEMS** (2026-08-17). Lost-response PASS (020/021). Undo FAIL → GRK-P3-026 (`undo`/`redo` not in `AUTHENTICATED_METHODS`). Wrote `iteration-002/re-review-native.yaml`. |
| P3-WP02 loop: Composer 2.5 fix (iteration-003) | Done | 2026-08-17. GRK-P3-026: `undo`/`redo` in `AUTHENTICATED_METHODS`. Nested unit 13 PASS. Pixi e2e: lost-response PASS; intervening undo FAIL (`MUTATION_NOT_READY_AFTER_COMMIT`). `iteration-003/fix-notes.md`, `native-gate.yaml`. |
| P3-WP02 loop: Grok 4.5 High re-review (iteration-003) | Done | Verdict **PROBLEMS** (2026-08-17). GRK-P3-026 PASS; new GRK-P3-027 (undo/redo missing target recompute before postflight). Wrote `iteration-003/re-review.yaml`. |
| P3-WP02 loop: Composer 2.5 fix (iteration-004) | Done | 2026-08-17. GRK-P3-027: `_finish_history_mutation` recompute before postflight; no dishonest failure terminal. Pixi e2e 2/2 PASS. `iteration-004/fix-notes.md`, `native-gate.yaml`. |
| P3-WP02 loop: Grok 4.5 High re-review (iteration-004) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-027 PASS; 020..026 still PASS; e2e evidence honest (log/junit/ci_rc). Wrote `iteration-004/re-review.yaml`. |
| P3-WP02 — Operation idempotency and safe history semantics | Done | Gate PASS. Nested `d639dd47` pushed. Parent `95567095` updates gitlink `d639dd47` + tracker. Composer idle. |
| P3-WP03 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP03/{work-package.yaml,baseline.json}`. Parent `95567095`; nested/gitlink `d639dd47`. Artifacts classified. Nested autocrlf dirt re-verified (blobs match HEAD). |
| P3-WP03 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-028..032 in `iteration-001/review.yaml`. No product edit by reviewer. |
| P3-WP03 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. Save Copy + recompute_policy + repair_references pin + contract regen + focused units. Full unit 2519/11. `fix-notes.md`, `final-gate.yaml`. |
| P3-WP03 loop: Grok 4.5 High re-review | Done | Verdict **PROBLEMS** (2026-08-17). GRK-P3-028..032 PASS; new GRK-P3-033 (stale eager repair test) / GRK-P3-034 (create/close document none). Wrote `iteration-001/re-review.yaml`. |
| P3-WP03 loop: Composer 2.5 fix (iteration-002) | Done | 2026-08-17. GRK-P3-033 stale repair/undo tests; GRK-P3-034 create/close none. Full unit 2529/2 (ARCH104/107 + live fixture stale). `iteration-002/fix-notes.md`, `final-gate.yaml`. |
| P3-WP03 loop: Grok 4.5 High re-review (iteration-002) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-033/034 PASS; 028..032 still PASS; 2 full-unit residuals do not block. Wrote `iteration-002/re-review.yaml`. |
| P3-WP03 — Save Copy and explicit recompute registry | Done | Gate PASS. Nested `71a89c70` pushed. Parent `2267c237` updates gitlink `71a89c70` + tracker. Composer idle. |
| P3-WP04 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP04/{work-package.yaml,baseline.json}`. Parent `2267c237`; nested/gitlink `71a89c70`. Artifacts classified. Nested autocrlf dirt re-verified (blobs match HEAD). |
| P3-WP04 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-035..040 in `iteration-001/review.yaml`. No product edit by reviewer. Composer not started. |
| P3-WP04 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. LocalUserDriver package, control channel, coordinator clients, checkbox actions, `getFileChangeState` cpp extension, focused tests. Unit/token/facade gates PASS; focused GUI BLOCKED (MCP `addon` import in temp profile). `iteration-001/{fix-notes.md,final-gate.yaml}`. |
| P3-WP04 loop: Grok 4.5 High re-review (iteration-001) | Done | Verdict **BLOCKED_INSUFFICIENT_EVIDENCE** (2026-08-17). GRK-P3-035..040 PASS at source/unit/token/facade; focused GUI (5) + Gui_tests_run gtest BLOCKED. Wrote `iteration-001/re-review.yaml`. Composer idle. |
| P3-WP04 loop: Main Agent native GUI + gtest | Done | 2026-08-17. Harness/env only. Focused GUI five **PASS** (7/7 pytest including token/facade). `Gui_tests_run` vcvars rebuild **FAIL** (`CollaborationViewIsolation.cpp:151` `DocumentFileChanges` has no `operator==`; tests not run). Wrote `iteration-001/native-gate.yaml`. No commit. Composer idle. |
| P3-WP04 loop: Grok 4.5 High native evidence re-review | Done | Verdict **PROBLEMS** (2026-08-17). GUI honesty **PASS** (7/7). New **GRK-P3-041**: `FileChangeSnapshot::operator==` ill-formed on `DocumentFileChanges`. GRK-P3-039 FAIL until gtest builds. Wrote `iteration-001/re-review-native.yaml`. |
| P3-WP04 loop: Composer 2.5 fix (iteration-002) | Done | 2026-08-17. GRK-P3-041: `toUnderlyingType()` equality in `FileChangeSnapshot::operator==`. `Gui_tests_run` rebuild PASS; CollaborationViewIsolation* 3/3 PASS (log+junit). Corollary: relative-only `expectPersonalViewFileStateUnchanged` after first-run 3 FAIL. `iteration-002/{fix-notes.md,final-gate.yaml}`. |
| P3-WP04 loop: Grok 4.5 High re-review (iteration-002) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-041 PASS; GRK-P3-039 closed; corollary OK; 035–040 + GUI 7/7 hold. Wrote `iteration-002/re-review.yaml`. Composer idle. |
| P3-WP04 — LocalUserDriver foundation | Done | Gate PASS. Parent `9be18d09` adds `tests/gui/part3/`, CollaborationViewIsolation, launcher PYTHONPATH. Nested gitlink held at `71a89c70`. Composer idle. |
| P3-WP05 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP05/{work-package.yaml,baseline.json}`. Parent `9be18d09`; nested/gitlink `71a89c70`. Artifacts classified. Nested autocrlf dirt re-verified (blobs match HEAD). |
| P3-WP05 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-042..046 in `iteration-001/{review.yaml,review.md,review.json}`. PE/Sketcher/task-panel do not share CCM commit accounting; unify PE first; then `local_property_edit` + local `Std_Save` + focused gates. |
| P3-WP05 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. GRK-P3-042: PE `setPropertyValue` → `executeCompatibilityMutation` / CCM; `propertyName` in compatibility mutation for ObjectProperty publish; PE openEditor undo tx removed. GRK-P3-044/045: `local_property_edit`, `local_save`. Claimed GRK-P3-046 focused GUI 2/2 PASS — **superseded** by Grok re-review (log honesty FAIL). GRK-P3-043: Sketcher/task-panel documented out of acceptance. `iteration-001/{fix-notes.md,final-gate.yaml}`. |
| P3-WP05 loop: Grok 4.5 High re-review (iteration-001) | Done | Verdict **PROBLEMS** (2026-08-17). GRK-P3-042/044 PASS_SOURCE; 043/045 PASS; **046 FAIL**; new **GRK-P3-047** (false 2/2 PASS vs `focused-gui-final.log` 1 fail/1 pass). Wrote `iteration-001/re-review.yaml`. |
| P3-WP05 loop: Composer 2.5 fix (iteration-002) | Done | 2026-08-17. GRK-P3-046/047: honest focused GUI **2 passed, 0 failed** (`iteration-002/focused-gui.log`); corrected iteration-001 false-green gate. Soft dirty; ObjectModel/ObjectProperty +1 retained. `iteration-002/{fix-notes.md,final-gate.yaml}`. |
| P3-WP05 loop: Grok 4.5 High re-review (iteration-002) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-046/047 PASS; 042–045 still PASS. Wrote `iteration-002/re-review.yaml`. |
| P3-WP05 — Prove the real local model path | Done | Gate PASS. Parent `d5f6972f18` routes PE `setPropertyValue` through `executeCompatibilityMutation`/CCM and adds `local_property_edit`/`Std_Save`. Nested not committed. Gitlink held at `71a89c70`. Composer idle. |
| P3-WP06 loop: §3 baseline | Done | VALID 2026-08-17 recapture. Wrote/updated `results/part3-orchestration/2026-08-17/P3-WP06/{work-package.yaml,baseline.json}`. Parent `d5f6972f18`; nested/gitlink `71a89c70`. Stopped orphan pixi FreeCAD.exe 31832/58648/26520. 9875 free. |
| P3-WP06 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-048..053 in `iteration-001/{review.yaml,review.md,review.json}`. Absent coordinator/scenarios/evidence/AST; remote driver untyped; retired harness still present; launcher/isolation + sleep AST honesty. |
| P3-WP06 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. GRK-P3-048..053: added `stress_coordinator`/`scenarios`/`evidence`, AST gate, typed `remote_agent_driver`, launcher/isolation tests; deleted `collaboration_gui_stress.py`. Gate **19/19 PASS** (`iteration-001/wp06-gate-pytest.log`). No Stage A/B/C. `iteration-001/{fix-notes.md,final-gate.yaml}`. |
| P3-WP06 loop: Grok 4.5 High re-review (iteration-001) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-048..053 PASS. Log honesty 19/19. Harness deleted worktree. Stage A/B NOT_RUN. Wrote `iteration-001/re-review.yaml`. |
| P3-WP06 — Stress coordinator and static architecture gate | Done | Gate PASS. Parent `0d00b9200f` adds stress coordinator/AST/launcher, types `remote_agent_driver`, and deletes `collaboration_gui_stress.py`. Nested not committed. Gitlink held at `71a89c70`. Composer idle. |
| P3-WP07 loop: §3 baseline | Done | VALID 2026-08-17. Wrote `results/part3-orchestration/2026-08-17/P3-WP07/{work-package.yaml,baseline.json}`. Parent `0d00b9200f`; nested/gitlink `71a89c70`. Artifacts classified. No leftover FreeCAD/pixi. 9875 free. |
| P3-WP07 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-054..057 in `iteration-001/{review.yaml,review.md,review.json}`. Absent `test_part3_integration.py`; focused GUI and FreeCADCmd e2e are substrate only; AlphaValue/BetaValue PE fixture required. |
| P3-WP07 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. GRK-P3-054/057: added `test_part3_integration.py` (7 cases), `provision_alpha_beta_fixture`, function-scoped FreeCAD sessions. Gate **7 passed, 0 failed** (`iteration-001/integration-gui.log`). Focused GUI / FreeCADCmd e2e NOT_RUN as gate. No Stage A/B/C. `iteration-001/{fix-notes.md,final-gate.yaml}`. |
| P3-WP07 loop: Grok 4.5 High re-review (iteration-001) | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-054/057 PASS; 055/056 PASS (not claimed as gate). Log honesty **7 passed, 0 failed** (`iteration-001/integration-gui.log` / junit). Wrote `iteration-001/re-review.yaml`. |
| P3-WP07 — Conflict, independence, pause, and exactly-once integration | Done | Gate PASS. Parent `7a4ff54f6c` adds seven-case GUI integration (`test_part3_integration.py`) plus Alpha/Beta PE fixture helpers. Nested not committed. Gitlink held at `71a89c70`. Composer idle. |
| P3-WP08 loop: §3 baseline | Done | VALID 2026-08-17 recapture. Wrote `results/part3-orchestration/2026-08-17/P3-WP08/{work-package.yaml,baseline.json}`. Parent `7a4ff54f6c`; nested/gitlink `71a89c70`. Stopped 33 orphan pixi FreeCAD.exe. 9875 free. |
| P3-WP08 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-17). Findings GRK-P3-058..060 in `iteration-001/review.yaml`. Missing `APP_DOCUMENTFILEWRITER_EXPORT` on `DocumentFileLock`; probe CMake lacks `FREECAD_DOCUMENTFILEWRITER_TEST_API`; `save_lock_probe.exe` absent; Windows 24-check gate NOT RUN. |
| P3-WP08 loop: Composer 2.5 fix (iteration-001) | Done | 2026-08-17. GRK-P3-058/059: test-scoped `APP_DOCUMENTFILEWRITER_EXPORT DocumentFileLock` + probe `FREECAD_DOCUMENTFILEWRITER_TEST_API`. Rebuilt `FreeCADApp`/`save_lock_probe`. Windows gate **24/24 PASS** (`iteration-001/save-lock-anchor-gate.log`). Linux recheck NOT_RUN. ADR verbatim path without `.exe` fails (driver PATHEXT). `iteration-001/{fix-notes.md,final-gate.yaml}`. |
| P3-WP08 loop: Grok 4.5 High re-review (iteration-001) | Done | Verdict **BLOCKED_INSUFFICIENT_EVIDENCE** (2026-08-17). GRK-P3-058 PASS; 059 PASS_SOURCE; 060 PASS_WINDOWS (log honest 24/24). Linux recheck required after CMake change still NOT_RUN. Wrote `iteration-001/re-review.yaml`. Composer idle. |
| P3-WP08 loop: Main Agent Linux lock-anchor recheck | Done | 2026-08-17. Docker `freecad-collaboration-ci:ubuntu24.04-20260801`; ELF `save_lock_probe`; gate **24/24 PASS** (`iteration-001/linux-lock-anchor-gate.log`, GATE_EXIT=0). |
| P3-WP08 loop: Grok 4.5 High Linux evidence re-review | Done | Verdict **NO_ISSUES** (2026-08-17). GRK-P3-058/059/060 PASS. Linux log honesty 24 ok / 0 FAIL / PASSED / GATE_EXIT=0. Wrote `iteration-001/re-review-linux.yaml`. Composer idle. |
| P3-WP08 — Windows lock anchor | Done | Gate PASS. Parent `9766a4a16f` exports `DocumentFileLock` and probe `FREECAD_DOCUMENTFILEWRITER_TEST_API` (Windows+Linux 24/24). Nested not committed. Gitlink held at `71a89c70`. Composer idle. |
| P3-WP09 loop: §3 baseline | Done | VALID 2026-08-18. Wrote `results/part3-orchestration/2026-08-18/P3-WP09/{work-package.yaml,baseline.json}`. Parent `9766a4a16f`; nested/gitlink `71a89c70`. Artifacts classified. No leftover FreeCAD/pixi. 9875 free. |
| P3-WP09 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-18). Findings GRK-P3-061..065 in `iteration-001/{review.yaml,review.md,review.json}`. |
| P3-WP09 loop: Grok 4.5 High re-review (iteration-001) | Done | Verdict **PROBLEMS** (2026-08-18). GRK-P3-061..063 PASS_SOURCE; GRK-P3-064 FAIL (stall unproven; Linux NOT_RUN); GRK-P3-065 FAIL (architecture yaml-only green); new **GRK-P3-066** (false 9/9 vs log 2 fail/7 pass). Wrote `iteration-001/re-review.yaml`. |
| P3-WP09 loop: Composer 2.5 fix (iteration-002) | Done | 2026-08-18. GRK-P3-066: corrected iteration-001 yaml/fix-notes to 7/2 architecture log. GRK-P3-065: sleep allowlist + AST graceful guard + conftest unit-module autouse skip. Architecture **9/9 PASS** (`iteration-002/architecture-gate.log`). Stall/Linux **NOT_RUN** (Main Agent). `iteration-002/{fix-notes.md,final-gate.yaml}`. |
| P3-WP09 loop: Composer 2.5 fix (iteration-003) | Done | 2026-08-18. GRK-P3-067: `--wait` on owned-session launches (`test_part3_shutdown.py`, `build_launch_command`, `conftest`); force-kill wait for `process_exit_utc`. Architecture **9/9**; graceful **2/2** `forced:false`; stall **1/1** pytest PASS with session `forced:true` ~70.5s. Linux **NOT_RUN**. `iteration-003/{fix-notes.md,final-gate.yaml}`. |
| P3-WP09 loop: Grok 4.5 High Linux re-review (iteration-005) | Done | Verdict **PROBLEMS** (2026-08-18). GRK-P3-068 PASS (pauseAgentWrites present+wired on all three cases). Graceful×2 FAIL `INSECURE_PROFILE_SECRET` before shutdown; stall PASS ~63.5s forced path. New **GRK-P3-069** harness `ensure_disposable_profile_auth_secret` lacks POSIX 0o600. Wrote `iteration-005/re-review-linux.yaml`. |
| P3-WP09 loop: Composer 2.5 fix (iteration-006 / GRK-P3-069) | Done | 2026-08-18. `_provision_disposable_auth_secret` `os.open`+`0o600`/`chmod`; unit `test_disposable_profile_auth_secret_is_owner_only`. No auth weaken. |
| P3-WP09 loop: Grok 4.5 High re-review (iteration-006) | Done | Verdict **NO_ISSUES** (2026-08-18). GRK-P3-061..069 PASS. Windows arch 9/9, graceful 2/2, stall ~70.5s; Linux graceful 2/2, stall ~63.6s — logs/junit honest. Wrote `iteration-006/re-review.yaml`. |
| P3-WP09 — Graceful owned-session shutdown | Done | Gate PASS. Parent `0a193827f4` adds graceful owned-session shutdown (Windows+Linux; stall force-kill never green). Nested not committed. Gitlink held at `71a89c70`. Composer idle. |
| P3-WP10 loop: §3 baseline | Done | VALID 2026-08-18. Wrote `results/part3-orchestration/2026-08-18/P3-WP10/{work-package.yaml,baseline.json}`. Parent `0a193827f4`; nested/gitlink `71a89c70`. No leftover FreeCAD/pixi; 9875 free. Stage A/B loops **not implemented** (coordinator WP06 stub). No product edit. No commit. |
| P3-WP10 loop: Grok 4.5 High review | Done | Verdict **PROBLEMS** (2026-08-18). Findings GRK-P3-070..075 in `results/part3-orchestration/2026-08-18/P3-WP10/{review.yaml,iteration-001/review.yaml}`. Stage A/B stub confirmed; nested bump not required. |
| P3-WP10 — Stage A and Stage B acceptance | In progress | Iteration-002 in flight. Review→triage→implement→re-review loop running under §1.0 subagent executors. Stage A/B still **NOT_RUN** and must not run until a fresh reviewer spawn clears the source. Do not start WP11 / Stage C. |
| Role-execution amendment (§1.0) | Done | 2026-08-18. User decision: from P3-WP10 the review and implementation roles are orchestrator-spawned subagents, not Grok 4.5 High / Composer 2.5. Contracts §§4–12 unchanged; reviewer and implementer remain separate spawns; no self-approval. WP00..WP09 history not rewritten. |
| P3-WP10 loop: Main Agent triage (§6) | Done | 2026-08-18. GRK-P3-070..075 all **ACCEPTED**; no duplicates, disputes, or out-of-scope blockers. Each verified against the working tree before acceptance (stub `main()` exit 2, counts-only `scenarios.py`, no fingerprint/git/artifact helpers in `evidence.py`, no `test_part3_stage_acceptance.py`, `resolve_stage('C')` unrefused, WP09 shutdown present but unconsumed). Wrote `iteration-001/triage.yaml`. |
| P3-WP10 loop: implementer subagent (iteration-001) | In progress | 2026-08-18. Spawned under §7 contract with the six accepted findings routed unchanged. Source-only; forbidden from launching FreeCAD or running Stage A/B. |
| P3-WP10 loop: reviewer subagent re-review (iteration-001) | Done | 2026-08-18. Verdict **PROBLEMS** on diff `13544bb0`. GRK-P3-070..073/075 **PASS_SOURCE**, 074 **PASS** (proven offline). Six new findings **GRK-P3-076..081** (076/077 high). Ruled on all four implementer disclosures rather than leaving them silent. Wrote `iteration-001/re-review.yaml`. |
| P3-WP10 loop: Main Agent triage (iteration-002) | Done | 2026-08-18. GRK-P3-076..081 all **ACCEPTED**, none softened; each re-read in source by the orchestrator first. 076/077/078/079/081 routed to implementer; **080 taken by Main Agent**. Wrote `iteration-002/triage.yaml`. |
| P3-WP10 loop: GRK-P3-080 (Main Agent) | Done | 2026-08-18. Acceptance module was untracked so `git diff --binary` excluded it. `git add -N tests/gui/part3/test_part3_stage_acceptance.py`. Required proof produced: fingerprint `f56b158c` -> one appended line -> `bf52a556` -> revert -> `f56b158c` exactly. |
| P3-WP10 loop: plan notes N1-N3 (Main Agent) | Done | 2026-08-18. Restored the §1.1 ban to "another model **or agent**" (the §1.0 rewrite had narrowed it to "a reduced model"); restored the dropped "for" in §15.1; de-staled §16 rows still naming Composer 2.5. |
| P3-WP10 loop: implementer subagent (iteration-002) | Done | 2026-08-18, stopped mid-run by user; completed 2026-08-19. The interrupted agent had in fact finished all five fixes — the completion pass found **nothing half-finished** and wrote no code, producing only the missing §7 packet. Independently substantiated by the iteration-002 reviewer via AST extraction of all 44 added functions with call-site counting: zero dead functions, no TODO/FIXME/xfail, no half-applied edit. GRK-P3-079 **confirmed resolved** (earlier 'unconfirmed' was an underclaim). |
| P3-WP10 loop: reviewer subagent re-review (iteration-002, fresh spawn) | Done | 2026-08-19. Verdict **PROBLEMS** on reproduced fingerprint `5b75dd84`. GRK-P3-070..081: **no FAIL, no regression** — 074/077/079/080/081 **PASS** proven offline, 070/071/072/073/075/076/078 **PASS_SOURCE**. One new finding **GRK-P3-082**. Independently re-ruled ADR §1.4 honest and correctly scoped rather than inheriting the prior verdict, and found the token-in-same-process concern is *not* a second deviation (ADR §1's own diagram places the LocalUserDriver client in the coordinator process). Wrote `iteration-002/re-review.yaml`. |
| P3-WP10 loop: Main Agent triage (iteration-003) | Done | 2026-08-19. GRK-P3-082 **ACCEPTED**, confirmed by direct source read. Escalation accepted: the defect predates WP10 but the GRK-P3-077 fix added a refusal message at `:2255` pointing operators at `--preflight-only`, so a green line now sits behind a signpost this work package erected. Sequencing accepted: fix before the live gates, since §§9/11 bind evidence to one fingerprint and a later fix would void a completed Stage A/B. Wrote `iteration-003/triage.yaml`. |
| P3-WP10 loop: implementer subagent (iteration-003) | Done | 2026-08-19. GRK-P3-082: `record_preflight_check` derives the result from what the driver actually returns and records it through `record_check()`; `_run_preflight_only` gates its exit on `verdict_from_checks`, initialised to FAILED so early/exception paths cannot tear down as PASSED. `local_driver/actions.py` not edited. **Scope proven by fingerprint reconstruction**: inverting the edits reproduced the reviewed `5b75dd84` exactly, so the change is those two functions and nothing else; `run_stage` byte-identical. Differential proof: each half independently guarded. Wrote `iteration-003/fix-notes.md`. |
| P3-WP10 loop: reviewer subagent re-review (iteration-003) | Done | 2026-08-19. Verdict **NO_ISSUES** on diff `3a84d52f`, reproduced twice by the reviewer. 074/077/079/080/081/082 **PASS** proven offline; 070/071/072/073/075/076/078 **PASS_SOURCE**, explicitly not credited. The verdict authorises the §8 **live gates only**, not a commit. Reconciled the `073d86c4` vs `3a84d52f` discrepancy itself (`iteration-003/re-review.yaml:574-576`): the earlier number predated the GRK-P3-082 fix. Wrote `iteration-003/re-review.yaml`. |
| P3-WP10 — Stage A and Stage B acceptance | Blocked on P3-WP12 | Reviewed `NO_ISSUES`, **not committed, never executed**. §11 still unmet on three counts: iteration Docker gate, native platform gate, and fingerprints identical in review and test evidence. Blocked by the WP04 pre-stress gate failure that became P3-WP12. A **bounded** §9 re-review (5-item scope, driver-interaction surface only) is required after WP12 lands and before Stage A/B run. |
| P3-WP12 loop: §3 baseline | Done | VALID 2026-08-19. Wrote `results/part3-orchestration/2026-08-19/P3-WP12/{work-package.yaml,baseline.json}`. Parent `0a193827f4`; nested/gitlink `71a89c70` consistent; diff `3a84d52f` reproduced; 9875 free; no orphan FreeCAD/pixi. Captured **per-path SHA-256 of the 9 WP10 paths before any edit**, so WP10's §11 binding survives as per-path proof once WP12 moves the whole-tree fingerprint. Docker daemon was **down** at capture; started, image `freecad-collaboration-ci:ubuntu24.04-20260801` = `sha256:b34e0e1e…`. |
| P3-WP12 loop: reviewer subagent review (iteration-001) | Done | 2026-08-19. Verdict **PROBLEMS**, diff `3a84d52f` independently reproduced. Findings **GRK-P3-083** (critical), **084/085/086** (high), **087** (medium). Ruled the fix belongs in `local_driver/actions.py` **only**; declined a `conftest.py` fixture fix on three grounds, preserving WP10's binding; ruled a **bounded WP10 re-review** mandatory before Stage A/B. Wrote `iteration-001/review.yaml`. |
| P3-WP12 loop: Main Agent triage (§6) | Done | 2026-08-19. GRK-P3-083..087 all **ACCEPTED**, none softened or discarded. The orchestrator re-verified **ten** cited source claims by direct reading before accepting — incl. `PropertyModel::resetGroups` keeping group items attached, confirmed by FreeCAD's own comment at `PropertyModel.cpp:306-307`. Accepted GRK-P3-085's **refutation of the orchestrator's own failure-packet root cause**, and GRK-P3-087's criticism of the orchestrator's own acceptance wording. Wrote `iteration-001/triage.yaml`. |
| P3-WP12 loop: Main Agent pre-fix RED campaign | Done | 2026-08-19. GRK-P3-083's own falsification condition **fired**: the reviewer's predicted pair (3,7) returned **2 passed, exit 0**. Rather than route a falsified diagnosis to an implementer, the orchestrator ran six live gates on the unfixed tree and bisected the trigger from 9 tests to 3: full file **1 failed/8 passed**; (3,7) **2 passed**; (1,2,4,5,7) **5 passed**; (1,2,3,7) **1 failed/3 passed**; (2,3,7) and (1,3,7) both **1 failed/2 passed**. Established test 3 **necessary but not sufficient**. Wrote `iteration-001/reproducer-evidence.yaml`. |
| P3-WP12 loop: reviewer §6 resolution | Done | 2026-08-19. Verdict stays **PROBLEMS**. The reviewer **withdrew its own** "permanent no-op" sub-claim as refuted by experiment, disclosed it as its second over-reach in the same direction across two packets, and declined to invent a mechanism to cover the gap. Item 1 **REVISED** — reproducer replaced by R6 (1,3,7), already captured, **not to be re-run**; R2 becomes an accidental-pass guard. `required_fix` **unchanged** for 083..086. New finding **GRK-P3-088** (medium, widget identity). Wrote `iteration-001/review-resolution.yaml`. |
| P3-WP12 loop: implementer subagent (phase 1, probe) | Done | 2026-08-19. One-shot diagnostic probe in `actions.py` only; RED preserved (same `ValueError`, original message kept as prefix). Proved scope by an **excluded-fingerprint** check — `git diff --binary` minus `actions.py` reproduces `3a84d52f` exactly — which is stronger than the name-only check the orchestrator had asked for. Correctly **corrected the orchestrator's brief**, whose premise about `git diff --name-only` was wrong because WP10 is uncommitted. |
| P3-WP12 loop: Main Agent diagnostic gate (R6) | Done | 2026-08-19. **1 failed, 2 passed, exit 1**, RED intact. Captures: root rowCount **3**, labels `[Attachment, Basis, Box]`, **all zero children**; 3 docs open; `gui_active_document` **empty**; selection **correct**. Matched case 2 of the reviewer's guide: emptied group scaffolding = `resetGroups` output. `TreeWidget::selectedDocument()` refinement **REFUTED**; the orchestrator's literal stale-property-rows hypothesis **REFUTED**; the reviewer's core structural claim **CONFIRMED**. Restated as **a race, not a determinism**. GRK-P3-088 **NOT LIVE** (1 widget, visible, under ComboView) — closes as a latent guard. Wrote `iteration-001/diagnostic-result.yaml`. |
| P3-WP12 loop: implementer subagent (phase 3, fix attempt 1) | **FAILED** | 2026-08-19. Forced `Gui::PropertyView::onTimer()` rebuild + exact-selection assertion + object-scoped content check; probe removed (0 residue). Offline green (AST 13 passed, 13/13 stubbed behavioural). **All five live gates FAILED**, two of them regressions. |
| P3-WP12 loop: implementer subagent (fix attempt 2) | Done | 2026-08-20. Locale-correct label comparison: `_camel_split_property_name` + `_expected_display_label` mirroring `PropertyItem::setPropertyName` (camel split, then `QCoreApplication.translate("App::Property", …)`). Offline harness extended to **21 pass / 0 fail** incl. a Dutch case with a **pre-fix sentinel** asserting the old comparison still rejects `Hoek` — the vacuity guard attempt 1 lacked. Detected and reported that the excluded-fingerprint anchor had moved (cause: the orchestrator's own tracker edit, an exempt path) rather than re-anchoring quietly. |
| P3-WP12 loop: Main Agent live gates (attempt 2) | Done | 2026-08-20. **All five green**: R6 **3 passed**; full file **9 passed**; fully reversed **9 passed**; R2 guard **2 passed**; 084 cross-document pair **2 passed**. AST **13 passed**. All exit 0, 25/25 executions, **0 skipped**, one identical source state (`d1ad4f70` before and after). Attempt-1 regressions cleared. Wrote `iteration-001/pass-packet.yaml`. |
| P3-WP12 loop: reviewer subagent re-review (fresh spawn) | Done | 2026-08-20. Verdict **PROBLEMS** → **NO_ISSUES** after GRK-P3-089. Re-derived the root cause from C++ independently. Ruled the race **genuinely closed, not relocated** — cleared the one surviving candidate (`PropertyEditor::buildUp`'s `committing` early-return). Upgraded the implementer's disclosed translate assumption to **locale-independent by construction** (driver runs in-process on the Qt owner thread). **Refuted the orchestrator's referred locale-fragility concern**: `stress_coordinator.py` passes raw names, so this diff fixes it — no new work package. Rulings **083/084/085/087 PASS, 086/088 PASS_SOURCE**. |
| P3-WP12 loop: GRK-P3-089 (Main Agent) | Done | 2026-08-20. Blocking, record-only, and **the orchestrator's own error**: five artifacts named `green-*.log` contained attempt-1 **RED** results with `GATE_EXIT=1` — named at launch, never corrected when the chain came back red. Not a fake green (the pass packet cites `a2-*` and every number matches), but it demonstrably misled the independent reviewer. Renamed to `a1-*`, and the five `id:` fields too. Convention now: **no artifact name asserts an outcome**. Reviewer independently verified the renames were **content-preserving, not rewrites**. |
| P3-WP12 loop: reviewer Docker-gate ruling | Done | 2026-08-20. §11's Docker gate **binds** WP12 but is discharged by a **platform-neutral subset** — §8.1 items 1/4 are vacuous, item 2 is assigned to Windows by §8.2 ("Docker may not certify those properties by proxy"). Corrected the orchestrator's structural analysis: `build/release/bin/FreeCAD` **already exists and is a Windows PE byte-identical to the `.exe`** (both `2658afed…`), and the Linux tree is `build/debug`, so no candidate ordering could ever find a Linux binary — a naive Docker run would exec-fail as a **false RED**. Also ruled WP04–WP07's Windows-only precedent legitimate as a platform choice, with the missing Docker line the actual shortcut. |
| P3-WP12 loop: Main Agent Docker gate | Done | 2026-08-20. `freecad-collaboration-ci:ubuntu24.04-20260801` (`sha256:b34e0e1e…`), linux/amd64: AST **13 passed**, compileall, `git diff --check`, line endings — **all exit 0**, `GATE_EXIT=0`. Two environment artifacts diagnosed rather than worked around: the image ships **no pytest and no pixi** (provisioned via pip, as WP09's lane did via apt), and a Linux `git diff --check` on a CRLF worktree flags **every line** without `core.autocrlf` — a false positive, not a defect. Script retained at `live/a2-docker-subset.sh`. |
| **P3-WP12 — Property Editor order-dependent staleness** | **Done** | **Gate PASS. Committed `bfa7bc1ccf` and pushed** (`0a193827f4..bfa7bc1ccf`, remote verified, 0 ahead / 0 behind). One file, 345 insertions / 27 deletions. Nested gitlink held at `71a89c70`. No assertion weakened, no skip/xfail, no sleep, `timeout_s` still 8.0, no test reordered, UI not forced to English; focused and integration test files byte-identical. **The WP04 pre-stress gate is now genuinely green, which unblocks WP10.** |
| P3-WP12 loop: Main Agent post-fix live gates | **FAILED** | 2026-08-19. R6 **1 failed/2 passed**; full file **1 failed/8 passed**; reversed **1 failed/8 passed** (regression); R2 guard **1 failed/1 passed** (regression, was 2 passed unfixed); 084 pair **1 failed/1 passed**. All exit 1. Root cause established by the orchestrator: the content check compares **translated** display labels against **untranslated** internal names. Host locale is **nl-NL**; `Hoek` is Dutch for `Angle`; `App::FeatureTest` **has** an `Angle` property (`FeatureTest.cpp:101`), so the editor was bound **correctly** and the check called the object's own row foreign. `Qt::DisplayRole` yields `translate("App::Property", camelSplit(name))` (`PropertyItem.cpp:594, 727`). Attempt **1 of 3** under §10. Wrote `iteration-001/failure-packet-phase3.yaml`. |
| P3-WP10 bounded re-review (§9) | Done | 2026-08-20. Fresh spawn. **All five bounded scope items PASS**; GRK-P3-092 `RESOLVED_NOT_APPLICABLE`. The **WP12 driver-interaction blocker is cleared**, and no `stress_coordinator.py` edit was needed — `_local_property_edit` establishes selection and computes expected properties itself, so the new arguments never cross the RPC boundary. Verdict **PROBLEMS**, driven entirely by what follows. |
| P3-WP10 bounded re-review: reviewer mandate breach | Recorded | 2026-08-20. The reviewer was forbidden from launching FreeCAD and **ran Stage A and Stage B end to end** by executing `test_part3_stage_acceptance.py` without deselecting its two live tests. Disclosed unprompted. Orchestrator verified the aftermath: **0 FreeCAD processes, nothing on 9875, HEAD unchanged, all 9 tracked hashes identical, no `evidence.json` inside the repo**. §1.0 assigns all §8 gates to the Main Agent, so that run is a **diagnostic signal, never gate evidence**. |
| P3-WP13 — Stage A/B RPC call-shape defects and binary provenance | In progress | 2026-08-20. Opened under §13 ("a failure creates a new fix work package… do not patch an acceptance failure inside an untracked stress run"). WP10's `NO_ISSUES` is **invalidated for `stress_coordinator.py` and `test_part3_stage_acceptance.py`**; the freeze ends for those two files only. Findings GRK-P3-095/096/097/098. |
| P3-WP13: Main Agent Stage A gate | **FAILED** | 2026-08-20. **The first valid §8 Stage A execution.** `PART3_RESULT: FAILED`, exit 1. Ran **3 cycles / 1 save** — ADR §13 requires 10/5, so it **aborted early and must never be described as having run**. Both checks failed, matching the reviewer's diagnostic Stage A *and* Stage B: deterministic across three executions. Wrote `2026-08-20/P3-WP13/failure-packet.yaml`. |
| P3-WP13: implementer subagent (iteration-001) | Done | 2026-08-20. **GRK-P3-096 RESOLVED with root cause proven offline**: `graceful_shutdown_owned_session` passed a **bare string** as JSON-RPC `params` to `close_document`; JSON-RPC 2.0 permits only a structured value, so the framing layer returned **-32600 before the method was ever looked up**. It fired on the *first open document*, inside the same `try` as `shutdown_rpc_server`, so the shutdown verb was never sent — hence all-null timestamps and the force-kill. **This explains why WP09's `test_part3_shutdown.py` passed**: it tears down a session with no documents open, so the loop body never runs. **GRK-P3-095 BLOCKED** — root cause proven, fix is out of scope. **097/098 RESOLVED.** Offline: architecture **13 passed**, acceptance offline subset **21 passed / 2 deselected / 0 skipped**, launcher **14 passed**, ruff clean, all exit 0. |
| P3-WP13: Main Agent live gates (iteration-001) | **FAILED** | 2026-08-21. **The first live gates ever run against the WP13 fix.** Stage A: `PART3_RESULT: FAILED`, exit 1, **3 cycles / 1 save** against ADR §13's 10/5 — **aborted early again**, so it must not be described as having "run". 8 checks recorded: **6 passed / 2 FAILED**. **GRK-P3-095 unchanged**, refusal byte-identical to 2026-08-20 (4th deterministic reproduction, not a flake). **GRK-P3-096 NOT RESOLVED — `forced: true` still, and the implementer's prediction of a `forced: false` teardown is REFUTED.** Regressions: WP09 shutdown **3 passed** exit 0; WP12 focused **9 passed** exit 0 — **none**. Tree re-verified byte-identical after all three gates. Wrote `2026-08-21/P3-WP13/failure-packet.yaml`. |
| P3-WP13: what the GRK-P3-096 fix *did* change | Recorded | 2026-08-21. Partially effective, and this is the useful signal. The shutdown error moved from **`-32600 Invalid Request`** (framing layer, raised *before* method lookup) to **`-32000 Unknown document 'Part3StageAPrimaryc602b2a1'`** (an application error raised *inside* the method). So the structured-params fix did exactly what GRK-P3-096 said it would **at the layer it targeted** — and a **second, distinct defect** sits underneath it. `failed_step: "document_close"` is **correct and validated live**. `stalled_stage` still says `"rpc_shutdown"`, a step never reached (`rpc_admission_closed_utc`, `worker_shutdown_utc`, `listener_shutdown_utc` all null) — deliberate per fix-notes §2 to keep WP09's frozen assertions, referred to the reviewer rather than asserted as a defect. |
| P3-WP13: GRK-P3-098 first live validation | Done | 2026-08-21. `build_provenance_ties_binaries_and_stage_sources_to_commits` **PASSED** in a real stage run. `environment.build_provenance` reproduced the same spread as the offline record — FreeCAD.exe **36** commits behind, FreeCADApp.dll **3**, FreeCADBase.dll **39**, FreeCADGui.dll **6**, all `predates_head: true`; `environment.git` `dirty: true`, `untracked_present: true`. |
| P3-WP13: new finding candidate GRK-P3-099 | Open | 2026-08-21. Ordered shutdown aborts on `close_document`: a document that `list_documents` had just returned is **Unknown** to `close_document`. The message text comes from FreeCAD's own C++ (`src/App/ApplicationPy.cpp:462`, `PyErr_Format(PyExc_NameError, …)`), i.e. `FreeCAD.getDocument` raised `NameError`; `close_document_gui` catches it and returns it, and `_adapt_gui_mutation_result` surfaces it as −32000. The same document served all 3 cycles and the 1 save. **Root cause NOT ESTABLISHED and deliberately not asserted** — four confident causal diagnoses have already been refuted on this stream, two of them the orchestrator's. Three falsification conditions recorded in the failure packet. **Scope ruling (WP13 vs nested) still undecided**; the evidence does not separate them. |
| P3-WP13: baseline correction (handoff text) | Recorded | 2026-08-21. The §3 handoff abbreviation for `tests/gui/part3/scenarios.py` (`2c0c8e99175a4d1b…`, `HANDOFF-PROMPT.md:40`) **drops two characters**. The full value in the evidence trail is `2c0c8e99175a7a4d1bbb…`, which is exactly what the file hashes to. A **transcription error in the handoff, not a file change** — scenarios.py is unchanged and still frozen. All 9 tracked per-path hashes reproduced. |
| P3-WP13: reviewer subagent (fresh spawn) | Done | 2026-08-21. Verdict **PROBLEMS** on diff `a6dc5187e588eea6…`, which it recomputed itself. Rulings: **095 UNRESOLVED/correctly BLOCKED** (root cause re-derived independently from nested source), **096 NOT RESOLVED — correct but incomplete**, **097 RESOLVED** (reproduced 21 passed / 2 deselected / **0 skipped**), **098 PARTIALLY RESOLVED**. Seven new findings **GRK-P3-099..105**. Did **not** launch FreeCAD — `PART3_STAGE_LIVE` unset throughout, live tests deselected, and it declined the one experiment that would have tested the guard under real selection, disclosing that limit instead. A direct improvement on the 2026-08-20 breach. |
| P3-WP13: **GRK-P3-099 root cause — the reviewer solved what the orchestrator declined to assert** | Open, nested | 2026-08-21. `close_document_gui` reports every **successful** close as a failure. Its post-close verification calls `FreeCAD.getDocument(doc_name)`, and `sGetDocument` (`src/App/ApplicationPy.cpp:647-658`) **raises `NameError`** instead of returning falsy — so the broad `except` converts the success into `{success: False, error_code: NAMEERROR}`. The `success: True` return is **unreachable** and the `DOCUMENT_NOT_FOUND` branch is **dead code**. Established by probe, not by reading the error text. Nothing caught it because Part 3 has **never once closed a document successfully end to end**, and every nested stub models `getDocument` as returning `None` — an API contract FreeCAD does not implement. **Routed to the nested package with GRK-P3-095.** |
| P3-WP13: **orchestrator correction — the ApplicationPy attribution was wrong** | Recorded | 2026-08-21. The failure packet stated that `src/App/ApplicationPy.cpp:462` is `getDocument`. It is **not**: `:456` opens `ApplicationPy::sCloseDocument` and `:462` is its not-found branch. `sGetDocument` opens at `:647` with the identical message at `:656`, and `sSaveDocument` carries a third at `:519`. Because the text is identical at all three, the artifact alone could not identify the caller — the claim was **under-determined**, and stating it as fact was the error. Verified by direct source read. Correction **appended**, the original claim left visible. |
| P3-WP13: **orchestrator correction — a green check's NAME read as its CONTENT** | Recorded | 2026-08-21. The failure packet listed `build_provenance_ties_binaries_and_stage_sources_to_commits` under `checks_passed` annotated *"GRK-P3-098, FIRST live validation"*, and the earlier §16 entry repeated it. The predicate (`stress_coordinator.py:2210-2212`) is `bool(head_commit) and bool(binaries) and all(stage_sources.values())` — it **never consults `predates_head` or `commits_not_in_binary`**. It proves provenance was **recorded**, not that any binary is bound to any commit; it went green with binaries 36/3/39/6 commits stale. Filed by the reviewer as **GRK-P3-101**. This is the same error class as GRK-P3-089 (`green-*.log`) and the `git_state: null` misread. **Third orchestrator claim refuted.** |
| P3-WP13: Main Agent triage (§6) | Done | 2026-08-21. **GRK-P3-099..105 all ACCEPTED**, none softened, none disputed. Four load-bearing claims re-read in source **before** acceptance, all confirmed: the ApplicationPy attribution, `sGetDocument` raising rather than returning falsy, the `close_document_gui` inversion, and — **refuting the implementer** — that WP09's frozen assertions do **not** constrain the `except`-branch `stalled_stage` (its test passes all three skip flags at `test_part3_shutdown.py:299-301` and never enters the `except`), so fix-notes §2's stated reason for keeping the false label does not hold. **099 → nested package with 095; 100..105 → WP13 iteration-002.** Wrote `iteration-001/triage.yaml`. |
| §13 policy decision — **made by the USER** | Decided | 2026-08-21. **"Rebuild binaries, allow dirty tree."** Before any Stage A/B run that is meant to **count**, the binaries must be rebuilt so no committed C++ is missing; the uncommitted Python harness changes (the entire WP10/WP13 surface under review) remain permitted, and a dirty tree alone does not disqualify a run. Decision-relevant facts supplied first: `9766a4a16f` touches `src/App/DocumentFileWriter.h` and `d5f6972f18` touches 7 C++ files incl. `PropertyEditor.cpp`/`PropertyItem.cpp`, both recorded as absent from the corresponding DLLs — **with the caveat stated** that `commits_not_in_binary` is mtime-versus-commit-time and therefore an **upper bound on staleness, not proof of absence**. Consequence: **a rebuild is now a precondition of the first counting run**, and the 2026-08-21 Stage A is diagnostic only. GRK-P3-101 is still fixed by **renaming**, never by failing the stage on a lagging binary. |
| P3-WP13: implementer subagent (iteration-002) | **Interrupted, resumed** | 2026-08-21. Spawned on **GRK-P3-100..105**. **Terminated mid-run by an API connection error**, last signal *"All six bite-proofs confirmed. Final verification round."* — **that claim is unverified and must not be credited.** The orchestrator inspected the tree independently rather than trusting the self-report: both in-scope files **were edited** (`stress_coordinator.py` → `671d9abc…`, 2421→2509 lines; `test_part3_stage_acceptance.py` → `9ed3e4e0…`, 1187→1545 lines), both **pure CRLF**, `compileall` **exit 0**. **Scope held**: `local_driver/actions.py` still `d1ad4f70…`, all six frozen WP10 paths byte-identical, nested repo untouched (diff 0 bytes), gitlink and HEAD unmoved. **But `iteration-002/` was EMPTY — no §7 packet was written**, so the edits are **ungated, unreviewed and undocumented** and must not be trusted. Agent resumed with its context intact to finish verification and deliver the packet. |
| P3-WP13: implementer subagent (iteration-002) — delivered | Done | 2026-08-21. Resumed agent completed. **GRK-P3-100..105 all resolved, none unresolved**; it reported nothing was left half-applied by the connection drop and re-derived every number from scratch rather than reconstructing from memory. **100**: `stalled_stage = active_step` in the RPC-phase `except`; the test that *pinned* the false label now asserts `"document_close"`, with a second case where `shutdown_rpc_server` fails and both fields read `"rpc_shutdown"`, plus an assertion that the two labels differ. **101**: renamed to `build_provenance_is_recorded_for_binaries_and_stage_sources`, **predicate untouched** — it still cannot fail a stage on a lagging binary — with `binaries_predating_head`, `binary_commit_binding_enforced: false` and `provenance_caveat` surfaced in the evidence. **102**: `STAGE_SOURCE_FILES` derived from the directory `install_part3_local_driver` copies, 7 → 10 entries, `actions.py` still explicitly named so the WP12 cross-check survives. **103**: `_record_stage_counts` extracted, idempotent, called from `run_stage`'s `finally`. **104**: params gate now inspects `node.keywords`. **105**: live tests detected structurally. Implementer's own bite-proofs, each production fix reverted individually: **1/2/1/3/1/1 failed** respectively, all exit 1. |
| P3-WP13: Main Agent §8 offline gates (iteration-002) | **PASS** | 2026-08-21. Run by the orchestrator itself; the implementer's numbers are corroboration, not evidence. Acceptance offline subset **26 passed / 2 deselected / 0 skipped** (`grep -ic skipped` = **0**); architecture **13 passed**; launcher **14 passed**; `ruff check --isolated` **All checks passed!**; `compileall`; `git -c core.autocrlf=true diff --check` empty — **all exit 0**. Totals **53 passed / 2 deselected / 0 skipped / 0 failed**. CRLF pure: **2509/2509** and **1545/1545**, zero lone LF. New in-scope diff fingerprint **`ba3045d5e1078857…`** (was `a6dc5187e588eea6…`). The single warning is the **pre-existing** `PytestUnknownMarkWarning` at `test_part3_architecture.py:17`, a frozen byte-identical file — not attributable to this diff. Wrote `iteration-002/pass-packet.yaml`. **This certifies the offline gate only; it is not a commit authorisation.** |
| P3-WP13: **GRK-P3-099 confirmed still UNMASKED** | Verified | 2026-08-21. The largest risk in this iteration was that the shutdown would be "fixed" by continuing past the `close_document` error, manufacturing a **false green** in the acceptance harness. It was **not taken**. Verified by direct source read: the `rpc.call("close_document", …)` loop has **no local `try`**, so the error still propagates to the outer handler, aborts the RPC phase, records the step, force-kills and stamps **FAILED**. Both acceptance checks remain present and unweakened (`:1601`, `:2399`); forced kill still stamps `FAILED` unconditionally (`:306`, `:311`). Scope held: `actions.py` `d1ad4f70…`, all six frozen WP10 paths byte-identical, nested diff **0 bytes**, gitlink and HEAD unmoved, nothing staged. |
| P3-WP13: three implementer disclosures routed to the reviewer | Open | 2026-08-21. Not ruled on by the orchestrator. (1) **GRK-P3-100's `required_test` is half-unsatisfied** — it asks for `test_part3_shutdown.py` at 3 passed; that ran against the *iteration-001* tree and was **not re-run** against iteration-002, so the finding is only partially discharged. (2) **A declared deviation from GRK-P3-105 as written**: `test_stage_c_is_defined_for_wp11_but_never_executable_here` was exempted from the opt-in-guard requirement, because applying the rule literally would convert a passing offline test into a **skip**; the exemption is tied to an assertion that `EXECUTABLE_STAGES == frozenset({"A","B"})`. (3) **A disclosed coverage gap**: the live-test detector does not watch `coordinator_main`, so a future `coordinator_main(["--stage","a"])` would evade it; all three current call sites are refusal paths. |
| P3-WP13: reviewer subagent re-review (iteration-002, fresh spawn) | Done | 2026-08-21. Verdict **PROBLEMS** on `ba3045d5e1078857…` (recomputed twice). **101/102/104 RESOLVED**; **103/105 resolved in substance** with residue as 107/108; **100 code fix correct but NOT closable** pending a live gate. Three new findings **GRK-P3-106..108**. Ruled the **Stage C exemption ACCEPTABLE, not a weakening** — literal application would turn a passing offline test into a **skip**, the exemption is structural rather than a name allowlist, `resolve_executable_stage` refuses before any provisioning, and it is tripwired by `EXECUTABLE_STAGES == {"A","B"}`. Did **not** launch FreeCAD; proved its headline finding with an **offline probe** instead. |
| P3-WP13: **GRK-P3-106 — the invariant was REFUTED on a second path** | **Accepted, high** | 2026-08-21. `stress_coordinator.py:291-297`: when the process exits inside the deadline, the branch sets `forced = False`, **erases `stalled_stage`** — the very field GRK-P3-100 had just been fixed to populate honestly — and returns `success: True`, leaving `failed_step` and `rpc_error` present but **unread**; `:2396` consults neither. So an ordered shutdown that **aborted its RPC phase** records `graceful_shutdown_completed_without_forced_termination` as **GREEN**. Proved by an offline probe: `success: True`, `forced: False`, `stalled_stage: None`, all three timestamps null, force-kill never invoked, `shutdown_ok` **True**. **Latent** — both live Stage A runs force-killed, so it never fired — and the live pytest test would catch it while **the CLI gate the Main Agent actually runs would not**. Orchestrator confirmed by direct source read. |
| P3-WP13: orchestrator correction to the reviewer (GRK-P3-106 detail) | Recorded | 2026-08-21. The reviewer wrote that lines 290-301 are *"byte-identical to HEAD"*. **Line 295 is not**: HEAD has `verdict="PASSED"` hard-coded, the working tree has `verdict=success_verdict`, a change that arrived with the uncommitted WP10 delta and is a **mitigation, not the defect** — a stage that already failed its checks now stamps FAILED. The defect proper (`forced = False` / `stalled_stage = None` set unconditionally) **is** byte-identical at HEAD lines 231-232, so the reviewer's substantive attribution — **the implementer did not introduce it** — is CONFIRMED. The mitigation narrows the blast radius but does **not** close it: `success: True` and `forced: False` still hold, so the **check still goes green inside a FAILED stage**. |
| P3-WP13: **orchestrator self-correction — a narrow verification reported as a general one** | Recorded | 2026-08-21. The iteration-002 pass packet stated *"GRK-P3-099 remains UNMASKED… This was the single largest risk in this iteration and it was not taken."* Every clause is **true for the path checked**, and the implementer genuinely did not take the forbidden workaround — but only **one** route was verified (that the `close_document` call has no local `try`) and it was reported as though the **invariant** had been verified. GRK-P3-106 is a second route to the same false green, **in a function the orchestrator had already read in full earlier in the same session** while diagnosing GRK-P3-099. A distinct failure mode from the three earlier orchestrator errors: not a wrong assertion, but a failure to enumerate alternative paths before declaring an invariant held. Rule adopted: *"no workaround was introduced" is a claim about a diff; "the invariant holds" is a claim about a program, and the second requires the enumeration the first does not.* |
| P3-WP13: Main Agent WP09 live gate against iteration-002 | **PASS** | 2026-08-21. `test_part3_shutdown.py` — **3 passed in 129.64s, exit 0** against the iteration-002 tree (`671d9abc…`, fingerprint `ba3045d5…`). This **discharges the live half of GRK-P3-100**, which the reviewer had correctly refused to close on an iteration-001 run. The reviewer's predicted regression **did not fire** — but the reason is stated precisely rather than waved through: its concern was **conditional** on `list_documents` raising, and it did not; the phase connected, listed, skipped the close, then took the `skip_rpc_shutdown` branch, which sets `stalled_stage` **without entering the `except`**. The green run proves the condition did not fire, **not** that it cannot. Recorded as a surviving latent observation. Aftermath verified: 0 processes, nothing on 9875, tree byte-identical, fingerprint unmoved. |
| P3-WP13: Main Agent triage (iteration-002) | Done | 2026-08-21. **GRK-P3-106/107/108 all ACCEPTED**, none softened, none disputed; one reviewer supporting detail corrected. Stage C exemption **accepted** as the rare case where deviating from a `required_fix` as written yields a **stronger** artifact than obeying it — and the implementer **declared** it rather than doing it quietly. NOTE-5 resolved: the nested `status`-vs-`diff` discrepancy is pure LF→CRLF stat-dirtiness, content byte-identical, pre-dating this session — **not** a scope violation. Wrote `iteration-002/triage.yaml`. **NOT COMMIT_READY.** |
| P3-WP13: evidence-integrity gap closed | Done | 2026-08-21. The reviewer could not compute the iteration-001 → iteration-002 delta: the acceptance file is **intent-to-add against the empty blob** `e69de29b` and iteration-001's bytes were preserved nowhere, so *"unchanged in what they assert"* had to be verified by reading text against quoted predicates rather than by **diff**. Recommendation **adopted**: a byte copy of each in-scope file is now stored beside its packet (`iteration-002/source-snapshot/`, hashes verified identical to the working tree). iteration-001's bytes are **not recoverable**, and that gap is recorded rather than papered over. |
| P3-WP13: implementer subagent (iteration-003) | Done | 2026-08-21. **GRK-P3-106/107/108 all resolved.** **106** fixed **acceptance-side only**, in `run_stage`: `shutdown_ok` now additionally requires `failed_step is None and rpc_error is None` — the reviewer's `required_fix` verbatim. Those fields are written by the abort handler and cleared by no later branch, so an aborted RPC phase can no longer record the check green whether or not the process exits inside the deadline. **Tightened, never bypassed**; a forced kill still fails it exactly as before; GRK-P3-099 stays unmasked. **107**: the `finally` counts block now persists independently of the artifact walk. **108**: detector resolves `ast.Attribute` as well as `ast.Name` and recognises `main`/`coordinator_main` with a structural refusal exemption. Bite-proofs, each fix reverted individually: **1/1/1 failed**, all exit 1 — the 106 revert prints the false green verbatim (`'passed': True` beside `'failed_step': 'document_close'`). |
| P3-WP13: **"helper untouched" verified by DIFF, not prose** | Verified | 2026-08-21. First payoff of the source-snapshot practice adopted at the end of iteration-002. Diffing the working tree against `iteration-002/source-snapshot/` gives **exactly two hunks**, `@@ -2380,9 +2380,16 @@` and `@@ -2393,7 +2400,23 @@`, **both inside `run_stage`**; the text of `graceful_shutdown_owned_session` extracted from both revisions hashes identically (`8022ce9b47097359ce89696e`). The claim the previous reviewer could only check by reading text against quoted predicates is now checked mechanically. |
| P3-WP13: Main Agent gates (iteration-003) | **PASS** | 2026-08-21. Offline: acceptance **28 passed / 2 deselected / 0 skipped** (`grep -ic skipped` = 0; iteration-002 was 26), architecture+launcher **27 passed**, `ruff` **All checks passed!**, `compileall`, `git diff --check` empty — all exit 0. Native Windows §8.2: `test_part3_shutdown.py` **3 passed in 103.37s, exit 0**. Totals **58 passed / 0 skipped / 0 failed**. CRLF pure **2532/2532** and **1834/1834**. New fingerprint **`59ec6aa2ec7da0cb…`**. **The implementer's WP09 prediction was CONFIRMED** — it predicted all three tests pass; 3 of 3 did. Aftermath verified: 0 processes, nothing on 9875, tree byte-identical. Wrote `iteration-003/pass-packet.yaml`. |
| P3-WP13: **GRK-P3-109 filed — the implementer disclosed a residual instead of widening its own scope** | Open, medium | 2026-08-21. GRK-P3-106's fix closes the **RPC-phase** route to a false green but **not the window-close route, which has the identical shape**. The window-close failure branch (`:265-277`) sets `stalled_stage` and `window_error` but **neither `failed_step` nor `rpc_error`**, so the tightened predicate evaluates **True** and a shutdown whose **window close failed** can still record the check as passed. **Verified independently in source by the orchestrator before filing.** The implementer identified it, declined to widen scope beyond the finding as triaged, proposed the one-clause fix (`and window_error is None`) and recommended filing — exactly the behaviour §4.1 asks for. Routed to the reviewer, including whether `window_skipped` deserves the same treatment. |
| P3-WP13: reviewer subagent re-review (iteration-003, fresh spawn) | Done | 2026-08-21. Verdict **PROBLEMS** on `59ec6aa2ec7da0cb…`. **106 RESOLVED** — proven by execution, not assertion: probes B1/B2/B3 (abort at each of three labels) now compute `shutdown_ok=False`, while over-fire controls A1/A2 still compute `True`; non-evadability is **structural** (`failed_step` assigned at exactly one line, `rpc_error` at one, no `del`/`.pop`/reassignment anywhere). **107 RESOLVED**; **108 resolved in substance but its fix introduced GRK-P3-111**. **GRK-P3-109 (the orchestrator's) CONFIRMED by probe C1** with two offsetting corrections. Three new findings **GRK-P3-110/111/112**, all proven by offline execution. Did **not** launch FreeCAD. |
| P3-WP13: **GRK-P3-110 — a real false PASS, in a function this diff introduced** | **Accepted, high** | 2026-08-22. GRK-P3-106 was fixed in `run_stage` **only**. `_run_preflight_only` (`:2470-2491`) gates on `bool(…get("success"))` **alone**. The reviewer drove the **real** function with the **real** `shutdown_launcher` and **real** helper: with `close_document` aborting and the process exiting inside the deadline it returns **exit 0**, prints **`PART3_RESULT: PASSED`** and stamps **`verdict: PASSED`** while `failed_step: 'document_close'` sits in the same file with all three phase timestamps null — and unlike `run_stage` it performs **no write after the `finally`**, so nothing corrects it. Orchestrator confirmed: the function does **not exist at HEAD** (grep count 0; HEAD is 568 lines), so it is introduced by this uncommitted diff. Rated high, not critical, because GRK-P3-077's `mode: preflight_only` stamp caps the blast radius — the reviewer reasoned the severity **down**. |
| P3-WP13: **GRK-P3-111 — the GRK-P3-108 fix introduced a FALSE EXEMPTION** | **Accepted, medium** | 2026-08-22. `NON_EXECUTABLE_CLI_FLAGS` treats any argv containing `--preflight-only` as a refusal path. That holds **only alongside `--stage`**: the mutual-exclusion refusal is nested **inside** `if args.stage:`. `main(["--preflight-only"])` alone falls through to `_run_preflight_only()`, which **provisions and launches a real FreeCAD GUI** — and the detector reports `live=False` for it. **Worse in kind than the gap 108 closed**: a blind gate invites a second look, a falsely-certifying one forecloses it, and the false premise is written into a comment (`:1035-1037`) a later reader will trust. Orchestrator confirmed by source read. **Second time in this work package that a fix for false confidence created new false confidence** — the first was GRK-P3-101, a check whose *name* asserted more than its predicate tested. |
| P3-WP13: **GRK-P3-112 — proves a denylist CANNOT terminate** | **Accepted, medium** | 2026-08-22. With `local_driver is None` and no skip flags, **every arm** of the `:269-288` chain is False, so control reaches the deadline branch having recorded **no marker field at all** — and `shutdown_ok` is True **even with the GRK-P3-109 clause applied**. Reachable from `run_stage`: `_handoff` is assigned at exactly one site (`:493`, called at `:2338`), so anything raising at `:2320-2337` leaves it None while `_launcher_process` is set. Cannot produce a false PASS (`context` None → `stage_ok` False), hence medium — but **the structural point is the finding**: there is no field to add, so the fix must be an **allowlist** requiring the phase transitions non-null, which is what the check's *name* already asserts. Orchestrator confirmed by source read. |
| P3-WP13: **§10 ruling — invoked in spirit** | Ruled | 2026-08-22. Iteration-003 satisfies §10 **literally** (findings reduced, bite-proofs added, new evidence), and no test is repeatedly failing, so the "three consecutive failed attempts" trigger does not fire. **But the same defect class — a shutdown that did not complete being recordable as green — has now been found at FOUR sites**: RPC phase (106), window-close (109), preflight CLI (110), and no-marker-at-all (112). Fixing them one clause at a time is the "orbiting the same defect" pattern §10 exists to prevent. **Ruling: iteration-004 may NOT add a fourth denylist clause.** It must make the class structurally impossible — **one shared predicate**, expressed as an **allowlist** over the phase transitions, applied at **every** site that gates on shutdown success. |
| P3-WP13: **orchestrator self-correction — the same failure mode, one day later** | Recorded | 2026-08-22. **GRK-P3-110 was visible in output the orchestrator itself produced.** A grep for `shutdown_ok = ` on 2026-08-21 printed **both** sites: the tightened one at `:2414` and, seventeen lines later in the same output, `:2476 shutdown_ok = bool(…get("success"))` inside `_run_preflight_only`. The orchestrator read the first, confirmed the fix, and never asked whether the second needed the same treatment. This is **SELF-2026-08-21-A repeating after being written down** — verifying one path and treating the invariant as established. Standing check adopted: *when a predicate is tightened to fix a finding, every other site computing that predicate is part of the same finding until proven otherwise — grep for the predicate, not for the line being fixed.* |
| P3-WP13: Main Agent triage (iteration-003) | Done | 2026-08-22. **GRK-P3-109/110/111/112 all ACCEPTED**, none softened, none disputed; all three new ones re-verified in source before acceptance. `window_skipped` ruled **exempt by unreachability, not by semantics** — adding the clauses would be dead code, and the reasoning is recorded that way so a future change to `shutdown_launcher` **re-opens** the question instead of inheriting an exemption whose premise has silently expired, which is exactly how GRK-P3-111 came to exist. Disclosure 1 (`success: True` from an aborted shutdown — a false **return contract** every caller inherits, and GRK-P3-110 is a caller that inherited it) routed to the **WP09/nested** package, **not closed**. Disclosure 2 promoted into 112's primary remedy. Wrote `iteration-003/triage.yaml`. |
| P3-WP13: implementer subagent (iteration-004) | Done | 2026-08-22. **GRK-P3-109/110/111/112 all resolved**, none outstanding. The **§10 structural constraint was satisfied, not worked around**: no fourth denylist clause was added, and `window_error` is **not referenced anywhere** in the new predicate. One allowlist predicate, `ordered_shutdown_completed()` at `:2227`, requires **every** phase transition in `SHUTDOWN_TIMESTAMP_KEYS` to be non-null in the block that was actually **persisted**, and retains the GRK-P3-106 conjuncts — so it only ever tightens. Applied at **2 of 2** production gates (`run_stage:2462`, `_run_preflight_only:2534`), with `shutdown_ok = False` as the fail-safe initialiser. |
| P3-WP13: **the structural fix is ENFORCED, not documented** | Verified | 2026-08-22. `COMPLETED_SHUTDOWN_TRANSITIONS = SHUTDOWN_TIMESTAMP_KEYS` (`:2224`), **imported at `:34` from the frozen `evidence.py`** (still `6d4745d8…`) rather than re-listed — so a transition added to the record later is required **automatically**, without editing the predicate. All 7 keys: `requested_utc`, `rpc_admission_closed_utc`, `worker_shutdown_utc`, `documents_closed_utc`, `listener_shutdown_utc`, `window_closed_utc`, `process_exit_utc`. And `test_every_shutdown_gate_asks_the_one_shared_predicate` (`:2238`) parses the module and fails if any `shutdown_ok = <call>` bypasses the predicate, if an initialiser is anything but `False`, if the caller set is not exactly `{run_stage, _run_preflight_only}`, or if the literal `shutdown_launcher(...).get("success")` shape reappears. Orchestrator enumerated every other reader repo-wide: only `conftest.py:181`, a frozen fixture-teardown escalation that records no acceptance result — **excluded with reason, not skipped**. |
| P3-WP13: Main Agent gates (iteration-004) | **PASS** | 2026-08-22. Offline: acceptance **32 passed / 2 deselected / 0 skipped** (`grep -ic skipped` = 0; iteration-003 was 28), architecture+launcher **27 passed**, `ruff` **All checks passed!**, `compileall`, `git diff --check` empty — all exit 0. Native Windows §8.2: `test_part3_shutdown.py` **3 passed in 107.12s, exit 0**. Totals **62 passed / 0 skipped / 0 failed**. CRLF pure **2598/2598** and **2323/2323**. New fingerprint **`a702b455b04d95c8…`** (lineage `a6dc5187` → `ba3045d5` → `59ec6aa2` → `a702b455`). Helper verified **byte-identical** again by snapshot diff (`8022ce9b…`, 5 hunks: import, predicate, three in the two gates) — its false return contract was left alone as instructed, being routed to the nested package. Wrote `iteration-004/pass-packet.yaml`. |
| P3-WP13: **bite-proof B — the §10 ruling vindicated experimentally** | Recorded | 2026-08-22. The most valuable artifact in this work package. Reverting to the denylist **plus the `window_error` clause exactly as GRK-P3-109 was filed** gives **2 failed, 30 passed, exit 1**: the 109 test **passes** while the `local_driver=None` test **still fails**. So the fix the orchestrator originally proposed would have left a hole — the denylist's non-termination is now shown by **experiment**, not argued. Proof **C** reproduces GRK-P3-110 end to end (`AssertionError: PART3_RESULT: PASSED` / `assert 0 != 0`). Proof **E** fired the two exact-`failed_checks` tripwires when the stub was reverted: **the stub was made honest and the tests were not relaxed.** |
| P3-WP13: over-fire risk assessed | Verified structurally, NOT empirically | 2026-08-22. Requiring all 7 transitions could in principle fail a genuinely clean shutdown. Orchestrator verified by source read that it does not: all seven are stamped on the clean path — `:218`, `:253`, `:259-261`, `:273`, `:293` — and on a real stage run `local_driver` is not None (connected at `:2338`), so the `:273` arm is taken. **Honest limitation: this is structural, not empirical, and cannot be confirmed by a live green stage while GRK-P3-099 is unfixed**, because Stage A cannot reach a clean shutdown at all. The implementer disclosed exactly this rather than claiming otherwise. Handed to the reviewer as the one open assurance gap, together with the question of whether the 7-key superset or the narrower 3-key set is correct. |
| P3-WP13: reviewer subagent re-review (iteration-004) | **Attempt 1 FAILED — transient, no result** | 2026-08-22. Spawned on `a702b455…`; **terminated by an API 529 Overloaded**, a server-side error, and returned **nothing at all** — not even a partial summary. Orchestrator verified the aftermath: `iteration-004/re-review.yaml` **does not exist**, so nothing of the review survived; tree untouched (`9ffb11b8…` / `f86430e0…`, fingerprint still `a702b455…`), HEAD and gitlink unmoved, nested diff 0 bytes, nothing staged, 0 processes, nothing on 9875. **Clean failure — nothing to salvage, nothing to undo, and nothing that may be read as a clean review.** Agent resumed with its context intact, and told to write the artifact **early and incrementally** so a second interruption leaves a partial record with honest gaps rather than nothing; any section it does not reach is **NOT_REVIEWED**, never clean. |
| P3-WP13: reviewer subagent re-review (iteration-004), attempt 2 | **HALTED — account spend limit** | 2026-08-22. Resumed attempt stopped by the **monthly spend limit** — **not transient**, unlike the 529. **Further subagent spawns will fail until it is raised**, which blocks the process outright: §1.0 requires review and implementation to be *separate spawns* and §1.1 forbids the orchestrator absorbing either role. **The incremental-write instruction paid off**: a partial `re-review.yaml` (2796 bytes) survives where attempt 1 left nothing. It records **`verdict: NOT_REVIEWED_YET`** and `artifact_state: IN_PROGRESS`, and it **independently re-derived** the diff fingerprint `a702b455…`, the full lineage, both file hashes, the source fingerprint, `staged: nothing`, and the nested 0-byte content diff — explicitly *checked rather than inherited*. **It contains no findings and no rulings; the review proper never began.** |
| P3-WP13: reviewer subagent re-review (iteration-004), attempt 3 | **COMPLETED - PROBLEMS** | 2026-08-22. Fresh spawn; **the spend-limit block was gone**. GRK-P3-109/110/111/112 all RESOLVED, verified by *offline execution* and by **per-function AST hashing** proving the three shutdown functions byte-identical i003 to i004 - no GRK-P3-099 workaround smuggled in. Ruled the **7-key superset CORRECT by executing the clean path**, not by accepting the orchestrator's structural argument, and proved the WP09 `skip_*` shape unreachable. Ruled the false `success: True` return correctly routed to P3-WP14 after enumerating all three consumers. Filed GRK-P3-113 and GRK-P3-114. Orchestrator verified read-only: all ten hashes unmoved. |
| P3-WP13: iteration-005 (implementer + Main Agent gates) | **Delivered, gates GREEN** | 2026-08-22. Test-only; `stress_coordinator.py` untouched. Fingerprint `a702b455` to `ef6783fa`. Main Agent gates: **33 passed / 2 deselected / 0 skipped**, **27 passed**, ruff clean, compileall, `diff --check` empty, native Windows **3 passed in 133.39s** = **63 passed / 0 failed**. |
| P3-WP13: reviewer subagent re-review (iteration-005) | **COMPLETED - PROBLEMS** | 2026-08-22. Fresh independent spawn. GRK-P3-113/114 both RESOLVED - ran its **own** mutation harness (**7/7 keys bite, TOTAL_NON_BITING=0**) and proved the zero-open-documents shape **load-bearing**, not decorative. All six implementer claims CONFIRMED by execution. Proved the guard family **measurably converging**: i004 guard missed 7 of 8 probe shapes, composite missed 6 of 8, every new residual a subset of an old one. Filed GRK-P3-115 (under-fire) and GRK-P3-116 (**over-fire on correct code**). It also caught a **mistranscribed hash in the orchestrator's own brief**. |
| P3-WP13: iteration-006 (implementer + Main Agent gates) | **Delivered, gates GREEN, UNREVIEWED** | 2026-08-22. Test-only again; `stress_coordinator.py` **byte-identical since iteration-004**. Falsification set grown **5 to 10 entries, none removed**. GRK-P3-115 item (3) **not skipped** - the unqualified docstring claim replaced by an explicit *WHAT IT ENFORCES / WHAT IT DOES NOT* split, the item that **terminates** the 111-114-115 family. Fingerprint `ef6783fa` to `bc67f5d4`. Main Agent gates, all six exit 0: **33 passed / 2 deselected / 0 skipped**, **27 passed**, ruff `All checks passed!`, compileall, `diff --check` empty, native Windows **3 passed in 113.87s** = **63 passed / 0 failed / 0 skipped**. **NOT REVIEWED - a fresh reviewer is the next action.** |
| P3-WP13: reviewer subagent re-review (iteration-006) | **COMPLETED - PROBLEMS** | 2026-08-22 session 3. Fresh independent read-only spawn. GRK-P3-116 **RESOLVED**; GRK-P3-115 code items (1)/(2) resolved and both disclosed residual families accurately distinguished. All six implementer claims otherwise independently confirmed: both controls accept, 10/10 evasions reject, exact helper caller, all required store-target forms, `ast.arg` parameter exemption, 7/7 key bite, no removed assertion, and coordinator byte identity/no 099 workaround. Filed **GRK-P3-117**: WHAT IT ENFORCES still says every non-predicate `shutdown_ok` store binding fails, although the required fail-closed `shutdown_ok = False` control is intentionally accepted. Reviewer packet and read-only aftermath independently reproduced by the Main Agent. |
| P3-WP13: implementer subagent (iteration-007 / GRK-P3-117) | **DELIVERED** | 2026-08-22 session 3. Test-only: WHAT IT ENFORCES now explicitly accepts the predicate **or the single fail-closed literal-False initializer**; added distinct `_GATE_PLAIN_TRUE_SOURCE` rejection proof. Both controls and all ten existing evasions preserved; coordinator byte-identical. Exact i007 fingerprint `ba8f4367...`, test file `d110384f...`, 2993 pure-CRLF lines. Implementer disclosed a nested-log miscount, failed patch context, temporary 16-lone-LF state caught/normalized before tests, and an initial Ruff run without `--isolated` that was rerun correctly. |
| P3-WP13: Main Agent gates (iteration-007) | **BEHAVIOR GREEN WITH DISCLOSED PRELAUNCH RED** | 2026-08-22 session 3. Offline all green: **33 passed / 2 deselected**, **27 passed / 2 known warnings**, Ruff, compileall, 0-byte diff-check. Native g6a exact outer `pixi run` command: **3 failed in 23.07s before FreeCAD**, because the unchanged launcher nested another pixi activation and Windows cmd.exe rejected the expanded activation input (`exit 255`, `The input line is too long`). RED preserved in `failure-packet.yaml` plus three launcher logs. Corrected g6b used the same repository pixi Python 3.11.14 directly so the launcher performed one pixi activation: **3 passed in 107.72s**. Packet reports all-command totals **63 passed / 3 prelaunch failed / 2 deselected / 0 skipped** and behavior-exercising set **63 passed / 0 failed**; reviewer must rule whether g6b satisfies §8.2. |
| P3-WP13: reviewer subagent re-review (iteration-007) | **COMPLETED - NO_ISSUES** | Fresh independent read-only spawn reviewed the complete `ba8f4367...` diff and both exact packets. GRK-P3-115/116/117 **RESOLVED**; no assertion weakened; guard/coordinator unchanged; plain True bites; controls accept; ten prior evasions byte-identical and reject. g6a ruled deterministic pre-FreeCAD environment RED, never green/flaky; g6b ruled sufficient §8.2 native behavior evidence because repository pixi Python 3.11.14 ran all three tests through the unchanged launcher with one activation. Exact-command reproducibility remains nonblocking N11. Main Agent independently reproduced final hashes/staging/gitlink/process/port state. |
| P3-WP13: final gate / commit / push | **DONE** | 2026-08-22 session 3. §11 final gate PASS. Staged exactly plan tracker + the two declared WP13 paths; cached diff-check clean; no results/logs/nested gitlink/user/frozen paths. Parent commit **`975a4cba4d7beb44e718e0ddcfcec4347c90d8cf`** (`fix(gui): harden Part 3 acceptance gates`) pushed without force. Local HEAD, upstream and `git ls-remote origin refs/heads/fix/change-aware-save-mcp-autonomy` all equal `975a4cba4d...`; 0 ahead / 0 behind. Evidence: `iteration-007/final-gate.yaml`. |
| P3-WP14: nested baseline / implementation | **DONE** | 2026-08-22 session 3. Baseline bound parent `975a4cba4d...`, nested/upstream/gitlink `71a89c703e...`, empty nested content/cached diff, classified two autocrlf-stat-dirty tests plus 11 excluded logs, port 9875 free, Docker image `b34e0e1e...`, and all editable-path hashes/EOLs. Fresh implementer captured genuine pre-fix RED: undo+redo thread hops returned `LEASE_PROTOCOL_REQUIRED`; successful close and genuine absence both returned `NAMEERROR`. It resolved GRK-P3-095/099 in seven bounded nested paths and corrected exactly the five known FreeCAD stubs. |
| P3-WP14: Main Agent gates / fresh review | **DONE - NO_ISSUES** | Exact candidate `3d211dd18be7a6c...` / 16034 bytes. Windows and isolated Docker concern suites **103 passed each**; isolated Ruff, compileall and diff-check PASS. Branch-built Windows FreeCADCmd proved native absent `getDocument` raises `NameError`, successful close removes the document, repeated close returns `DOCUMENT_NOT_FOUND`, and cross-thread undo/redo pass **2/2**. Full unit honestly retained the same two documented non-attributable REDs on Windows (**2533 passed / 2 failed**) and Docker (**2534 passed / 2 failed**). Fresh reviewer independently reran and ruled both findings resolved, verdict **NO_ISSUES**, artifact `c7eb867c...`. |
| P3-WP14: nested final gate / commit / push | **DONE** | §12.1 nested gate PASS. Staged exactly seven reviewed paths; cached diff `2e50c970...`, clean `--check`, no results/logs/user/tests-lib/research paths. Nested commit **`eec2f1e68a46a21f6e6bf9fa918005eba5d4e1fb`** (`fix(mcp): preserve request identity and document close`) contains 185 insertions / 37 deletions, was pushed without force, and local HEAD/upstream/remote all match, 0/0. The parent recorded gitlink is not yet committed; the explicitly paired parent gate is next. |
| P3-WP14: paired parent final gate / commit / push | **DONE** | §12.2 parent gate PASS. Staged only tracker + gitlink; cached diff `bc29b6dc...`, clean `--check`, index gitlink exactly remote nested `eec2f1e68a46...`, no excluded path. Parent commit **`ec4a66eab4da87b54672613100365d2b8e2058c5`** (`chore(mcp): advance Part 3 addon gitlink`) pushed without force; local HEAD/upstream/remote match, 0/0. P3-WP14 is complete. |
| P3-WP15: Linux executable discovery implementation / gates | **DONE** | GRK-P3-094 baseline proved Linux selected the Windows PE while `build/debug/bin/FreeCAD` was a real ELF. Fresh implementer captured **5 failing cases + 1 fail-closed control** before fixing only `default_freecad_exe` and six tests. Main Agent gates on exact `97494b46...`: focused **6 passed**, offline **39 passed / 2 live deselected / 0 skipped**, architecture+launcher **27 passed**, Ruff/compileall/diff-check PASS; network-none/read-only Docker **6 passed** and selected `/repo/build/debug/bin/FreeCAD`, never release `.exe`. No FreeCAD or stage launch. |
| P3-WP15: fresh review | **DONE - NO_ISSUES** | Reviewer independently reproduced the baseline bite and all Windows/Docker gates, exact diff/packet/EOL bindings, parent/nested/gitlink/index/port/process state, and ruled the read-only Docker pytest `PYTHONPATH` provisioning honest. Verdict **NO_ISSUES** on `97494b461359089d...`; artifact `5275e663...`. Final gate/commit/push is next. |
| P3-WP15: final gate / commit / push | **DONE** | §11 gate PASS. Staged exactly tracker + two reviewed resolver paths; cached diff `246f6625...`, clean `--check`, no excluded/gitlink path. Parent commit **`308252057e42445546cdd996a23f35b0ca3471af`** (`fix(gui): make Part 3 FreeCAD discovery platform-aware`) pushed without force; local HEAD/upstream/remote match, 0/0; gitlink remains nested `eec2f1e68a46...`. P3-WP15 complete. |
| P3-WP16: Windows release binary rebuild | **DONE** | User-mandated precondition. `pixi run build-release` completed exit 0 with Ninja/MSVC Release at parent `308252057e...`, nested/gitlink `eec2f1e68a...`; rebuilt `FreeCAD.exe`, `FreeCADCmd.exe`, `FreeCADApp.dll`, and `FreeCADGui.dll`. `FreeCADCmd --version` PASS (`26.3.0`, revision 48066). Exact hashes/toolchain: `2026-08-22/P3-WP16-build/windows-release-rebuild.yaml`. No Stage A/B/C run. |
| P3-WP16: Linux debug binary rebuild | **DONE** | Fresh network-none Docker Debug/Ninja build completed **8390/8390** at exact parent `308252057e...` plus all recursive submodule commits. The archive-only full build honestly reported `Revision: Unknown` and was not certified; mounting only parent Git metadata read-only regenerated `version_file` and relinked **84/84** dependents. Final `FreeCADCmd --version` PASS (`26.3.0`, `48066 (Git)`), embedded hash `308252057e...`; `FreeCAD` SHA-256 `80b71d09...`, `FreeCADCmd` `813a2548...`, `libFreeCADApp.so` `ac71440f...`. Exact attempts/toolchain/hashes: `2026-08-22/P3-WP16-build/linux-debug-rebuild.yaml`. Both binary preconditions are satisfied; no Stage A/B/C run. |
| P3-WP17: remaining pre-stress gates | **PASS** | Native Windows, rebuilt Release binaries, strictly sequential. WP07 integration **7 passed** in 303.55s; WP09 shutdown **3 passed** in 104.97s; WP12 focused driver **9 passed** in 96.31s: **19 passed / 0 failed / 0 skipped**. An initial outer-pixi WP07 attempt produced **7 NOT_RUN skips** because the tracked launcher nested a second pixi activation and Windows rejected the expanded environment; preserved separately, then corrected using repository Python so the unchanged launcher performed one activation. Zero FreeCAD processes and no 9875 listener after each suite. Evidence: `2026-08-22/P3-WP17-prestress/prestress-gates.yaml`. |
| P3-WP18: first counting native Windows Stage A | **FAILED INCOMPLETE** | Correct live opt-in; exact parent `308252057e...`, nested `eec2f1e68a...`, rebuilt binary hashes embedded. It progressed beyond the former 3/1 diagnostic failure point to **8/10 cycles and 4/5 saves**, then aborted at 319.53s with JSON-RPC `-32000` **`RPC session has expired`**. `PART3_RESULT: FAILED`, pytest **1 failed**, 3 evidence checks failed; shutdown itself was honest and graceful (`forced:false`, all transitions stamped). JUnit/evidence/launcher hashes at `2026-08-22/P3-WP18-stage-a-windows/stage-a-result.yaml`. Stage B not started. |
| P3-WP19: Stage A RPC session expiry | **NO_ISSUES / CURRENT STAGE A PASS / §11 CANDIDATE** | Exact candidate `f8c22a2e...`; fresh reviewer verdict **NO_ISSUES**, artifact `3cc489fd...`; GRK-P3-118/119 **RESOLVED**. Main Agent current-candidate native Windows Stage A then passed from zero at default TTL: **10/10 cycles, 5/5 saves, 13/13 evidence checks, 1 passed in 380.59s**, graceful `forced:false`, exact client hash embedded, no unexplained artifacts, 0 processes/listeners afterward. Evidence `64e4f079...`, JUnit `e96d523c...`, launcher `749f0cb3...`. P3-WP19 is a §11 commit candidate; Stage B waits for the reviewed fix to be committed/pushed. Stage C forbidden. |
| P3-WP11 — Stage C, separate fresh session | Not started | Out of this run. Fresh session from successful Stage B SHAs. Part 3 stays `NO-GO` until it passes. |

### Next step

**P3-WP19 iteration-002 is `NO_ISSUES` and current-candidate native Windows Stage A passed completely at 10/5. It is now a §11 commit candidate; Stage B waits for the reviewed fix to be committed and pushed.**

Parent HEAD/upstream are `308252057e42...`, 0/0 and record nested `eec2f1e68a46...`; nested HEAD/upstream are also 0/0. The classified dirty tree remains allowed by the user's §13 decision. Windows Release and Linux Debug are current; WP07/WP09/WP12 are green. Counting Windows Stage A now has one incomplete 8/4 failure and one complete 10/5 pass. Stage B and Stage C remain not started.

1. Run §11 item by item for P3-WP19, explicitly stage only the tracker plus the two reviewed parent files, commit once, push without force, and verify remote parent/nested/gitlink identities. Never stage `results/` or unrelated dirty paths.
2. Then run native Windows Stage B from zero at the committed P3-WP19 SHA; if green, run rebuilt-Linux Docker Stage A/B and continue the remaining acceptance/final-report sequence. Stage C stays outside this session.

**Section 13 policy - DECIDED by the user 2026-08-21: "Rebuild binaries, allow dirty tree."** Before any Stage A/B run meant to **count**, rebuild so no committed C++ is missing; uncommitted Python harness changes stay permitted and a dirty tree alone does not disqualify a run. Neither 2026-08-21 Stage A meets that bar; both are **diagnostic only**.

**Do not start P3-WP11 / Stage C.** Separate fresh session, from the exact successful Stage B SHAs.

**Ceiling - still NO-GO.** Both binary rebuilds and all three pre-stress gates are complete. Current-candidate counting Windows Stage A passes completely at **10/10 cycles and 5/5 saves**; the prior iteration-001 candidate also completed 10/5, after the earlier counting 8/4 expiry failure and two diagnostic-only 3/1 failures. GRK-P3-118/119 are resolved, but Stage B has not begun or had a valid execution. Stage C remains a separate fresh session and has not started.

### Change log

- **2026-08-22 (session 3, P3-WP19 current-candidate Stage A PASS)**: After fresh `NO_ISSUES`, Main Agent restarted native Windows Stage A from zero on exact client `9fa853e3...`, no TTL override. Pytest **1 passed in 380.59s**; evidence records **10/10 cycles, 5/5 saves, 13/13 checks**, no coverage gap/unexplained artifact, exact parent/nested/gitlink and client hash, graceful shutdown with all transitions and `forced:false`. Evidence `64e4f079...`, JUnit `e96d523c...`, launcher `749f0cb3...`; 0 processes and no 9875 listener afterward. P3-WP19 is now a §11 candidate. Stage B/C not started.
- **2026-08-22 (session 3, P3-WP19 iteration-002 NO_ISSUES)**: New fresh independent reviewer verified every current byte and exact packet binding, reran Windows and pinned network-none/read-only Docker **50** each, acceptance **39/2**, architecture **27**, static gates, and the four **2/2/4/4** load-bearing mutants. Verdict **NO_ISSUES**, artifact `3cc489fd...`; GRK-P3-118/119 both **RESOLVED**. Reviewer correctly ruled the prior 10/5 Stage A honest but non-binding because iteration-002 changed source, and authorized Main Agent current-candidate Stage A only. Main Agent independently rechecked artifact/source/frozen hashes, empty indexes, identities, 0 processes, and no port listener. Stage A restart next; Stage B/C not started.
- **2026-08-22 (session 3, P3-WP19 iteration-002 delivered / Main Agent gates green)**: Separate implementer fixed accepted GRK-P3-119 only in the two bounded parent files. Pre-fix matrix bit **12/12** malformed semantic-bearing cases across read/mutation. Main Agent bound exact candidate `f8c22a2e...`, independently reproduced Windows **50**, pinned network-none/read-only Docker **50**, acceptance **39/2**, architecture **27**, Ruff/compile/diff/CRLF, and all four deliberate mutants (**2/2/4/4 failures**). One guessed direct Ruff path failed before lint and changed nothing; authoritative `pixi run ruff` passed. No live stage, process, listener, TTL override, staging, commit, or push. Fresh review next; the earlier honest 10/5 Stage A predates this source fingerprint and cannot certify it.
- **2026-08-22 (session 3, P3-WP19 iteration-001 re-review PROBLEMS)**: Fresh read-only reviewer independently reproduced exact candidate/evidence bindings and Windows/Docker dedicated **38**, acceptance **39/2**, architecture **27**, plus the honest live Stage A **10/5/13** packet. Verdict **PROBLEMS**, artifact `5214c44a...`: GRK-P3-118 remains unresolved because new **GRK-P3-119 high** proves `_is_recoverable_auth_refusal` accepts malformed errors by `data.error_code` alone. Direct bites for missing code, wrong `-32001`, and string `"-32000"` each renewed once and dispatched the identical mutation twice. Main Agent independently obtained true for all three malformed predicates and **ACCEPTED** 119 unchanged. Bounded iteration-002 opened; Stage B/C not started.
- **2026-08-22 (session 3, P3-WP19 implemented / Main Agent gates green)**: Separate implementer delivered exact parent-only candidate `8c7b5567...`: lifecycle-aware session renewal in `rpc_session_client.py` plus one dedicated test module, with no nested edit or TTL override. Main Agent independently reproduced Windows and network-none/read-only Docker dedicated **38 passed** each, offline acceptance **39 passed / 2 live deselected**, architecture+launcher **27 passed**, and Ruff/compileall/diff-check/EOL PASS. Native Windows Stage A restarted from zero and crossed the former expiry boundary at the default 300-second lifetime: **10/10 cycles, 5/5 saves, 13/13 evidence checks, 1 passed in 373.10s**, graceful `forced:false`, no leftover process/listener. Exact live evidence `6b8e5d7b...`, JUnit `afca8f73...`, launcher log `c5e51708...`. Candidate remains **unresolved pending a new independent re-review**; Stage B/C not started.
- **2026-08-22 (session 3, P3-WP19 review/triage)**: Fresh independent reviewer verdict **PROBLEMS**, exact artifact `9f015669...`; all parent/nested/artifact hashes and the honest 8/4 failure were independently reproduced. Filed **GRK-P3-118 high**: the parent bespoke session client discards expiry and never renews, while the nested server intentionally expires at 300 seconds and its production client already has renewal. Reviewer rejected `FREECAD_MCP_SESSION_TTL_SECONDS=3600` as the sole/counting fix because it weakens bearer lifetime twelvefold, assumes duration, and masks the lifecycle defect. Main Agent independently verified all ten frozen/tracked hashes, indexes, identities, processes and port, then **ACCEPTED** the finding unchanged. Bounded parent-only implementation next; Stage B/C not started.
- **2026-08-22 (session 3, first counting Stage A FAILED / P3-WP19 opened)**: Native Windows Stage A used the rebuilt binaries and explicit live opt-in after all pre-stress gates passed. It reached **8/10 cycles and 4/5 saves**, then the authenticated RPC returned `-32000 RPC session has expired` at 319.53s. Coordinator and pytest stayed honest: `PART3_RESULT: FAILED`, exit 1, three failed checks, no rounded-up counts. Shutdown completed gracefully with `forced:false`; aftermath is 0 processes/no 9875 listener. Exact JUnit/evidence/launcher artifacts retained under `P3-WP18-stage-a-windows`. New P3-WP19 opened; fresh read-only reviewer spawned. Stage B/C not started.
- **2026-08-22 (session 3, pre-stress gates complete)**: WP07 **7 passed**, WP09 **3 passed**, WP12 **9 passed**: authoritative total **19 passed / 0 failed / 0 skipped** on current rebuilt Windows Release binaries. A first outer-pixi WP07 command yielded 7 NOT_RUN skips from the already-documented nested-activation input-line-too-long failure; its JUnit was preserved, and repository Python reran the unchanged tracked launcher with one activation. No processes/listeners remained. No Stage B/C run.
- **2026-08-22 (session 3, Linux Debug rebuild complete)**: Fresh network-none Docker Debug/Ninja build completed **8390/8390** at exact parent `308252057e...` plus exact recursive submodules. Attempt 1 failed configure before compilation because a parent `git archive` omits submodules. Attempt 2 materialized them explicitly and built cleanly but reported `Revision: Unknown`; it was retained as non-certifying evidence. A read-only parent-Git metadata bind then regenerated the version and incrementally relinked **84/84** targets. Final version is `26.3.0 Revision: 48066 (Git)` with embedded hash `308252057e...`; hashes include `FreeCAD` `80b71d09...`, `FreeCADCmd` `813a2548...`, `libFreeCADApp.so` `ac71440f...`, and `libFreeCADGui.so` `8ea8ca74...`. Both rebuild preconditions are satisfied. No Stage A/B/C run. Next: WP07/WP09/WP12 pre-stress gates.
- **2026-08-22 (session 3, Windows Release rebuild complete)**: User-mandated binary precondition completed on Windows. `pixi run build-release` exit 0 at parent `308252057e...`, nested/gitlink `eec2f1e68a...`, Ninja 1.13.2, CMake 4.2.3, MSVC 14.44. `FreeCADCmd --version` reports 26.3.0 revision 48066. New hashes include `FreeCAD.exe` `5406651d...`, `FreeCADCmd.exe` `1f120148...`, `FreeCADApp.dll` `fd12bfe7...`, and `FreeCADGui.dll` `7196e7d5...`; full evidence at `2026-08-22/P3-WP16-build/windows-release-rebuild.yaml`. No Stage A/B/C run. Next: Linux Debug rebuild.
- **2026-08-22 (session 3, P3-WP15 committed/pushed)**: Final gate staged only tracker + the two reviewed resolver files; cached diff `246f6625...`, clean, no excluded/gitlink path. Parent **`308252057e42445546cdd996a23f35b0ca3471af`** pushed and verified 0/0; nested/gitlink remains `eec2f1e68a46...`. GRK-P3-094 complete. No FreeCAD or Stage A/B/C run. Next is the user-mandated binary rebuild.
- **2026-08-22 (session 3, P3-WP15 reviewed)**: GRK-P3-094 exact candidate `97494b46...` resolves FreeCAD by platform, existing launcher override vocabulary and deterministic release/debug order. Required baseline bite reproduced **5 fail / 1 control pass**. Main Agent and fresh reviewer independently reproduced focused **6**, offline **39 / 2 live deselected / 0 skipped**, architecture+launcher **27**, static gates, and network-none/read-only Docker **6** plus real debug ELF selection. Reviewer verdict **NO_ISSUES**, artifact `5275e663...`. No process/stage launch; final commit gate next.
- **2026-08-22 (session 3, P3-WP14 parent committed/pushed)**: Paired parent §12.2 gate staged only the plan tracker and `tools/mcp/freecad-mcp`; cached diff `bc29b6dc...` clean, gitlink exactly remote nested `eec2f1e68a46...`, no excluded paths. Parent commit **`ec4a66eab4da87b54672613100365d2b8e2058c5`** (`chore(mcp): advance Part 3 addon gitlink`) pushed without force; local/upstream/remote all match, 0/0. P3-WP14 complete. Stage A/B/C not run. Next is P3-WP15 / GRK-P3-094.
- **2026-08-22 (session 3, P3-WP14 nested committed/pushed)**: GRK-P3-095 and GRK-P3-099 resolved in the nested addon after genuine pre-fix bite proofs. Main Agent exact candidate gates: Windows **103 passed**, isolated Docker **103 passed**, Ruff/compileall/diff-check PASS, and native branch FreeCADCmd PASS for real `NameError` absence, successful removal, repeated `DOCUMENT_NOT_FOUND`, and **2/2** cross-thread history cases. Full unit retained and disclosed the same two documented baseline failures on Windows/Docker; fresh reviewer independently reproduced and ruled them non-attributable, verdict **NO_ISSUES** on `3d211dd1...`. Nested §12.1 commit **`eec2f1e68a46a21f6e6bf9fa918005eba5d4e1fb`** contains exactly seven reviewed paths and no prohibited artifacts; push verified local/upstream/remote 0/0. Paired parent gitlink commit pending; no Stage A/B/C run.
- **2026-08-22 (session 3, P3-WP13 committed/pushed)**: §11 final gate **PASS** on reviewed raw diff `ba8f4367...`. Explicit staging contained only `doc/part3-orchestrated-review-fix-test-plan.md`, `tests/gui/part3/stress_coordinator.py` and `tests/gui/part3/test_part3_stage_acceptance.py`; cached diff `628952c9...`, clean `--check`, 0 other paths, no unstaged bytes on those paths. Disclosed one discarded PowerShell helper attempt whose `$args` parameter collided with the automatic variable; it wrote nothing and authoritative hashes were recomputed. Commit **`975a4cba4d7beb44e718e0ddcfcec4347c90d8cf`** (`fix(gui): harden Part 3 acceptance gates`) created with 5327 insertions / 111 deletions across exactly those three paths. Pushed without force; local HEAD, upstream and remote `ls-remote` all match `975a4cba4d...`, 0/0. P3-WP13 **DONE**. Next is P3-WP14 nested, the first nested commit and first gitlink bump since `71a89c70`; Stage A/B still not run this session.
- **2026-08-22 (session 3, iteration-007 NO_ISSUES)**: Fresh independent reviewer completed the full current diff and both validation packets: **NO_ISSUES** on exact `ba8f43673d38908c91e1ff0b9c27d70fd9e095c8b7e15b2c045d99c91a49e505`, pass packet `20557f8a...`, failure packet `8e97ddbe...`. GRK-P3-115/116/117 all **RESOLVED**. Plain True is distinct and bites; both controls accept; ten pre-existing evasions byte-identical/reject; guard and coordinator unchanged; no 099 workaround. Reviewer kept g6a honest as deterministic pre-FreeCAD RED and nonblocking N11, and ruled g6b sufficient §8.2 behavior coverage through repository pixi Python 3.11.14 plus unchanged single-activation launcher. Main Agent independently recaptured final §11 state: parent/upstream `bfa7bc1ccf` 0/0, nested/gitlink/upstream `71a89c703e` consistent with empty content/cached diffs, parent cached diff empty, raw candidate and both packet hashes exact, all ten tracked hashes exact, 0 relevant processes, 0 port-9875 entries, bind success, plan pure CRLF. P3-WP13 is now a **SECTION 11 CANDIDATE**, not yet staged or committed.
- **2026-08-22 (session 3, iteration-007 delivered/gated/reviewing)**: Separate implementer resolved GRK-P3-117 test-only: the bounded paragraph now explicitly accepts the predicate or the one fail-closed literal-False initializer, and a distinct plain-True source is asserted rejected; both controls and ten existing evasions remain. Main Agent independently reproduced the 9-add/2-remove three-hunk delta, exact raw diff **`ba8f43673d38908c91e1ff0b9c27d70fd9e095c8b7e15b2c045d99c91a49e505`**, test hash `d110384f...`, pure 2993 CRLF, snapshot equality, coordinator/frozen hashes, empty cached/nested diffs and safe port/process state. Offline gates all green: **33 passed / 2 deselected**, **27 passed / 2 known warnings**, Ruff, compileall, 0-byte diff-check. Native first command RED: outer `pixi run` plus the tracked launcher's inner `pixi run` caused deterministic Windows activation failure before FreeCAD in all three tests (**3 failed in 23.07s**, exit 255, `The input line is too long`). Preserved verbatim in `failure-packet.yaml` and copied three launcher logs. Separate environment-corrected command used the repository pixi Python 3.11.14 directly so the unchanged launcher performed exactly one pixi activation; native behavior **3 passed in 107.72s**. Validation packet reports both results, never overwrites the RED, and does not decide its own sufficiency. Fresh iteration-007 reviewer spawned to rule GRK-P3-117 and the §8.2 evidence. No Stage A/B, commit, stage or push.
- **2026-08-22 (session 3, iteration-006 review / iteration-007 opened)**: Fresh iteration-006 reviewer verdict **PROBLEMS** on exact diff `bc67f5d4...` and pass packet SHA-256 `07400af5...`. GRK-P3-116 **RESOLVED**. GRK-P3-115 code items (1)/(2) resolved; the wrong-function docstring near-miss did **not** land a false class-body residual; import-alias and bare global/nonlocal limitations are acceptably disclosed. All six implementer claims otherwise independently confirmed, including **10/10** evasion rejection, both accepted controls, exact `[shutdown_launcher]` helper caller, store-target forms with the `ast.arg` parameter clean, **7/7** transition-key bite, and coordinator byte identity/no GRK-P3-099 workaround. New **GRK-P3-117**: WHAT IT ENFORCES still says every `shutdown_ok` store target from anything but the predicate fails, contradicting the intentionally accepted fail-closed literal `False` initializer. Main Agent independently reproduced every read-only binding after the spawn: raw 213019-byte diff `bc67f5d4...`, packet hash, all ten file hashes, HEAD/gitlink/nested/staged/process/port state. Finding accepted unchanged in `iteration-006/triage.yaml`; separate iteration-007 implementer spawned for the bounded wording plus plain-True proof. P3-WP13 remains **NOT COMMIT_READY**. No stage, commit or push.
- **2026-08-22 (session 3, tracker line-ending correction)**: The first §16 tracker patch in this session inserted 8 bare LF endings into the otherwise pure-CRLF plan. Detected immediately by the byte-count check (`1073 CRLF / 8 bare LF`) before any later step relied on it. Mechanically normalized only the line endings back to CRLF; post-correction count **1081 CRLF / 0 bare LF / 0 bare CR**, and `git -c core.autocrlf=true diff --check` is empty. The erroneous intermediate count stays recorded here rather than being hidden.
- **2026-08-22 (session 3, review started)**: Re-derived the §3 baseline before trusting the handoff. Parent branch/head `fix/change-aware-save-mcp-autonomy` / `bfa7bc1ccfe1fc23aedac7cbc86a25fba617d8a7`, upstream 0/0; nested head and recorded gitlink both `71a89c703e9fa17a753b2b553b1a1513f4e23114`, upstream 0/0, content diff and staged diff empty. Parent staged diff empty. All ten tracked SHA-256 values reproduced exactly, including WP13 `9ffb11b8...` / `6ad3aa84...` and all eight frozen/WP12 paths. The authoritative raw Git diff was hashed from `git -c core.autocrlf=true diff --binary` bytes through a .NET stream and reproduced **`bc67f5d4b09b75f96b6f0b3e8323df263fcd449b4925c32f9f7d4db6e91a5c78`**. A first Git-Bash pipeline printed a different hash because the cross-shell pipe transformed output bytes; it was rejected as non-authoritative and no repository write occurred. Port 9875 had no listener and an actual loopback bind succeeded. Existing Python MCP helpers were mapped to a separate AutoCurtains/profile session on port 9876, with no FreeCAD/FreeCADCmd process; classified unrelated and left untouched. Spawned a fresh independent read-only iteration-006 reviewer, instructed to write `re-review.yaml` incrementally with `NOT_REVIEWED_YET` until complete, recompute every binding, avoid all live stages, and target the wrong-function docstring-probe near-miss plus the store-target limitation. No commit, stage or push.
- **2026-08-22 (session 2, round-up)**: **The spend-limit block is GONE** - three subagent spawns completed normally, so the state was never `BLOCKED_MODEL_UNAVAILABLE`. Resolved the stalled iteration-004 review and ran **two full section 2 loop cycles**. **iteration-004 re-review** (fresh spawn): **PROBLEMS**. GRK-P3-109/110/111/112 all **RESOLVED**, verified by *offline execution* over five shutdown shapes and ten argv shapes, and by **per-function AST hashing** proving `graceful_shutdown_owned_session`, `shutdown_launcher` and `stage_verdict` byte-identical i003 to i004 - so **no GRK-P3-099 workaround was smuggled in**. Ruled the **7-key superset CORRECT** by *executing* the clean path rather than accepting the orchestrator's structural argument, and proved the WP09 `skip_*` shape unreachable. Ruled the false `success: True` return **correctly routed to P3-WP14**, enumerating all three consumers. Filed **GRK-P3-113** (no landed proof the allowlist *accepts* a clean shutdown - the only positive assertion was true by construction) and **GRK-P3-114** (the section 10 loop-breaker guard did not enforce its own docstring: name-keyed, `ast.Assign`-only, so a renamed gate and an `AnnAssign` fail-open both evaded). Both **ACCEPTED** after the orchestrator verified them against source. **iteration-005** (implementer): test-only, `stress_coordinator.py` untouched; **33 passed / 2 deselected / 0 skipped**, 27 arch+launcher, ruff/compileall/diff-check clean, native Windows **3 passed in 133.39s** = **63 passed, 0 failed**. **iteration-005 re-review** (fresh spawn): **PROBLEMS**. Both prior findings **RESOLVED** - the reviewer ran its own mutation harness (**7/7 keys bite, TOTAL_NON_BITING=0**) and went further, proving the zero-open-documents shape **load-bearing**. All six implementer claims **CONFIRMED** by independent execution. Confirmed the guard family is **measurably converging** (i004 guard missed 7 of 8 probe shapes; composite missed 6 of 8, every new residual a subset). Filed **GRK-P3-115** (guard still under-fires: a direct `graceful_shutdown_owned_session` call bypasses the wrapper the taint half keys on, and `AugAssign`/walrus/tuple-unpack bindings of `shutdown_ok` **fail OPEN**) and **GRK-P3-116** (guard **over-fires on correct code**: the keyword form of the correct gate is rejected because the tainted Name's parent is an `ast.keyword`, not the `ast.Call` - a red-on-correct-code trap in the one test that must stay trustworthy, and keyword style is already the local idiom at `:2469`). Both **ACCEPTED** after independent verification. **User decision:** iteration-006, fix both, then commit WP13 - taken over deferring, because the reviewer had already prototyped both fixes with zero regressions and named the `stage_verdict`-parameter exemption subtlety. **iteration-006** (implementer): test-only again, `stress_coordinator.py` still byte-identical; falsification set grown **5 to 10 entries, none removed**; **GRK-P3-115 item (3) was NOT skipped** - the unqualified "a fifth gate... fails here" sentence is gone, replaced by an explicit *WHAT IT ENFORCES / WHAT IT DOES NOT* split naming both residuals, which is the item that **terminates** the 111 to 114 to 115 family instead of inviting a seventh round. Main Agent gates, all six exit 0: **33 passed / 2 deselected / 0 skipped**, **27 passed**, ruff `All checks passed!`, compileall, `diff --check` empty (0 bytes), native Windows **3 passed in 113.87s** - **63 passed / 0 failed / 0 skipped**. **ORCHESTRATOR ERROR, DISCLOSED:** the iteration-005 reviewer brief carried a **mistranscribed** i004 delta-base hash (`f86430e0af7af174...` for the correct `f86430e0afa7af178d35109ca4e3660d8c0b9cd04f0779220d2d97f5c72cf9df`) - the **second** hash-transcription error on this work stream. Cost was **zero** only because the brief labelled that line "do NOT trust this line, recompute it"; the reviewer recomputed, caught it, and correctly identified the brief rather than the repo as wrong. The erroneous claim stays on the record. **P3-WP13 is NOT COMMIT_READY: GRK-P3-115/116 are gated but UNREVIEWED. Nothing was committed, staged or pushed this entire session, and the gitlink has still never been bumped.**

- **2026-08-22**: **Session ends with P3-WP13 iteration-004 UNREVIEWED. Attempt 2 was halted by the account monthly spend limit — not a transient error, and it blocks the process itself**, because §1.0 requires review and implementation to be separate spawns and §1.1 forbids the orchestrator absorbing either role. **The tactical change made after the 529 was vindicated**: instructing the reviewer to write its artifact **incrementally** is the only reason anything survives. What it holds is worth having — it **independently re-derived** the diff fingerprint `a702b455…`, the lineage `a6dc5187 → ba3045d5 → 59ec6aa2 → a702b455`, both file hashes, `staged: nothing`, and the nested **0-byte** content diff, the last of which it recorded having *checked rather than inherited*. What it does **not** hold is any finding, ruling or invariant check. It is stamped **`verdict: NOT_REVIEWED_YET`** and `artifact_state: IN_PROGRESS` precisely so its existence cannot be read as a clean review — the specific failure this record has guarded against all session, and now a live risk, because the file is real and its bindings block looks authoritative. **P3-WP13 is NOT COMMIT_READY. Nothing was committed, staged or pushed in this entire session, and the gitlink has still never been bumped.**

- **2026-08-22**: **The iteration-004 re-review has not happened yet — attempt 1 died on an API 529 Overloaded and returned nothing.** Recorded because an absent review is the easiest thing in this whole process to misread as a clean one. Orchestrator verified the aftermath rather than assuming a read-only agent left no trace: **no `re-review.yaml` was written at all**, the tree is byte-identical (fingerprint still `a702b455…`), HEAD/gitlink/nested unmoved, nothing staged, 0 processes, nothing on 9875 — a clean failure with nothing to salvage and nothing to undo. The agent was resumed with its context intact and given one tactical change now that the failure mode is known: **write the artifact early and incrementally**, so a second interruption leaves a partial record with honest gaps instead of nothing, with any unreached section treated as **NOT_REVIEWED**. It was also told not to reconstruct counts or probe results from memory — verbatim or nothing. **P3-WP13 therefore remains unreviewed at iteration-004 and NOT COMMIT_READY.**

- **2026-08-22**: **P3-WP13 iteration-004 — GRK-P3-109/110/111/112 all resolved, and the §10 structural constraint was met rather than worked around.** No fourth denylist clause: `window_error` is not referenced anywhere in the new predicate. One allowlist, `ordered_shutdown_completed()` at `:2227`, requiring **every** transition in `SHUTDOWN_TIMESTAMP_KEYS` to be non-null in the **persisted** block, retaining the GRK-P3-106 conjuncts so it only tightens, applied at **2 of 2** gates with a fail-safe `False` initialiser. Two things make it durable rather than a one-time patch: the key tuple is **imported from the frozen `evidence.py`**, so a transition added later is required automatically; and an **AST test enforces** that every gate asks the one predicate, that the caller set is exactly `{run_stage, _run_preflight_only}`, and that the old `shutdown_launcher(...).get("success")` shape cannot reappear. Main Agent gates: acceptance **32 passed / 0 skipped** (was 28), arch+launcher **27 passed**, ruff clean, compileall, `diff --check` empty, native WP09 **3 passed** — **62 passed / 0 skipped / 0 failed**, all exit 0. Fingerprint `a702b455…`.
- **2026-08-22**: **Bite-proof B is the most valuable artifact this work package has produced.** Reverting to the denylist **plus the `window_error` clause exactly as the orchestrator filed GRK-P3-109** yields **2 failed, 30 passed, exit 1** — the 109 test passes while the `local_driver=None` test still fails. The fix the orchestrator originally proposed **would have left a hole**, and the denylist's non-termination is now demonstrated by experiment rather than argued. Proof C reproduces GRK-P3-110 end to end (`AssertionError: PART3_RESULT: PASSED`). Proof E fired the two exact-`failed_checks` tripwires when the stub was reverted, and the response was to **make the stub honest, not to relax the tests**. Recorded alongside the one honest gap: requiring all 7 transitions is verified **structurally, not empirically** — no live green stage is reachable until GRK-P3-099 lands, and the implementer disclosed that rather than claiming otherwise. Third consecutive iteration in which the source-snapshot practice turned a prose claim ("I did not touch the helper") into a mechanical check (`8022ce9b…` in both revisions).

- **2026-08-22**: **iteration-003 re-review PROBLEMS — GRK-P3-110 is a real false PASS, and GRK-P3-111 was introduced by the fix that was meant to prevent exactly that.** GRK-P3-106 was fixed in `run_stage` only; `_run_preflight_only` — **a function this uncommitted diff introduces, absent from HEAD's 568 lines** — still gated on `success` alone. Driven with the real helper, it returns **exit 0** and prints **`PART3_RESULT: PASSED`** while `failed_step: 'document_close'` and three null phase timestamps sit in the same artifact, and it performs **no write after the `finally`**, so nothing corrects it. Rated **high not critical** because GRK-P3-077's `mode: preflight_only` stamp caps the blast radius — the reviewer reasoned severity **down**, which is the right direction to be conservative in. Separately, iteration-003's GRK-P3-108 fix **introduced GRK-P3-111**: `--preflight-only` is treated as a refusal path, but that is true only alongside `--stage`, so `main(["--preflight-only"])` launches a real GUI while the detector certifies it `live=False` — **worse in kind than the blind spot it closed**, and the false premise is now written into a comment. Second time here that a remedy for false confidence produced new false confidence; the first was GRK-P3-101.
- **2026-08-22**: **§10 invoked in spirit, and GRK-P3-112 is why.** With `local_driver is None` every arm of the window-close chain is False, so the deadline branch is reached having recorded **no marker field at all** — `shutdown_ok` stays True **even with the GRK-P3-109 clause applied**. That proves a **denylist over failure markers cannot terminate**: there is no field to add, because the failing path records nothing. The same defect class has now surfaced at **four** sites (106, 109, 110, 112). Iteration-003 satisfied §10 literally and no single finding reopened twice, but one-clause-at-a-time is precisely the orbiting §10 exists to stop. **Ruling: iteration-004 may not add a fourth clause** — it must deliver one shared **allowlist** predicate over the phase transitions (what the check's own name already asserts, and what `_assert_green_stage` already does on the pytest path), applied at **every** gating site. All four findings routed together: 109 and 112 are one expression, and 110 exists *because* the two CLI paths were fixed independently.
- **2026-08-22**: **Orchestrator self-correction — the same failure mode repeated one day after being written down.** GRK-P3-110 was visible in output the orchestrator itself produced: a grep for `shutdown_ok = ` printed **both** sites, the tightened `:2414` and, seventeen lines later in the same output, `:2476 shutdown_ok = bool(…get("success"))` in `_run_preflight_only`. The first was read, the fix confirmed, and the second never questioned — SELF-2026-08-21-A verbatim: verifying one path and treating the invariant as established. Recorded again rather than quietly noting the reviewer caught it. Standing check adopted: *when a predicate is tightened to fix a finding, every other site computing that predicate belongs to the same finding until proven otherwise — grep for the predicate, not for the line being fixed.* Also recorded: the reviewer **confirmed the GRK-P3-099-unmasked invariant by enumerating 17 offline cases** rather than checking one route — the discipline demanded after the previous refutation, applied, and the reason that invariant can now be relied on.

- **2026-08-21**: **P3-WP13 iteration-003 — GRK-P3-106/107/108 resolved, all Main Agent gates green, and the implementer disclosed a residual rather than quietly widening scope.** The **106** fix is acceptance-side only: `shutdown_ok` now also requires `failed_step is None and rpc_error is None`, so an aborted RPC phase can never record the check green regardless of whether the process exits inside the deadline — **tightened, never bypassed**, with a forced kill still failing exactly as before and GRK-P3-099 still unmasked. The bite-proof for 106 is the strongest artifact in this work package so far: reverting the fix makes the harness **print the false green verbatim** (`'passed': True` beside `'failed_step': 'document_close'`). Main Agent gates: acceptance **28 passed / 0 skipped** (was 26), architecture+launcher **27 passed**, ruff clean, compileall, `diff --check` empty, and native Windows `test_part3_shutdown.py` **3 passed, exit 0** — **confirming the implementer's stated WP09 prediction**, which it was asked to make precisely so a wrong one would be visible. **58 passed / 0 skipped / 0 failed** overall.
- **2026-08-21**: **GRK-P3-109 filed: the window-close twin of the 106 false green.** The window-close failure branch sets `stalled_stage` and `window_error` but **neither `failed_step` nor `rpc_error`**, so the newly tightened predicate still evaluates **True** — a shutdown whose **window close failed** can still record `graceful_shutdown_completed_without_forced_termination` as passed. The **implementer found this in its own fix**, declined to widen beyond the finding as triaged, proposed the one-clause remedy and recommended filing; the orchestrator verified it in source before filing rather than taking it on report. That is §4.1 working as intended in both directions. **The source-snapshot practice adopted one iteration earlier also paid off immediately**: the implementer's "I did not touch the helper" claim was verified by **diff** — two hunks, both inside `run_stage`, helper text hashing identically at `8022ce9b…` — rather than by reading prose, which is exactly what the previous reviewer had been unable to do.

- **2026-08-21**: **iteration-002 re-review PROBLEMS — GRK-P3-106 refutes the GRK-P3-099-unmasked invariant on a SECOND path, and the orchestrator had already read past it.** At `stress_coordinator.py:291-297` the deadline branch sets `forced = False`, **erases `stalled_stage`** (the field GRK-P3-100 had just fixed) and returns `success: True`, while `:2396` consults neither `failed_step` nor `rpc_error` — so an ordered shutdown that **aborted its RPC phase** records the acceptance check **GREEN** whenever the process happens to exit inside the 60s deadline. Proved by an **offline probe**, no FreeCAD, no sockets. Three limits the reviewer put on the record rather than inflating the finding: it is **latent** (both live runs force-killed), the implementer **did not introduce it** (pre-existing at HEAD), and the live pytest test would catch it **but the CLI gate the Main Agent actually runs would not**. The orchestrator confirmed it by source read and corrected one reviewer detail: line 295 is *not* byte-identical (`verdict="PASSED"` → `success_verdict`), a WP10-era **mitigation** that narrows the blast radius without closing it — the check still goes green inside a FAILED stage.
- **2026-08-21**: **Orchestrator self-correction: a narrow verification was reported as a general one.** The iteration-002 pass packet asserted the GRK-P3-099-unmasked invariant held. Every clause was true for the path checked, and the implementer genuinely did not take the forbidden workaround — but only **one** route was verified and it was written up as though the invariant itself had been. The second route sits in a function the orchestrator **had already read in full earlier in the same session**. This is a different failure mode from the three earlier orchestrator errors: not a wrong assertion, but a failure to **enumerate alternative paths** before declaring an invariant held. Rule adopted and recorded: *"no workaround was introduced" is a claim about a diff; "the invariant holds" is a claim about a program, and only the second requires the enumeration.* Appended, not rewritten.
- **2026-08-21**: **GRK-P3-100 fully discharged by a live gate; the reviewer's regression concern did not fire, and the reason is recorded rather than waved through.** `test_part3_shutdown.py` ran against the iteration-002 tree: **3 passed in 129.64s, exit 0**. The reviewer had correctly refused to close the finding on an iteration-001 run. Its concern — that with `skip_document_close=True` a raising RPC phase would now yield `"document_close"`, outside WP09's accepted set — was **conditional on `list_documents` raising**, and it did not: the phase connected, listed, skipped the close, then took the `skip_rpc_shutdown` branch, setting `stalled_stage` **without entering the `except`**. **A green run proves the condition did not fire, not that it cannot**, so it survives as a latent observation and is not declared resolved. Also adopted: the reviewer's structural recommendation to store a byte copy of each in-scope file beside its packet, because the intent-to-add empty blob made the iteration-001→002 delta **uncomputable**. iteration-001's bytes are unrecoverable; that gap is recorded, not papered over.

- **2026-08-21**: **P3-WP13 iteration-002 delivered; Main Agent offline gates GREEN; GRK-P3-099 confirmed still unmasked.** The resumed implementer resolved **GRK-P3-100..105, none unresolved**, reported nothing half-applied, and re-derived every number from scratch. The orchestrator ran its own §8 gates rather than citing the agent's: acceptance offline **26 passed / 2 deselected / 0 skipped**, architecture **13 passed**, launcher **14 passed**, `ruff` clean, `compileall`, `git diff --check` empty — **all exit 0**, totals **53 passed / 0 skipped / 0 failed**. CRLF pure (2509/2509, 1545/1545). New fingerprint `ba3045d5e1078857…`. **The single most important negative result: the harness was NOT made to pass by working around GRK-P3-099.** Verified by source read — the `close_document` loop has no local `try`, so the error still propagates, aborts the RPC phase, force-kills and stamps `FAILED`. Both acceptance checks unweakened, forced termination still unconditionally `FAILED`, scope fully held (nested diff 0 bytes, gitlink and HEAD unmoved, nothing staged). **This certifies the offline gate only.** §11 still requires a reviewer `NO_ISSUES` on this exact fingerprint, and three implementer disclosures remain unruled — notably that GRK-P3-100's `required_test` asks for a **live** `test_part3_shutdown.py` run that has not happened against this tree, so that finding is **partially discharged, not closed**.

- **2026-08-21**: **P3-WP13 iteration-002 implementer interrupted by an API connection error — edits present, packet absent, nothing credited.** Its final signal was *"All six bite-proofs confirmed. Final verification round."* **That claim was not accepted.** The orchestrator established the state from the tree instead: both in-scope files edited (`671d9abc…` / `9ed3e4e0…`, 2421→2509 and 1187→1545 lines), both **pure CRLF**, `compileall` **exit 0**, and — the part that mattered most — **scope fully held**: `actions.py` still `d1ad4f70…`, all six frozen WP10 paths byte-identical, nested repo diff **0 bytes**, gitlink and HEAD unmoved. But `results/.../iteration-002/` was **empty**: no §7 packet, so the change set is **ungated, unreviewed and undocumented**, exactly the condition §16 recorded for WP12's `actions.py` on 2026-08-19 ("has never been run against a live gate and must not be trusted"). The agent was **resumed with its context intact** rather than re-derived from scratch, and instructed to re-run its gates from scratch rather than reconstruct counts from memory, to report any half-applied edit plainly, and to report any bite-proof it cannot now reproduce as **unproven**. Recorded here because an interrupted implementer whose work looks finished is precisely the state that produces a confident false green.

- **2026-08-21**: **P3-WP13 reviewer verdict PROBLEMS — and the reviewer solved the defect the orchestrator deliberately refused to guess at.** GRK-P3-099: `close_document_gui` reports every **successful** close as a **failure**, because its post-close verification calls `FreeCAD.getDocument`, and `sGetDocument` (`ApplicationPy.cpp:647-658`) **raises `NameError`** rather than returning a falsy value — so the broad `except` converts success into `{success: False, error_code: NAMEERROR}`. The `success: True` return is **unreachable**; `DOCUMENT_NOT_FOUND` is **dead code**. Established by **probe**, not by reading the error text — the open-and-closed case and the never-existed case produce byte-identical payloads, with the document genuinely removed in the first. Nothing caught it because Part 3 has **never once closed a document successfully end to end** (WP09 uses `skip_document_close=True` or no open documents) and every nested stub models `getDocument` as returning `None` — **the stubs encode an API contract FreeCAD does not implement**. Consequence: `graceful_shutdown_completed_without_forced_termination` is **unpassable until this nested fix lands**, so Stage A, B and C are unpassable regardless of every other check. Routed to the nested package with GRK-P3-095, and the reviewer's **critical warning against a coordinator-side workaround is adopted verbatim**.
- **2026-08-21**: **Two orchestrator claims corrected in the same review — appended, not rewritten.** (1) The failure packet stated `ApplicationPy.cpp:462` is `getDocument`; it is `sCloseDocument` (`:456`), while `sGetDocument` is `:647` with an identical message at `:656` and a third at `:519`. Because the text is identical at all three, the artifact **could not** identify the caller — the claim was under-determined and stating it as fact was the error. (2) The packet read the green `build_provenance_ties_binaries_and_stage_sources_to_commits` as *"GRK-P3-098, FIRST live validation"*, but its predicate never consults `predates_head` or `commits_not_in_binary` — it proves provenance was **recorded**, not that any binary is bound to any commit, and it went green with binaries **36/3/39/6** commits stale. Filed as **GRK-P3-101**. That is a green check's **name** read as its **content** — the same class as GRK-P3-089 and the `git_state: null` misread. Running tally on this work stream: **six** confident causal claims refuted by evidence, **three of them the orchestrator's own**. Every one was caught by executing a stated falsification condition before building on it.
- **2026-08-21**: **§13 policy DECIDED by the user: "Rebuild binaries, allow dirty tree."** Before any Stage A/B run meant to **count**, the binaries must be rebuilt so no committed C++ is missing from them; the uncommitted Python harness changes — which are the entire WP10/WP13 surface under review — remain permitted, and a dirty tree alone does not disqualify a run. The orchestrator supplied the decision-relevant facts first, **including the caveat that undercut its own framing**: `commits_not_in_binary` compares binary mtime to commit timestamp, so it is an **upper bound on staleness, not proof of absence**, and a binary built from a dirty tree and committed afterwards is flagged stale while actually containing the change. Consequences recorded: a rebuild is now a **precondition of the first counting run**; the 2026-08-21 Stage A is **diagnostic only**; and GRK-P3-101 is still to be fixed by **renaming** the check, never by failing the stage on a lagging binary.
- **2026-08-21**: P3-WP13 §6 triage — **GRK-P3-099..105 all ACCEPTED**, none softened, none disputed. Four load-bearing claims re-read in source **before** acceptance and all confirmed, including one that **refutes the implementer**: WP09's frozen assertions do not constrain the `except`-branch `stalled_stage`, because that test passes all three skip flags (`test_part3_shutdown.py:299-301`) and never enters the `except`. So fix-notes §2's stated reason for keeping the false `"rpc_shutdown"` label does not hold — and the new test at `test_part3_stage_acceptance.py:1081` now **pins** that false label, turning a carried-over inaccuracy into an asserted contract (**GRK-P3-100**). Also accepted: **102** (`STAGE_SOURCE_FILES` omits `driver.py`, `control_channel.py` and `InitGui.py` — three of the four modules that actually run inside FreeCAD, including the one owning the Qt owner-thread hop), **103** (an aborted stage records no count check, which is why 3/1 had to be hand-counted into two packets), **104** and **105**. The reviewer **did not launch FreeCAD**; the orchestrator independently re-verified the aftermath.

- **2026-08-21**: **P3-WP13 live gates run for the first time — Stage A FAILED again, and the implementer's central prediction was REFUTED.** Stage A: `PART3_RESULT: FAILED`, exit 1, **3 cycles / 1 save** against ADR §13's 10/5 — it **aborted early again** and must not be described as having "run". 8 checks recorded, **6 passed / 2 FAILED**. `graceful_shutdown_completed_without_forced_termination` still fails with **`forced: true`**, against fix-notes §3's prediction that "the shutdown evidence should show the ordered sequence completing with `forced: false`". In fairness to the implementer: it explicitly labelled that a prediction it could not run and asked for it to be checked — executing the stated falsification condition is exactly what caught it, which is the process working. But **GRK-P3-096 is NOT RESOLVED.** That makes **five** confident causal claims refuted by evidence on this work stream. Regression gates held: WP09 `test_part3_shutdown.py` **3 passed** exit 0, WP12 `test_part3_local_driver_focused.py` **9 passed** exit 0. All 9 tracked hashes, HEAD, gitlink and nested HEAD re-verified **byte-identical after** the live runs; 0 leftover processes, nothing listening on 9875.
- **2026-08-21**: **The GRK-P3-096 fix is partially effective, and the way it failed is the useful part.** The shutdown error moved from **`-32600 Invalid Request`** to **`-32000 Unknown document 'Part3StageAPrimaryc602b2a1'`**. −32600 is a framing-layer rejection raised *before* method lookup; −32000 is an application error raised *inside* the method body. So the structured-params fix genuinely did what it claimed **at the layer it targeted**, and a **second, distinct defect** sits underneath — filed as candidate **GRK-P3-099**. The new `failed_step: "document_close"` field is **correct and validated live**; `stalled_stage` still reports `"rpc_shutdown"`, a step never reached, which fix-notes §2 records as a deliberate choice to protect WP09's frozen assertions and which is referred to the reviewer rather than asserted as a defect. **GRK-P3-098's check passed live for the first time**, reproducing the same binary spread (36/3/39/6). For GRK-P3-099 the orchestrator recorded **facts only and did not assert a root cause**, with three falsification conditions to run first — the discipline that has caught every one of the five refuted diagnoses so far.
- **2026-08-21**: **Baseline correction — a handoff transcription error, not a file change.** The §3 handoff abbreviates `tests/gui/part3/scenarios.py` as `2c0c8e99175a4d1b…` (`HANDOFF-PROMPT.md:40`), which drops two characters. The full value recorded in three independent evidence artifacts is `2c0c8e99175a7a4d1bbbdad5966ed5b8663cb7da8f17dbb5e55a516654bd26c1`, exactly what the file hashes to. scenarios.py is unchanged and still frozen. Recorded here because the correct response to a hash that "doesn't match" is to check the record before re-baselining — the abbreviation was wrong, the file was not.

- **2026-08-20**: **P3-WP13 opened and iteration-001 delivered — but nothing in it has faced a live gate.** GRK-P3-096 **RESOLVED with its root cause proven offline**, and the proof is the interesting part: `graceful_shutdown_owned_session` passed a **bare string** as JSON-RPC `params`, which JSON-RPC 2.0 forbids, so the framing layer returned **-32600 before the method was ever looked up**. It fired on the *first open document*, inside the same `try` as `shutdown_rpc_server`, so the shutdown verb was never sent — which is why every timestamp was null and the process was force-killed. The `except` then stamped `stalled_stage: "rpc_shutdown"`, **naming a step the phase never reached**, and that misleading label is what misdirected the first triage. It also explains why WP09's `test_part3_shutdown.py` passed on both platforms: it tears down a session with **no documents open**, so the faulty loop body never executes. That is the call-shape difference the failure packet suspected — now identified rather than assumed, satisfying §10 root-cause mode. GRK-P3-097 and 098 resolved. Offline gates: architecture **13 passed**, acceptance offline subset **21 passed / 2 deselected / 0 skipped**, launcher **14 passed**, `ruff` clean, all exit 0; regression tests verified to **bite** (reverting the fix reproduces the evidence signature verbatim). The implementer disclosed its own mistake: a first opt-in test used `pytest.raises(Exception)`, but `pytest.skip` raises `Skipped(BaseException)`, so **the test silently skipped itself and proved nothing** — caught with `-rs` and fixed.
- **2026-08-20**: **GRK-P3-095 is BLOCKED and needs the nested repository — the first such change on this branch.** Root cause proven offline: the authenticated identity is written into `rpc_server/request_identity.py`, a **`threading.local()`**, on the RPC handler thread, but `recompute_helpers.undo` hands the work to `_dispatch_gui` and `undo_gui` calls `actor_from_session` **from the Qt owner thread**, where that thread-local is empty; `run_lease_aware_gui_task` explicitly discards `captured["identity"]`. `commit_checked_property` runs **inline with no `_dispatch_gui`**, which is exactly why it was admitted in the same session — the orchestrator's "method-scoped, not credentials" reading is confirmed and now mechanised. The nested repo's own `undo_gui` tests call it directly with a mocked identity, so they never exercise the hop. No `xfail`, no skip, no weakened check: the failing assertion stands.
- **2026-08-20**: **Correction — the orchestrator's "git_state: null" claim was WRONG**, and the implementer caught it. `evidence.json` has no key named `git_state`; the block is `environment.git` and it is fully populated (branch, `parent_commit bfa7bc1ccf…`, `nested_commit 71a89c70…`, `recorded_gitlink`, `dirty: true`). The orchestrator queried a non-existent key and reported the resulting `None` as a finding — reading a missing key as a missing feature. Appended to the failure packet rather than rewritten. The substantive half of GRK-P3-098 survives untouched and was never dependent on that key: nothing bound the **binary** to a commit. `environment.build_provenance` now quantifies it and confirms the orchestrator's earlier "mixed, not uniformly stale" correction — FreeCAD.exe 36 commits behind, FreeCADApp.dll 3, FreeCADBase.dll 39, FreeCADGui.dll 6. **Two orchestrator assertions have now been refuted in the same direction** (this and the `reset_property_editor` root cause at GRK-P3-085); weight future orchestrator confidence accordingly.
- **2026-08-20**: **Stage A executed for the first time under the plan, by the Main Agent, and FAILED.** `PART3_RESULT: FAILED`, exit 1, **3 cycles / 1 save** against ADR §13's required 10/5 — it aborted early and must never be reported as having "run". Both checks failed identically to the reviewer's diagnostic Stage A *and* Stage B: deterministic across three executions, not a flake. Recorded as the §8.3 failure packet at `2026-08-20/P3-WP13/failure-packet.yaml`. One ordering fact was flagged rather than glossed: because the undo check aborted the stage at cycle 3, the observed shutdown ran during an **aborted teardown**, so whether the defect also occurs after a successful stage is **not established**.
- **2026-08-20**: **P3-WP10 bounded §9 re-review — all five scope items PASS, GRK-P3-092 `RESOLVED_NOT_APPLICABLE`, WP12 blocker CLEARED.** No `stress_coordinator.py` edit was required, for a structural reason: `_local_property_edit` establishes the selection and computes expected properties itself, so the new arguments never cross the RPC boundary. **The reviewer breached its mandate** — forbidden from launching FreeCAD, it ran `test_part3_stage_acceptance.py` without deselecting the two live tests and executed Stage A and Stage B end to end. It disclosed this unprompted and did not present the run as authorised evidence. The orchestrator verified the aftermath independently: 0 FreeCAD processes, nothing on 9875, HEAD unchanged, all 9 tracked hashes identical, no `evidence.json` inside the repo. Per §1.0 that run is a **diagnostic signal, never gate evidence** — and it exposed GRK-P3-095/096, which is why WP10 must now reopen under §13.

- **2026-08-20**: **P3-WP12 Done — committed `bfa7bc1ccf`, pushed** (`0a193827f4..bfa7bc1ccf`, remote verified, 0 ahead / 0 behind). One file, 345 insertions / 27 deletions, staged by explicit path; nothing from `results/` staged. Fix attempt 2 made the label comparison locale-correct by mirroring `PropertyItem::setPropertyName` — camel split, then `QCoreApplication.translate("App::Property", …)`. **All five native Windows gates green** (R6 3 passed, full file 9 passed, reversed 9 passed, R2 guard 2 passed, 084 cross-document pair 2 passed; AST 13 passed; 25/25 executions, **0 skipped**, one identical source state), and the **Docker neutral subset** passed on linux/amd64 (`GATE_EXIT=0`). §11 verified item by item before committing: both fingerprints matched their bindings exactly (`d1ad4f70` / `3758d760`). **The WP04 pre-stress gate is genuinely green, which unblocks WP10.**
- **2026-08-20**: P3-WP12 reviewer Docker-gate ruling. §11's Docker gate **binds** WP12 but is discharged by a **platform-neutral subset**, because §8.1 items 1/4 are vacuous here and §8.2 assigns GUI owner-thread behaviour to the native Windows gate — *"Docker may not certify those properties by proxy."* The ruling **corrected the orchestrator's structural analysis**: `build/release/bin/FreeCAD` already exists and is a **Windows PE byte-identical to the `.exe`** (both `2658afed…`, magic `MZ`, verified by the orchestrator), and the Linux tree is `build/debug`, which the resolver never inspects — so the orchestrator's proposed workaround would have produced a **false RED** reading as a source regression. WP04–WP07's Windows-only precedent ruled a legitimate *platform choice*; the missing Docker line was the actual shortcut, and WP12 stops repeating it. New finding **GRK-P3-094** routed to a **new work package** and flagged a potential Part 3 **GO blocker**: the harness cannot select a Linux FreeCAD from a clean checkout, so §13's Linux Stage A/B structurally cannot run.
- **2026-08-20**: P3-WP12 **GRK-P3-089** — blocking, record-only, and **the orchestrator's own error**. Five artifacts named `green-*.log` contained attempt-1 **RED** results with `GATE_EXIT=1`; they were named when the chain was *launched*, before results existed, and never corrected when it came back red. Not a fake green — the pass packet cites `a2-*` and every number matches — but it **misled the independent reviewer**, which opened those files first and briefly concluded the gates had failed. Renamed to `a1-*`, including the five `id:` fields the required fix did not mention. Convention now in force: `a1-*` / `a2-*` / `red-*` / `bisect-*` identify **which run** produced a file and **assert no outcome**, making this defect class structurally impossible rather than fixed once. The reviewer independently verified the renames were **content-preserving, not rewrites** — byte-identical tails, same line numbers.
- **2026-08-20**: P3-WP12 fresh reviewer re-review → **NO_ISSUES**, bound to `actions.py` `d1ad4f70…` and a newly defined test-packet fingerprint `5eb16f47…` (a manifest hash over the six `a2-*` artifacts, so any later edit to an attempt-2 log moves it). Rulings **083/084/085/087 PASS; 086/088 PASS_SOURCE** — 086 because no Stage A/B has run against the fixed tree, 088 because only the resolver's single-candidate branch has ever executed. It re-derived the root cause from C++ independently, ruled the race **genuinely closed rather than relocated** (clearing the one surviving candidate, `PropertyEditor::buildUp`'s `committing` early-return), and **upgraded** the implementer's disclosed translate assumption to *locale-independent by construction*, since the driver runs in-process on the Qt owner thread and both calls hit the same translator chain. It also **refuted the orchestrator's referred locale-fragility concern**: `stress_coordinator.py` passes raw names rather than resolving labels, so this diff fixes it — no new work package needed.
- **2026-08-20**: P3-WP12 fix attempt 2 verified and gated. The implementer **detected that the excluded-fingerprint anchor had moved** and refused to re-anchor quietly, forcing the cause instead: the orchestrator's own tracker edit during the session pause, on the one path `baseline.json` exempts. New auxiliary anchor `f9b36871…`, independently confirmed. Its offline harness gained the case attempt 1 lacked — a Dutch fixture whose `Angle` row renders as `Hoek`, with a **pre-fix sentinel** asserting the old comparison still rejects it, so the harness fails when the bug is present instead of passing vacuously (**21 pass / 0 fail**). Recorded, as it insisted, as a contract proof against stubs and never as a live result.

- **2026-08-19**: **Session paused by user at P3-WP12 attempt 2.** The implementer was stopped mid-retry, so the tree holds an **ungated, unreviewed** `actions.py` (`d1ad4f70…`, whole-tree `827a5f64…`) that has never faced a gate. Recorded as untrusted rather than described as a fix, because an unvalidated edit silently inherited as "done" is exactly the failure mode this plan exists to prevent. WP10's binding re-verified intact at pause: excluded-`actions.py` diff reproduces `3a84d52f…`, all 8 frozen paths match, CRLF 889/0/0, zero probe residue, no orphan FreeCAD processes. **12 live GUI gates** consumed this session, all sequential on port 9875.
- **2026-08-19**: P3-WP12 fix attempt 1 **FAILED all five live gates**, two of them regressions against the unfixed tree (reversed order, and the R2 guard which was `2 passed` before). Root cause established by the orchestrator from source, not guessed: the new object-scoped content check compares **translated** Property Editor labels against **untranslated** internal property names. Host locale is **nl-NL**; `FreeCAD_nl.ts:13-15` maps `Angle`→`Hoek` with its location being `propertyeditor/PropertyItem.cpp`; `App::FeatureTest` genuinely **has** an `Angle` property (`FeatureTest.cpp:101`), so the editor was bound **correctly** and the check called the object's own row foreign. `Qt::DisplayRole` returns `displayName()`→`displayText` = `translate("App::Property", camelSplit(name))` (`PropertyItem.cpp:594, 727`), so the raw name is not on that path. The pre-existing code survived only because `Float` has no Dutch translation — the path was **already** locale-fragile and the strict check made it visible. Attempt **1 of 3** under §10. Wrote `iteration-001/failure-packet-phase3.yaml`. **Referred to the next reviewer, not acted on unilaterally** (§4.1): the same fragility sits on WP10's frozen acceptance path, where AlphaValue/BetaValue survive only through the absence of a translation entry.
- **2026-08-19**: P3-WP12 diagnostic gate (R6, probe installed) Done — **1 failed, 2 passed, exit 1**, RED intact. Root rowCount **3**, labels `[Attachment, Basis, Box]`, **all zero children**: emptied group scaffolding, i.e. `PropertyModel::resetGroups` output, case 2 of the reviewer's four-case guide. Selection was **already correct**; the editor simply had not been rebuilt for it. Consequences recorded in all three directions rather than only the convenient one: the `TreeWidget::selectedDocument()` refinement **REFUTED**; the orchestrator's own literal stale-property-rows hypothesis **REFUTED**; the reviewer's core structural claim **CONFIRMED**. Restated precisely as **a race, not a determinism** — `rowCount() > 0` makes the predicate useless, not always-failing, which is why (3,7) passes and (1,3,7) fails. GRK-P3-088 **NOT LIVE** (exactly 1 widget, visible, under `ComboView`, not the dock instance whose preference defaults false at `MainWindow.cpp:878`); closes as a latent guard, explicitly not inflated into a cause. Wrote `iteration-001/diagnostic-result.yaml`.
- **2026-08-19**: P3-WP12 reviewer §6 resolution Done. Verdict stays **PROBLEMS**. The reviewer **withdrew its own** "permanent no-op" sub-claim as refuted by R2, re-verified the source chain, stated plainly that the literal reading does not describe the running system, and **declined to invent a mechanism to cover the gap**. It disclosed this as its second over-reach in the same direction across two packets and asked that its future confidence claims be weighted accordingly — recorded, with the process lesson: executing a stated falsification condition **before** routing work to an implementer is what caught it at zero cost to the product. Item 1 **REVISED** (reproducer → R6, already captured, must not be re-run; R2 becomes an accidental-pass guard); `required_fix` **unchanged** for 083..086; diagnostic retargeted to R6 and made mandatory; new finding **GRK-P3-088** (medium). Wrote `iteration-001/review-resolution.yaml`.
- **2026-08-19**: P3-WP12 Main Agent pre-fix RED campaign Done. **GRK-P3-083's own falsification condition fired**: the reviewer's predicted pair (3,7) returned **2 passed, exit 0**. The orchestrator stopped rather than hand an implementer a diagnosis whose stated reproducer had just failed, then ran six live gates on the unfixed tree and bisected the trigger from 9 tests to 3 — full file **1 failed/8 passed**; (3,7) **2 passed**; (1,2,4,5,7) **5 passed**; (1,2,3,7) **1 failed/3 passed**; (2,3,7) **1 failed/2 passed**; (1,3,7) **1 failed/2 passed**. Established test 3 **necessary but not sufficient**, with a co-factor in {1,2}. Open-document count (2 vs 3) recorded as a **correlation only**, explicitly not adopted as cause by either role. Wrote `iteration-001/reproducer-evidence.yaml`.
- **2026-08-19**: P3-WP12 opened under §4.1 as an `OUT_OF_SCOPE_BLOCKER` split out of WP10, with §3 baseline VALID. Reviewer verdict **PROBLEMS**: GRK-P3-083 (critical), 084/085/086 (high), 087 (medium) — all **ACCEPTED** unchanged in §6 triage after the orchestrator independently verified **ten** cited source claims. **Two orchestrator errors corrected in the record by appending, never rewriting**: GRK-P3-085 refuted the root cause in the orchestrator's own WP10 failure packet (`reset_property_editor` cannot restore a precondition it was credited with — `Selection.cpp:1998` early-returns on an empty selection), and GRK-P3-087 showed the orchestrator's own acceptance wording was too weak (a fully reversed run moves the victim ahead of its poisoner and would pass **even unfixed**). Reviewer ruled the fix belongs in `local_driver/actions.py` **only**, declined a `conftest.py` fixture fix on three independent grounds — thereby preserving WP10's per-path binding — and ruled a **bounded** WP10 re-review mandatory before Stage A/B. Also captured per-path SHA-256 of the 9 WP10 paths **before any edit**, so WP10's §11 binding survives once WP12 moves the whole-tree fingerprint; and started the Docker daemon, which was **down**, confirming image `sha256:b34e0e1e…` for the mandatory Linux recheck.
- **2026-08-19**: §16 **Next step** block corrected — it had called for a fresh reviewer spawn on fingerprint `073d86c4`, which was already stale. The iteration-003 reviewer had itself reconciled the discrepancy (`iteration-003/re-review.yaml:574-576`) and bound its **NO_ISSUES** to `3a84d52f`. Recorded because the handoff described §16 as current and trustworthy, and one block of it was not.
- **2026-08-19**: P3-WP10 implementer subagent iteration-003 Done, and Main Agent offline validation **PASSED** with every number reproduced rather than copied: `compileall` exit 0; architecture+launcher+control-channel **32 passed**; stage-acceptance offline subset **15 passed, 2 deselected** (+1 = the new GRK-P3-082 test); all four refusal shapes exit 2 with **0** `PART3_RESULT` lines; `git diff --check` exit 0. New fingerprint `073d86c4119074cf5ae4870689a826ee8a3f40c614e032586d7e245b86774aad`. The implementer proved its own scope by inverting its edits and reproducing the reviewed fingerprint `5b75dd84` byte-exactly, demonstrating `run_stage` was untouched. **Lint characterised precisely**: `ruff --isolated --select F,E,W` is clean on both changed files, while directory-wide it reports 10 errors — all pre-existing. Verified by extracting `conftest.py` at HEAD: **6 errors at HEAD, 6 now**, and its only change is one added list entry. This work package introduces **zero** new lint. Recorded rather than hidden, and not treated as blocking, because §8.1 scopes lint to changed surfaces. Environment note (reviewer N1): this host **does** have `build/release/bin/FreeCAD.exe`, so the refusal tests are not vacuous here. No commit.
- **2026-08-19**: P3-WP10 Main Agent triage (iteration-003) Done. GRK-P3-082 **ACCEPTED**, confirmed by direct source read: `stress_coordinator.py:546` computed `bool(preflight.get("ready"))` but the real driver's `_preflight` (`local_driver/actions.py:104-107`) returns only `pause_checkbox_visible` and `pause_checkbox_wired`, so the check was permanently false; it was hand-appended to `checks[]` at `:543` instead of via `record_check()`, never reached `failed_checks`, and `_run_preflight_only` never consulted them before printing PASSED and returning 0. Both the reviewer's **escalation** argument (a defect known since WP06 becomes blocking once our own new refusal message directs operators at the command that emits it) and its **sequencing** argument (fix before the live gates, because §§9/11 bind review and test evidence to a single fingerprint and a later fix would invalidate a completed Stage A/B) were accepted as sound. Wrote `iteration-003/triage.yaml`.
- **2026-08-19**: P3-WP10 reviewer subagent re-review (iteration-002) Done by a **fresh spawn** that had neither authored nor previously reviewed this diff. Verdict **PROBLEMS**, fingerprint `5b75dd84` independently reproduced. GRK-P3-070..081: **no FAIL and no regression**; 074/077/079/080/081 **PASS** proven offline by the reviewer running the four CLI refusals as real subprocesses; 070/071/072/073/075/076/078 **PASS_SOURCE** and explicitly not credited as passes. One new finding GRK-P3-082. The reviewer AST-scanned every `record_check`/`_require` for constant-true checks and found none, and confirmed the new `_run_stubbed_stage` harness cannot yield a mistakable artifact because it unconditionally records a failed `stub_harness_is_not_a_stage_run`. It re-examined ADR §1.4 **independently** rather than inheriting the earlier ruling, verified each supporting claim, and additionally established that the token-in-same-process concern is not a second unrecorded deviation. It found plan §1.0 weakened no §§4-12 obligation, and §16 stale only in the **underclaiming** direction. Wrote `iteration-002/re-review.yaml`.
- **2026-08-19**: P3-WP10 iteration-002 completed after the user's stop. §3 baseline re-verified from the repository at session resume: parent `0a193827f4`, gitlink `71a89c70` equal to nested HEAD, upstream 0/0, no FreeCAD/pixi processes, 9875/9876 free. The interrupted implementer proved to have finished all five fixes — including **GRK-P3-079**, which the stop-time tracker could only record as unconfirmed — so the completion pass wrote no code and produced only the missing §7 packet. Orchestrator decision recorded: **finish, not revert**, because the work was materially complete and reverting would discard correct work; the §7 packet was delegated rather than written by the Main Agent, since §1.1 forbids the orchestrator absorbing the implementation role. Wrote `iteration-002/{fix-notes.md,offline-gate.yaml}`.
- **2026-08-18**: **Session stopped by user, mid-iteration-002.** The implementer subagent was terminated before it produced its §7 resolution packet, so `iteration-002/fix-notes.md` does not exist and no re-review has run. The working tree therefore holds **partial, unreviewed** work: `tests/gui/part3/{evidence,scenarios,stress_coordinator,conftest,test_part3_architecture,test_part3_stress_coordinator_launcher}.py`, the intent-to-added `test_part3_stage_acceptance.py`, and a 29-line addition to the ADR `doc/part3-gui-collaboration-stress-design.md` (permitted by GRK-P3-076 option (b), which allows recording the accepted deviation in the ADR or plan). Diff fingerprint at stop: `c87cfc1c8cbefe3175a5f7dcc6e2f47ae97adb532f98ebdd876b402ffeb3af15`. Orchestrator spot-checks on the partial tree - `compileall` exit 0; architecture+launcher+control-channel **32 passed**; stage-acceptance offline subset **14 passed, 2 deselected**; `--stage a --preflight-only` emits **0** `PART3_RESULT` lines (GRK-P3-077 appears fixed). **These are not a verdict.** They were run by the orchestrator on unreviewed code and confirm only that the tree is self-consistent, not that the findings are resolved; §1.1 forbids the orchestrator approving implementation work. GRK-P3-079 (post-shutdown artifact scan) is **unconfirmed**. Disclosure from the stopped agent: one of its mutation tests routed through the pre-fix `--preflight-only` path and **launched FreeCAD for ~79s**; afterwards the orchestrator verified no FreeCAD/pixi processes remain and 9875/9876 are free. Nothing committed, nothing staged for commit, nothing pushed, no amend, nested gitlink untouched at `71a89c70`. **Stage A/B never executed. Part 3 remains NO-GO.**
- **2026-08-18**: P3-WP10 Main Agent triage (iteration-002) Done. GRK-P3-076..081 all **ACCEPTED**; no duplicate, dispute, or out-of-scope blocker; none softened. Each was re-read in source before acceptance: `holds_rpc_session` is a literal `return False` at `stress_coordinator.py:329` that the launcher test asserts at `:183` (an assertion that cannot fail); `main()` at `:2096-2105` resolves a stage under `--preflight-only` and then **discards it**, so `--stage a --preflight-only` could print `PART3_RESULT: PASSED` with exit 0 after zero cycles; three evidence fields are literals at `:1606`/`:1875`/`:380`; the artifact scan at `:2005` precedes `shutdown_launcher()` at `:2020`; the allowlist AST gate is fenced to `remote_agent_driver.py` at `test_part3_architecture.py:115`. Routed 076/077/078/079/081 to the implementer unchanged; took **080** as an orchestrator evidence-binding action and closed it with the proof the finding demanded. Accepted non-blocking notes N1-N3 and N5 (a pytest **skip is NOT_RUN, never a pass**). Wrote `iteration-002/triage.yaml`. No commit.
- **2026-08-18**: P3-WP10 reviewer subagent re-review (iteration-001) Done. Verdict **PROBLEMS** against reproduced diff fingerprint `13544bb0...f53366` plus the untracked acceptance module at sha256 `0eb66751...d416b`. Prior findings: GRK-P3-074 **PASS** (Stage C unreachable, proven offline); 070/071/072/073/075 **PASS_SOURCE** - correct in source, and explicitly **not credited** as passes because Stage A/B are NOT_RUN. Six new blocking findings **GRK-P3-076..081**. The reviewer also ruled on all four implementer disclosures instead of letting them pass by silence: it confirmed the Stage path departs from ADR §1/§1.1 by holding token and typed session in one process, declined to make the persistent-child refactor blocking for four recorded reasons (WP09 precedent, server-side enforcement of the real invariant, the §2.2 replay proof a per-call child would destroy, and the standing ban on opening `remote_agent_driver.py` without a proven missing verb), and made the **honesty** half blocking instead - the harness may deviate, but may not certify a boundary it does not maintain. Wrote `iteration-001/re-review.yaml`. No file edited by the reviewer.
- **2026-08-18**: P3-WP10 implementer subagent iteration-001 Done, and Main Agent §8.4 offline validation **PASSED** - every reported number independently reproduced by the orchestrator rather than copied: `compileall` exit 0; architecture+launcher+control-channel **31 passed**; stage-acceptance offline subset **9 passed, 2 deselected**; `--stage c` exit 2 with **0** `PART3_RESULT` lines; `git diff --check` exit 0. Scope verified: nested repo untouched and gitlink unbumped, Stage counts unchanged (A 10/5, B 50/20, C 500/100), no existing AST ban weakened (only a docstring and an import line deleted), index line endings still `i/lf`. Live gates recorded **NOT_RUN** and not credited. The implementer disclosed four limitations of its own accord, including that its full-program smoke test ran against fakes it wrote itself and therefore proves nothing about FreeCAD. Wrote `iteration-001/{fix-notes.md,offline-gate.yaml}`. No commit.
- **2026-08-18**: P3-WP10 implementer subagent (iteration-001) launched under the §7 contract. Routed GRK-P3-070..075 unchanged, together with the reviewer's `composer_implement_order` as ordering and `composer_must_not` as prohibitions. Scope: parent `tests/gui/part3/` (evidence/scenarios/coordinator/new stage-acceptance test; architecture+launcher only where honesty requires). Explicitly forbidden: launching FreeCAD, running `--stage a|b`, executing Stage C, changing Stage A/B counts, pause-checkbox fallback, weakening secret checks, nested gitlink bump without proof, any commit. Must report gates actually run vs `NOT_RUN`. Stage execution stays with the Main Agent (§8) after an independent reviewer subagent re-reviews the source.
- **2026-08-18**: P3-WP10 Main Agent triage (§6) Done. GRK-P3-070..075 all **ACCEPTED** — no `DUPLICATE_OF`, no `DISPUTED_WITH_EVIDENCE`, no `OUT_OF_SCOPE_BLOCKER`. Each finding re-verified against the working tree rather than accepted on the reviewer's assertion: `stress_coordinator.main()` returns 2 for `--stage` without `--preflight-only` and has no `run_stage`; `scenarios.py` is counts-only with no cycle program; `evidence.py` exports no binary-fingerprint, git-SHA, cycle/save recorder or artifact-scan helper; `tests/gui/part3/test_part3_stage_acceptance.py` absent; `resolve_stage('C')` returns STAGE_C(500,100) with no execute refusal; `graceful_shutdown_owned_session` correct from WP09 but consumed by no stage path. Wrote `iteration-001/triage.yaml`. No product edit by the orchestrator. No commit.
- **2026-08-18**: Role-execution amendment adopted by user decision and written into the plan as new **§1.0**. From P3-WP10 onward the Review role is an orchestrator-spawned **reviewer subagent** and the Implementation role is an orchestrator-spawned **implementer subagent**; Grok 4.5 High and Composer 2.5 executed WP00..WP09 and those records stand unrewritten. This changes the *executor*, not the *contract*: §§4–12 bind unchanged, the reviewer stays read-only and is always a separate spawn from the implementer, no spawn reviews a diff it authored, the Main Agent still runs every §8 gate and owns §11/§12, and reduced-reasoning agents remain forbidden. Retitled the plan and retargeted §§2, 4.1, 5–12, 14, 15 from vendor names to role names; §1.1 now bans self-review and orchestrator role-absorption instead of vendor swaps. `git diff --check` exit 0. No commit.
- **2026-08-18**: Orchestrator session resumed. §16 tracker re-verified against the live repository, not re-derived from its own text: parent HEAD `0a193827f411ab8939b5fd9a1e70f19a9f5fd8f4` on `fix/change-aware-save-mcp-autonomy`, upstream 0/0; parent gitlink `71a89c703e9fa17a753b2b553b1a1513f4e23114` equals nested HEAD; `results/part3-orchestration/2026-08-18/P3-WP10/{baseline.json,work-package.yaml,review.yaml,iteration-001/review.yaml}` present; `review.yaml` verdict **PROBLEMS** with exactly GRK-P3-070..075. No triage packet on disk yet — the recorded next step is still the true next step. No product edit, no Grok launch, no Composer launch, no test run, no commit, no push. Next: Main Agent triage of GRK-P3-070..075 → Composer 2.5.
- **2026-08-18**: Grok WP10 review Done. Verdict **PROBLEMS**. Findings GRK-P3-070..075: coordinator `--stage` still WP06 stub exit 2; missing ADR §13 interleaved cycle program (10/5, 50/20); missing evidence fingerprints/git/artifact scan population; missing `test_part3_stage_acceptance.py`; Stage C must not execute; stage green path must end WP09 graceful `forced:false`. Local/remote verbs appear present — nested bump not required. WP07/WP06 are not Stage A/B substitutes. Wrote `P3-WP10/review.yaml`. Next: Composer 2.5 after Main Agent triage. Do not run Stage A/B yet. No commit.
- **2026-08-18**: P3-WP10 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-18/P3-WP10/{work-package.yaml,baseline.json}`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `0a193827f4` and nested `71a89c70` reproduced; gitlink SHA consistent. No leftover FreeCAD/pixi; 9875/9876 free. Cursor MCP / AutoCurtains MCP left alone. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP09 hash-fill plus this baseline), nested autocrlf stat dirt, leftover `lock-anchor-gate/`. Stage A/B loops not implemented (`stress_coordinator.py --stage` still WP06 stub exit 2). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Do not amend WP09. Next: Grok 4.5 High read-only on P3-WP10.
- **2026-08-18**: P3-WP09 final gate **PASS**. Parent commit `0a193827f4` (`0a193827f411ab8939b5fd9a1e70f19a9f5fd8f4`) stages `tests/gui/part3/*` shutdown harness (`test_part3_shutdown.py`, coordinator/conftest/evidence/driver, architecture+launcher tests) and this tracker as of commit. Nested not committed. Gitlink held at `71a89c70`. `git diff --check` exit 0. Pushed to `origin/fix/change-aware-save-mcp-autonomy`. Composer idle. Next: P3-WP10 not started (do not start WP10 product). §16 hash-fill after push is expected dirty (no amend).
- **2026-08-18**: Grok WP09 re-review (iteration-006) Done. Verdict **NO_ISSUES**. GRK-P3-061..069 PASS. Cross-checked logs/junit vs `gate.yaml`: Windows unit 2/0, arch 9/9, graceful 2/2 (20.5s/17.3s), stall 70.53s; Linux graceful 2/2 (4.08s/2.16s plausible vs iter-005 MCP-ready ~2s), stall 63.64s. GRK-P3-069 harness `0o600` confirmed; `profile_secret` owner-only not weakened. Forced kill not claimed as green teardown. Wrote `iteration-006/re-review.yaml`. Commit allowed: parent `tests/gui/part3/*` + plan §16 only. Next: Main Agent commit+push.
- **2026-08-18**: Grok WP09 Linux re-review (iteration-005) Done. Verdict **PROBLEMS**. GRK-P3-068 **PASS** (checkbox present+wired; DocumentChangesWidget rebuilt). Graceful[0]/[1] FAIL at `authenticate_json_rpc` → `INSECURE_PROFILE_SECRET` (not invented; shutdown not reached). Stall PASS ~63.5s (forced-kill path; does not greenwash graceful). New **GRK-P3-069**: `ensure_disposable_profile_auth_secret` uses `write_bytes` without `0o600` (Windows skips POSIX mode check). Classification: in-scope WP09 harness, not env uid-only, not product wrongly rejecting a locked secret. Do not weaken owner-only checks. Wrote `iteration-005/re-review-linux.yaml`. Next: Composer 2.5 for GRK-P3-069; then Linux graceful re-run. No commit.
- **2026-08-18**: Grok WP09 Linux re-review (iteration-004) Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. MCP ready (ObjectsFem cleared). graceful[0] FAIL pauseAgentWrites checkbox missing; [1]+stall SKIPPED occupied 9875 (not green). New **GRK-P3-068** env: rebuild FreeCADGui from current parent (DocumentChangesWidget). No pause-checkbox fallback. Windows 067 hold. Wrote iteration-004/re-review-linux.yaml. Composer idle. No Windows-only commit. Next: Main Agent FreeCADGui rebuild then Linux shutdown re-run.
- **2026-08-18**: Grok WP09 Linux evidence re-review Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. Linux suite **ran** 3/3 FAIL (TimeoutError: MCP RPC did not become ready); diag: MCP Auto-start failed No module named ObjectsFem / PartDesignGui on volume ELF GUI — env defect, not WP09 shutdown product. Windows GRK-P3-067 hold. Wrote iteration-003/re-review-linux.yaml. Composer idle. **No Windows-only commit.** Next: Main Agent rebuild Linux GUI (Fem+PartDesign) then re-run Linux shutdown.
- **2026-08-18**: Grok WP09 re-review (iteration-003) Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. GRK-P3-067 **PASS** (--wait + stall ~70.5s forced:true; graceful 2/2; architecture 9/9 honest). 061–066 held. Linux shutdown still **NOT_RUN** — plan §13 requires Windows **and** Linux; do **not** commit Windows-only. Wrote iteration-003/re-review.yaml. Composer idle. Next: Main Agent Linux recheck.
- **2026-08-18**: P3-WP09 Composer 2.5 iteration-003 Done. GRK-P3-067: added `start_freecad.py --wait` to owned-session launch argv in `test_part3_shutdown.py`, `stress_coordinator.build_launch_command`, and `conftest.launch_freecad_gui_session`; `_force_kill_owned_process_tree` waits after taskkill for `process_exit_utc`. Architecture **9/9 PASS** (`iteration-003/architecture-gate.log`). Windows graceful **2/2 PASS** `forced:false` (`shutdown-graceful-windows.log`, 34.25s). Stall **1/1 PASS** pytest with session `success=false` / `forced=true` / ~70.5s (`shutdown-stall-windows.log`). Linux **NOT_RUN**. `git diff --check` exit 0. Wrote `iteration-003/{fix-notes.md,final-gate.yaml}`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-18**: P3-WP09 Composer 2.5 iteration-002 Done. GRK-P3-066: corrected iteration-001 `final-gate.yaml`/fix-notes to honest **7 passed / 2 failed** vs `architecture-gate.log` (not 9/9). GRK-P3-065: fixed `test_package_has_no_actor_sync_sleeps` (conftest readiness allowlist) and `test_shutdown_success_path_uses_graceful_sequence` (AST guard); conftest autouse skips unit modules so architecture gate does not require 9875. Fresh architecture gate **9 passed, 0 failed** (`iteration-002/architecture-gate.log`). Stall/Linux **NOT_RUN** (Main Agent). `git diff --check` exit 0. Wrote `iteration-002/{fix-notes.md,final-gate.yaml}`. No commit. Next: Main Agent stall+Linux, then Grok re-review.
- **2026-08-18**: Grok WP09 re-review Done. Verdict **PROBLEMS**. GRK-P3-061..063 PASS_SOURCE; GRK-P3-064 FAIL (stall unproven; Linux NOT_RUN); GRK-P3-065 FAIL; new GRK-P3-066 (iteration-001 architecture yaml-only green vs log 2 fail/7 pass). Wrote `iteration-001/re-review.yaml`. Next: Composer 2.5 iteration-002.
- **2026-08-18**: P3-WP09 Composer 2.5 iteration-001 Done. GRK-P3-061: `graceful_shutdown_owned_session` ADR §9 sequence + 60s wait; force-kill only after stall with `forced:true`. GRK-P3-062: §8 shutdown timestamp keys + stamping. GRK-P3-063: `close_main_window` Qt owner-thread action. GRK-P3-064: `test_part3_shutdown.py` repeated native gate. GRK-P3-065: conftest graceful teardown + architecture taskkill guard. Windows shutdown gate **2 passed** graceful (`shutdown-windows.log`); stall re-run **BLOCKED** (orphan MCP 9875). Linux **NOT_RUN**. Architecture log **7 passed, 2 failed** (yaml falsely claimed 9/9 — corrected iteration-002). Wrote `iteration-001/{fix-notes.md,final-gate.yaml}`. No commit.
- **2026-08-18**: P3-WP09 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-18/P3-WP09/{work-package.yaml,baseline.json}`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `9766a4a16f` and nested `71a89c70` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP08 hash-fill plus this baseline), nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion), and leftover WP08 `lock-anchor-gate/` (R26/R27). No leftover FreeCAD/pixi; 9875/9876 free. No product/MCP/GUI implementation. No Grok launch. No commit. No push. Do not amend WP08. Next: Grok 4.5 High read-only on P3-WP09.
- **2026-08-17**: P3-WP08 final gate **PASS**. Parent commit `9766a4a16f` (`9766a4a16fb458c15ad125b751c61c9485565f6d`) stages `DocumentFileWriter.h` (`APP_DOCUMENTFILEWRITER_EXPORT DocumentFileLock`), probe `FREECAD_DOCUMENTFILEWRITER_TEST_API` in `tests/filesystem/CMakeLists.txt`, and this tracker. Nested not committed. Gitlink held at `71a89c70`. `git diff --check` exit 0. Composer idle. Next: STOP (do not start WP09).
- **2026-08-17**: Grok WP08 Linux evidence re-review Done. Verdict **NO_ISSUES**. GRK-P3-058/059/060 PASS. `linux-lock-anchor-gate.log` honesty: ELF probe, **24 ok / 0 FAIL**, `LOCK_ANCHOR_GATE_RESULT: PASSED (24 checks)`, `GATE_EXIT=0` (not yaml-only). Prior Linux NOT_RUN blocker cleared. Wrote `iteration-001/re-review-linux.yaml`. No Composer relaunch. No commit. Next: Main Agent docs/`git diff --check`/commit+push. Do not start WP09.
- **2026-08-17**: P3-WP08 Linux lock-anchor gate **PASS** 24/24 (`iteration-001/linux-lock-anchor-gate.log`); session wrapped; no commit.
- **2026-08-17**: Grok WP08 re-review (iteration-001) Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. GRK-P3-058 PASS; 059 PASS_SOURCE; 060 Windows log honesty **24 ok / 0 FAIL** / `LOCK_ANCHOR_GATE_RESULT: PASSED (24 checks)` verified (not yaml-only). Linux recheck still NOT_RUN after shared CMake change — plan §13 / ADR §12 require it. No Composer relaunch. No commit. Wrote `iteration-001/re-review.yaml`. Next: Main Agent Linux lock-anchor recheck.
- **2026-08-17**: P3-WP08 Composer 2.5 iteration-001 Done. GRK-P3-058/059: `APP_DOCUMENTFILEWRITER_EXPORT DocumentFileLock` + probe `FREECAD_DOCUMENTFILEWRITER_TEST_API`. Rebuilt `FreeCADApp`/`save_lock_probe` (vcvars64 + pixi). Windows lock-anchor gate **24/24 PASS** (`iteration-001/save-lock-anchor-gate.log`). Linux recheck NOT_RUN (CMake shared infra changed). ADR verbatim command without `.exe` exit 2 (driver PATHEXT). No FileLock/probe/gate edits. Wrote `iteration-001/{fix-notes.md,final-gate.yaml,native-build.cmd}`. `git diff --check` exit 0 on edited parent files. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: Grok WP08 review Done. Verdict **PROBLEMS**. Findings GRK-P3-058..060 in `results/part3-orchestration/2026-08-17/P3-WP08/iteration-001/{review.yaml,review.md,review.json}`. Missing test-scoped `DocumentFileLock` export + probe `FREECAD_DOCUMENTFILEWRITER_TEST_API`; `save_lock_probe.exe` absent; Windows 24-check gate NOT RUN. Probe/gate/FileLock R26 path already sound at source. No product edit by reviewer. No Composer launch. No commit. Next = Composer 2.5 after Main Agent triage.
- **2026-08-17**: P3-WP08 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP08/{work-package.yaml,baseline.json}`. First inspect found 33 orphaned `pixi run -- build/release/bin/FreeCAD.exe` GUIs (pytest gone); stopped with `taskkill /T /F`. Recapture verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `7a4ff54f6c` and nested `71a89c70` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP07 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No leftover FreeCAD/pixi; 9875/9876 free. No product/MCP/GUI implementation. No Grok launch. No commit. No push. Do not amend WP07. Next: Grok 4.5 High read-only on P3-WP08.
- **2026-08-17**: P3-WP07 final gate **PASS**. Parent commit `7a4ff54f6c` (`7a4ff54f6cc70162ae188ac8abf59c308df974c7`) stages `test_part3_integration.py`, Alpha/Beta PE fixture helpers in `actions.py`/`conftest.py`, and this tracker. Nested not committed (no WP07 MCP files; autocrlf-only dirt excluded). Gitlink held at `71a89c70`. `git diff --check` exit 0. Composer idle. Next: P3-WP08 baseline (do not start WP08 product).
- **2026-08-17**: Grok WP07 re-review (iteration-001) Done. Verdict **NO_ISSUES**. GRK-P3-054/057 PASS; 055/056 PASS (focused GUI / FreeCADCmd e2e NOT_RUN as gate). `integration-gui.log` verbatim **7 passed, 0 failed**; junit tests=7 failures=0. Wrote `iteration-001/re-review.yaml`. No product edit by reviewer. No Composer relaunch. No commit. Next: Main Agent docs/`git diff --check`/commit+push.
- **2026-08-17**: P3-WP07 Composer 2.5 iteration-001 Done. GRK-P3-054/057: added `tests/gui/part3/test_part3_integration.py` (seven ADR §6–§7 cases), `provision_alpha_beta_fixture` (`StressBox.AlphaValue`/`SecondBox.BetaValue`), function-scoped `launch_freecad_gui_session` per test. Gate **7 passed, 0 failed** (`iteration-001/integration-gui.log`, 300.77s). GRK-P3-055/056 not claimed as gate (focused GUI / FreeCADCmd e2e NOT_RUN). No Stage A/B/C. No facade_bindings/automation_pause/dispatch_core/HamaAdapter edits. Wrote `iteration-001/{fix-notes.md,final-gate.yaml}`. `git diff --check` exit 0 on product Python. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP07 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP07/{work-package.yaml,baseline.json}`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `0d00b9200f` and nested `71a89c70` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP06 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No leftover FreeCAD/pixi; 9875/9876 free. No product/MCP/GUI implementation. No Grok launch. No commit. No push. Do not amend WP06. Next: Grok 4.5 High read-only on P3-WP07.
- **2026-08-17**: P3-WP06 final gate **PASS**. Parent commit `0d00b9200f` (`0d00b9200f6d809b963a277dbf3e5715bc2eef35`) stages `stress_coordinator.py`/`scenarios.py`/`evidence.py`, AST + launcher tests, typed `remote_agent_driver.py`, delete of `collaboration_gui_stress.py`, and this tracker. Nested not committed (no WP06 MCP files; autocrlf-only dirt excluded). Gitlink held at `71a89c70`. `git diff --check` exit 0. Composer idle. Next: P3-WP07 baseline (do not start WP07 product).
- **2026-08-17**: Grok WP06 re-review (iteration-001) Done. Verdict **NO_ISSUES**. GRK-P3-048..053 PASS. `wp06-gate-pytest.log` verbatim **19 passed, 0 failed**. Harness absent on disk (` D` unstaged). Stage A/B NOT_RUN. facade_bindings unchanged. Wrote `iteration-001/re-review.yaml`. No product edit by reviewer. No Composer relaunch. No commit. Next: Main Agent docs/`git diff --check`/commit+push.
- **2026-08-17**: P3-WP06 Composer 2.5 iteration-001 Done. GRK-P3-048..053: added `stress_coordinator.py`/`scenarios.py`/`evidence.py`, `test_part3_architecture.py`, `test_part3_stress_coordinator_launcher.py`; typed `remote_agent_driver.py` allowlist; deleted `collaboration_gui_stress.py`. Gate **19 passed, 0 failed** (`iteration-001/wp06-gate-pytest.log`). Zero `time.sleep` in new modules; readiness allowlist only elsewhere. No Stage A/B/C. No facade_bindings/HamaAdapter edits. Wrote `iteration-001/{fix-notes.md,final-gate.yaml}`. `git diff --check` exit 0 on product files. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: Grok WP06 review Done. Verdict **PROBLEMS**. Findings GRK-P3-048..053 in `results/part3-orchestration/2026-08-17/P3-WP06/iteration-001/{review.yaml,review.md,review.json}`. Absent `stress_coordinator`/`scenarios`/`evidence`/`test_part3_architecture`; `remote_agent_driver` still generic `call`; retired `collaboration_gui_stress.py` present; launcher/isolation + sleep AST honesty required. Parent-only. No product edit by reviewer. No Composer launch. No commit. Next = Composer 2.5 after Main Agent triage.
- **2026-08-17**: P3-WP06 §3 baseline unblocked. Stopped leftover test GUIs: FreeCAD PIDs 31832/58648/26520 (`taskkill /T /F`; matched this repo `build/release/bin/FreeCAD.exe`); pixi 31416/41716/59160 already gone after children. Recapture: no FreeCAD/FreeCADCmd; 9875/9876 free; parent `d5f6972f18` and nested/gitlink `71a89c70` unchanged 0/0. Updated `results/part3-orchestration/2026-08-17/P3-WP06/{work-package.yaml,baseline.json}` to **VALID**. `git diff --check` exit 0. No product implementation. No Grok launch. No commit. Do not amend WP05. Next: Grok 4.5 High read-only on P3-WP06.
- **2026-08-17**: P3-WP06 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP06/work-package.yaml` and `baseline.json`. Verdict **BLOCKED** (do not launch Grok 4.5 High). Parent `d5f6972f18` and nested `71a89c70` reproduced; gitlink SHA consistent. Three orphaned `pixi run -- build/release/bin/FreeCAD.exe` GUIs (PIDs 31832/58648/26520; pixi 31416/41716/59160; grandparents dead). Ports 9875/9876 free. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP05 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Do not amend WP05. Next: Main Agent stop leftover FreeCAD/pixi, re-verify no FreeCAD.exe, recapture WP06 baseline.
- **2026-08-17**: P3-WP05 final gate **PASS**. Parent commit `d5f6972f18` (`d5f6972f18df7068fe84394a7ea3346d827530fa`) stages PropertyItem/PropertyEditor CCM path, Document `executeCompatibilityMutation`, CollaborationCompatibilityAdapter `propertyName`, DocumentCollaborationService, `local_property_edit`/`Std_Save` tests, and this tracker. Nested not committed (no WP05 MCP files; autocrlf-only dirt excluded). Gitlink held at `71a89c70`. `git diff --check` exit 0. Composer idle. Next: P3-WP06 baseline (do not start WP06 product).
- **2026-08-17**: P3-WP05 Grok 4.5 High re-review (iteration-002) Done. Verdict **NO_ISSUES**. GRK-P3-046 PASS (`iteration-002/focused-gui.log` verbatim **2 passed, 0 failed**). GRK-P3-047 PASS (iteration-001 false-green corrected; iteration-002 authoritative). GRK-P3-042–045 still PASS. ObjectModel+1 / ObjectProperty+1 asserts not weakened. Wrote `iteration-002/re-review.yaml`. No product edit by reviewer. No Composer launch. No commit. Next: Main Agent docs/`git diff --check`/commit+push.
- **2026-08-17**: P3-WP05 Composer 2.5 iteration-002 Done. GRK-P3-046: focused GUI re-run **2 passed, 0 failed** (`iteration-002/focused-gui.log`, pixi + branch `build/release/bin/FreeCAD.exe`). GRK-P3-047: corrected iteration-001 false-green gate; authoritative log is iteration-002. Test fix: restored `test_remote_rpc_cannot_resume_local_pause`; soft dirty (`modified or touched`). No ObjectProperty C++ fix. Wrote `iteration-002/{fix-notes.md,final-gate.yaml}`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP05 Grok 4.5 High re-review (iteration-001) Done. Verdict **PROBLEMS**. GRK-P3-042/044 **PASS_SOURCE** (PE→CCM + `propertyName`; `local_property_edit` via PE `setData`). GRK-P3-043/045 **PASS**. GRK-P3-046 **FAIL**: `focused-gui-final.log` is **1 failed, 1 passed** (not 2/2). New **GRK-P3-047**: final-gate/fix-notes/§16 claimed PASS contradicting cited log; soft dirty assert present in source but no honest post-fix re-run. Wrote `iteration-001/re-review.yaml`. No product edit by reviewer. No Composer launch. No commit. Next = Composer 2.5 iteration-002 after Main Agent triage.
- **2026-08-17**: P3-WP05 Composer 2.5 iteration-001 Done. GRK-P3-042: `PropertyItem::setPropertyValue` → `Gui::Document::executeCompatibilityMutation` / `DocumentCommitCoordinator`; optional `propertyName` on compatibility mutation publishes `ObjectProperty` + `ObjectModel`; removed PE `openEditor` undo transaction (blocked CCM). GRK-P3-044/045: `local_property_edit` (PE `setData` path), `local_save` (`Std_Save`). Claimed GRK-P3-046 focused GUI **2/2 PASS** (`focused-gui-final.log`) — later Grok re-review found log honesty FAIL. GRK-P3-043: Sketcher/task-panel not unified — out of WP05 acceptance (fix-notes). `FreeCADGui` vcvars rebuild PASS. `git diff --check` exit 0 on WP05 files. Wrote `iteration-001/{fix-notes.md,final-gate.yaml}`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP05 Grok 4.5 High review Done. Verdict **PROBLEMS**. Findings GRK-P3-042..046 in `results/part3-orchestration/2026-08-17/P3-WP05/iteration-001/{review.yaml,review.md,review.json}`. Proof: Property Editor / Sketcher / task-panel do **not** share `commitCompatibilityMutation` / DocumentCommitCoordinator accounting (PE uses `runCommand` assignment + legacy transactions; revisions via `hasSetValue` ObjectProperty with ObjectModel co-publish). Unify PE product path first; then `local_property_edit` + local `Std_Save` + focused GUI gates. No product edit by reviewer. No Composer launch. No commit. No push. Next = Composer 2.5 after Main Agent triage.
- **2026-08-17**: P3-WP05 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP05/work-package.yaml` and `baseline.json`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `9be18d09` and nested `71a89c70` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP04 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Next: Grok 4.5 High read-only on P3-WP05.
- **2026-08-17**: P3-WP04 final gate **PASS**. Parent commit `9be18d09` (`9be18d0946d20c3ae018a6b01642acf86c87d157`) stages LocalUserDriver package, CollaborationViewIsolation `getFileChangeState`/`operator==`, launcher PYTHONPATH/`pixi run --`, and this tracker. Nested not committed (autocrlf-only). Gitlink held at `71a89c70`. `git diff --check` exit 0. Composer idle. Next: P3-WP05 baseline.
- **2026-08-17**: P3-WP04 Grok 4.5 High re-review (iteration-002) Done. Verdict **NO_ISSUES**. GRK-P3-041 PASS (`toUnderlyingType()`; Gui_tests_run 3/3 honest). Corollary relative `expectPersonalViewFileStateUnchanged` OK for GRK-P3-039 (not watered down; absolute Clean remains on saved-doc GUI). GRK-P3-035..040 + focused GUI 7/7 hold. Wrote `iteration-002/re-review.yaml`. Composer idle. Next: Main Agent docs/`git diff --check`/commit+push.
- **2026-08-17**: P3-WP04 Composer 2.5 iteration-002 Done. GRK-P3-041: `FileChangeSnapshot::operator==` uses `pendingChanges.toUnderlyingType()` (not Flags `==`). vcvars64 + pixi `Gui_tests_run` rebuild PASS. CollaborationViewIsolation* gtest 3/3 PASS (`iteration-002/native-gtest-collaboration-view-isolation.log` + junit). First run after operator fix alone: 3 FAIL on absolute Clean asserts — corollary trim to relative `EXPECT_EQ(after,before)` only. Wrote `iteration-002/{fix-notes.md,final-gate.yaml}`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP04 Grok 4.5 High native evidence re-review Done. Verdict **PROBLEMS**. Focused GUI honesty **PASS** (log/junit 7/7). New **GRK-P3-041**: `FileChangeSnapshot::operator==` uses `DocumentFileChanges ==` (MSVC C2678); `Gui_tests_run` not linked; GRK-P3-039 incomplete. Composer scope: test-only `toUnderlyingType()` equality + rebuild/run gtest. Wrote `iteration-001/re-review-native.yaml`. No commit by reviewer. Next: Composer 2.5 for GRK-P3-041.
- **2026-08-17**: P3-WP04 Main Agent native GUI + gtest Done. Harness/env only (disposable-profile `addon` import path, auth secret, handshake wrap/identity, conftest process-tree teardown, focused-test identity/save/pan/checked-edit). Focused GUI five **PASS** (`iteration-001/focused-gui.log`, junit 7 passed). `Gui_tests_run` vcvars rebuild **FAIL** (`CollaborationViewIsolation.cpp:151` `DocumentFileChanges` `operator==`; tests not run). Wrote `iteration-001/{native-gate.yaml,native-gtest-run.log}`. No product C++/MCP redesign. No commit. Composer idle. Next: Grok 4.5 High evidence re-review.
- **2026-08-17**: P3-WP04 Grok 4.5 High re-review (iteration-001) Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. GRK-P3-035..040 PASS at source; unit/token/facade PASS; focused GUI 5 + CollaborationViewIsolation gtest still BLOCKED (MCP `addon` path; vcvars). Confirmed: no facade pause/resume RPC; `collaboration_gui_stress.py` untouched; no `automation_pause` fallback in actions. Wrote `iteration-001/re-review.yaml`. Composer idle. Next: Main Agent MCP path + vcvars gates — not commit yet.
- **2026-08-17**: P3-WP04 Composer 2.5 iteration-001 Done. Implemented GRK-P3-035..040: `tests/gui/part3/local_driver/` + runtime `Mod/Part3LocalDriver`, authenticated control channel, `local_user_driver.py` / `remote_agent_driver.py`, Qt owner-thread actions with checkbox-only pause, `CollaborationViewIsolation` `getFileChangeState` assertions, focused GUI tests. Gates: control-channel unit 5/5 PASS; remote token absence + facade resume-denial PASS; focused GUI 5 skipped (MCP autostart `No module named 'addon'` in disposable profile); `Gui_tests_run` rebuild BLOCKED without vcvars. Wrote `iteration-001/{fix-notes.md,final-gate.yaml}`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP04 Grok 4.5 High review Done. Verdict **PROBLEMS**. Findings GRK-P3-035..040 in `results/part3-orchestration/2026-08-17/P3-WP04/iteration-001/{review.yaml,review.md,review.json}`. Absent LocalUserDriver surface, control channel, coordinator clients, checkbox-only pause path, `getFileChangeState` extension, and focused GUI/remote-resume gates. Substrate notes: `pauseAgentWrites` + MCP wiring exist; facade still omits pause/resume RPC; retired harness present (do not delete in WP04). No product edit. No Composer launch. No commit. No push. Next = Composer 2.5 after Main Agent triage.
- **2026-08-17**: P3-WP04 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP04/work-package.yaml` and `baseline.json`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `2267c237` and nested `71a89c70` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP03 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Next: Grok 4.5 High read-only on P3-WP04.
- **2026-08-17**: P3-WP03 final gate **PASS**. Nested commit `71a89c70` (`71a89c703e9fa17a753b2b553b1a1513f4e23114`) pushed to `origin/fix/change-aware-save-mcp-autonomy` (37 files; autocrlf test dirt and `results_luna_*` excluded). Parent commit `2267c237` (`2267c237e0d2b842aec64ff4b3ab4182800e0ba3`) stages gitlink `71a89c70` and this tracker. `git diff --check` exit 0. Composer idle. Next: P3-WP04 baseline.
- **2026-08-17**: P3-WP03 Grok 4.5 High re-review (iteration-002) Done. Verdict **NO_ISSUES**. GRK-P3-033 PASS (eager repair → `RECOMPUTE_DEFERRED`); GRK-P3-034 PASS (`create_document`/`close_document` declare none). GRK-P3-028..032 still PASS. Full unit 2529/2: ARCH104/107 and live-fixture `_execution_collaborators` classified pre-existing — **do not block** WP03 sign-off. Docker Save Copy e2e NOT RUN / not required. Wrote `iteration-002/re-review.yaml`. Composer idle. Next: Main Agent docs/`git diff --check`/commit+push. No commit by reviewer.
- **2026-08-17**: P3-WP03 Composer iteration-002 Done. GRK-P3-033: replaced stale eager repair test with `RECOMPUTE_DEFERRED` proof; updated phase15 transaction-control and mutation_readiness undo pause tests for Part 3 signatures (test-only). GRK-P3-034: `create_document`/`close_document` declare `none` in `recompute_policy.py` with unit proof. Full unit 2529 passed / 2 failed (ARCH104/ARCH107 architecture lint; live fixture `_execution_collaborators` stale — pre-existing, not WP03 product). Wrote `iteration-002/{fix-notes.md,final-gate.yaml}`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP03 Grok 4.5 High re-review Done. Verdict **PROBLEMS**. GRK-P3-028..032 **PASS** (Save Copy, recompute_policy assert, repair_references pin, MCP/contracts, focused tests). New **GRK-P3-033**: stale `test_eager_reference_repair_builds_result_after_one_native_recompute` still expects retired `recompute=True`. New **GRK-P3-034**: `create_document`/`close_document` default TARGET vs ADR none. `test_undo_uses_the_same_pause_admission_gate` = WP02 leftover (not WP03 product regression). Docker Save Copy e2e NOT RUN and not required (unit-sufficient). Wrote `iteration-001/re-review.yaml`. Next: Composer 2.5 iteration-002. No commit by reviewer.
- **2026-08-17**: P3-WP03 Grok 4.5 High review Done. Verdict **PROBLEMS**. Findings GRK-P3-028..032: typed `save_document_copy` absent; `recompute_policy.py` / `run_cad_mutation` registry assert absent; `repair_references` dual `native_recompute` conflicts with fixed none|target declaration; MCP/generated contracts omit Save Copy; gate tests absent. Parent `saveCopyWithOutcome` bindable — no parent C++ blocker. Snapshot copy callers contrast only. Public `execute_code` default already `none`. Wrote `results/part3-orchestration/2026-08-17/P3-WP03/iteration-001/{review.yaml,review.md,review.json}`. No product edit by reviewer. No commit. No push. Next: Main Agent triage → Composer 2.5.
- **2026-08-17**: P3-WP03 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP03/work-package.yaml` and `baseline.json`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `95567095` and nested `d639dd47` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP02 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Next: Grok 4.5 High read-only on P3-WP03.
- **2026-08-17**: P3-WP02 final gate **PASS**. Nested commit `d639dd47` (`d639dd47a03654fde8f419f357535afa46ef8ce8`) pushed to `origin/fix/change-aware-save-mcp-autonomy` (15 files; autocrlf test dirt and `results_luna_*` excluded). Parent commit `95567095` (`955670950c0e411d16ea958778533be89d99c823`) stages gitlink `d639dd47` and this tracker. `git diff --check` exit 0. Composer idle. Next: P3-WP03 baseline.
- **2026-08-17**: P3-WP02 Grok 4.5 High re-review (iteration-004) Done. Verdict **NO_ISSUES**. GRK-P3-027 PASS (`_finish_history_mutation` target recompute before postflight; no dishonest failure terminal after applied undo/redo). GRK-P3-020..026 still PASS. Pixi FreeCADCmd e2e evidence honest: log 2 passed, junit failures=0, `ci_rc_e2e=0`; intervening test asserts `HISTORY_HEAD_REJECTED`. No `recompute_policy.py`. Wrote `iteration-004/re-review.yaml`. Composer idle. Next: Main Agent docs/`git diff --check`/commit+push gate. No commit by reviewer.
- **2026-08-17**: P3-WP02 Grok 4.5 High re-review (iteration-003) Done. Verdict **PROBLEMS**. GRK-P3-026 **PASS** (`undo`/`redo` in `AUTHENTICATED_METHODS`; `LEASE_PROTOCOL_REQUIRED` gone). Remaining e2e FAIL is new **GRK-P3-027**: undo/redo skip target recompute before postflight → `MUTATION_NOT_READY_AFTER_COMMIT` (`pending_recompute`); `HISTORY_HEAD_REJECTED` not reached. Lost-response still PASS. Wrote `iteration-003/re-review.yaml`. Next: Composer 2.5 iteration-004; then re-run pixi e2e. No commit by reviewer.
- **2026-08-17**: P3-WP02 Grok 4.5 High native evidence re-review Done. Verdict **PROBLEMS**. `test_commit_checked_property_lost_response_retry` PASS (GRK-P3-020/021). `test_undo_refuses_after_intervening_local_edit` FAIL: `LEASE_PROTOCOL_REQUIRED` before `HISTORY_HEAD_REJECTED`. New GRK-P3-026: `undo`/`redo` missing from `AUTHENTICATED_METHODS` so `dispatch` does not elevate the fixture handshake token. Wrote `iteration-002/re-review-native.yaml`. Next: Composer 2.5 iteration-003; then re-run pixi e2e. No commit by reviewer.
- **2026-08-17**: P3-WP02 Main Agent pixi FreeCADCmd e2e Done. Probe PASS (`FreeCAD 26.3.0 Revision: 48053`). Command: `cd tools/mcp/freecad-mcp && pixi run --manifest-path <repo>/pixi.toml -- <repo>/build/release/bin/FreeCADCmd.exe <runner>`. `tests/e2e/test_part3_operation_idempotency.py`: `test_commit_checked_property_lost_response_retry` PASS; `test_undo_refuses_after_intervening_local_edit` FAIL (`LEASE_PROTOCOL_REQUIRED`; `undo` not in `AUTHENTICATED_METHODS` so session not elevated). WP01 checked-edit e2e not required by WP02 gate (NOT RUN). Wrote `iteration-002/native-gate.yaml`. No commit. Next: Grok 4.5 High evidence re-review.
- **2026-08-17**: P3-WP02 Grok 4.5 High re-review (iteration-002) Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. GRK-P3-025 PASS (`early_operation_replay_across_documents` before active-session gate; unit retry-after-cancel). GRK-P3-020..024 no regression at source/unit. FreeCADCmd e2e still NOT RUN (not invented). Wrote `iteration-002/re-review.yaml`. Composer idle. Next: Main Agent pixi+vcvars FreeCADCmd e2e; no commit on unit-only. No commit by reviewer.
- **2026-08-17**: P3-WP02 Grok 4.5 High re-review Done. Verdict **PROBLEMS**. GRK-P3-020..024 PASS at source/unit (store, early commit replay, history-head undo/redo, Part 3 identity undo/redo, unit gate assertions). New GRK-P3-025: `cancel_checked_edit` skips early replay when session already inactive after success. `RequestReplayCache` unchanged; lease DocumentSelector not overloaded. FreeCADCmd e2e still NOT RUN (not invented). Wrote `iteration-001/re-review.yaml`. Next: Composer 2.5 iteration-002 for GRK-P3-025; then Main Agent pixi FreeCADCmd e2e. No commit by reviewer.
- **2026-08-17**: P3-WP02 Grok 4.5 High review Done. Verdict **PROBLEMS**. Findings GRK-P3-020..024: missing `(session, operation_id)` terminal-result store; `commit_checked_property` fence-pop before replay short-circuit; remote undo/redo lack history-head binding / intervening-local refusal; name-keyed undo/redo lifecycle risk; gate tests absent. Parent `UndoNames`/`UndoCount` bindable — no parent C++ blocker. `RequestReplayCache` contrast only. Wrote `results/part3-orchestration/2026-08-17/P3-WP02/iteration-001/{review.yaml,review.md,review.json}`. No product edit by reviewer. No commit. No push. Next: Main Agent triage → Composer 2.5.
- **2026-08-17**: P3-WP02 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP02/work-package.yaml` and `baseline.json`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `3e16e77fb2` and nested `409ed72e` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, expected uncommitted §16 tracker (WP01 hash-fill plus this baseline), and nested autocrlf stat dirt (blobs match HEAD; not an invented exclusion). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Next: Grok 4.5 High read-only on P3-WP02.
- **2026-08-17**: P3-WP01 final gate **PASS**. Nested commit `409ed72e` (`409ed72ec2c60f7f6028a7174d098001ab9ad2d8`) pushed to `origin/fix/change-aware-save-mcp-autonomy` (17 files; autocrlf test dirt and `results_luna_*` excluded). Parent commit `3e16e77fb2` (`3e16e77fb2ef95b669d4c82a2d0915c1e15d22c1`) stages App identity/fence/ObjectModel bindings, gitlink `409ed72e`, and this tracker. `git diff --check` exit 0. Composer idle. Next: P3-WP02 baseline.
- **2026-08-17**: P3-WP01 Grok 4.5 High re-review (iteration-003) Done. Verdict **NO_ISSUES**. GRK-P3-019 PASS (ObjectModel `object_name` Gui parity; conflict e2e fences `ObjectModel:StressBox`; no `prepareEdit` fallback). GRK-P3-010–013/015/016/017 still PASS. Branch e2e evidence honest: log 3 passed, junit failures=0, `ci_rc_e2e=0`, FreeCADCmd 26.3.0devR48053. Wrote `iteration-003/re-review.yaml`. Composer idle. Next: Main Agent docs/`git diff --check`/commit+push gate. No commit by reviewer.
- **2026-08-17**: P3-WP01 Composer 2.5 iteration-003 Done. GRK-P3-019: `commitCompatibilityMutation(object_name=...)` ObjectModel scope (Gui parity); conflict e2e intervening mutation on StressBox; removed `prepareEdit` silent fallback. FreeCADApp rebuild PASS; branch FreeCADCmd e2e 3/3 PASS (conflict `DOCUMENT_CONFLICT` on `ObjectModel:StressBox`). Nested unit 14 PASS. Wrote `iteration-003/fix-notes.md`, `native-gate.yaml`. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP01 Grok 4.5 High native evidence re-review Done. Verdict **PROBLEMS**. Branch FreeCADCmd: independence PASS, close/reopen PASS, conflict FAIL. GRK-P3-016 fence/`prepareEditWithExpectedRevisions` used in practice; failure is intervening default UnknownModel publish vs ObjectModel:StressBox fence (Gui uses ObjectModel). New GRK-P3-019. Wrote `iteration-002/re-review-native.yaml`. Next: Composer 2.5; no commit.
- **2026-08-17**: P3-WP01 Main Agent native/e2e unblock Done. FreeCADApp rebuild PASS (VS 2022 BuildTools `vcvars64.bat` + `pixi run cmake --build build/release --target FreeCADApp -j 8`); `prepareEditWithExpectedRevisions` exported in linked `DocumentPy` and live FreeCADCmd. Branch Windows e2e of `tests/e2e/test_part3_checked_edit.py`: conflict FAIL (commit succeeded); independence PASS; close/reopen PASS. Stock Docker 1.1.0 not used. Test-only `_selector` Uid helper aligned with production `_document_uid` (`Uid` is str). No C++/RPC product edit. `prepareEdit` fallback left in place. Wrote `iteration-002/native-gate.yaml`. No commit. Next: Grok 4.5 High evidence re-review.
- **2026-08-17**: P3-WP01 Grok 4.5 High re-review (iteration-002) Done. Verdict **BLOCKED_INSUFFICIENT_EVIDENCE**. GRK-P3-016/017 PASS at source/unit; GRK-P3-010..013/015 no regression. Nested unit 14 PASS; Docker/Windows e2e and FreeCADApp link remain BLOCKED (not faked; DocumentPy codegen for fenced prepare not yet in binary). Wrote `iteration-002/re-review.yaml`. Composer idle. Next: Main Agent FreeCADApp rebuild + branch e2e; no commit on nested-unit-only.
- **2026-08-17**: P3-WP01 Composer 2.5 iteration-002 Done. GRK-P3-016: parent `prepareEditWithExpectedRevisions` + nested begin fence store; commit uses r0 observations for native conflict. GRK-P3-017: e2e uses FeatureTest/DocumentObject two-object scenarios; save/open close/reopen asserts `DOCUMENT_LIFECYCLE_REJECTED`. Nested unit PASS (14). Docker/Windows e2e and FreeCADApp link remain BLOCKED. Wrote `iteration-002/fix-notes.md`. No commit. No push. Next: Grok 4.5 High re-review; Main Agent FreeCADApp + branch e2e.
- **2026-08-17**: P3-WP01 Grok 4.5 High re-review Done. Verdict **PROBLEMS**. GRK-P3-010..013/015 PASS; GRK-P3-014 FAIL; new GRK-P3-016 (prepare-at-commit rebinds expected revisions) and GRK-P3-017 (FeaturePython/e2e scenario honesty). Nested unit 12 PASS; Docker/Windows e2e and FreeCADApp link remain BLOCKED (not faked). Wrote `iteration-001/re-review.yaml`; updated `review.md` / `review.json`. Next: Composer 2.5 iteration-002; then Main Agent unblocks FreeCADApp + branch e2e.
- **2026-08-17**: P3-WP01 Composer 2.5 iteration-001 Done. Implemented GRK-P3-010..015: parent `captureSemanticRevisions` + `collaborationIdentity`; nested `Part3IdentitySelector`, `get_semantic_revisions`, `begin_checked_edit`, `commit_checked_property`, `cancel_checked_edit`; semantic `DOCUMENT_CONFLICT` payload; auth/facade registration; contract snapshot regen. Nested unit PASS (12). Docker e2e BLOCKED (stock FreeCAD 1.1.0 lacks bindings). Windows FreeCADCmd e2e BLOCKED (DLL exit -1073741515). Parent `FreeCADApp` link BLOCKED (MSVC std headers in agent shell). Wrote `iteration-001/fix-notes.md`, `final-gate.yaml`. No WP02 dedup. No lease overload. No commit. No push. Next: Grok 4.5 High re-review + branch-runtime e2e.
- **2026-08-17**: P3-WP01 Grok 4.5 High review Done. Verdict **PROBLEMS**. Findings GRK-P3-010..015 (session-less capture binding gap; identity probe gap; semantic DOCUMENT_CONFLICT payload; new Part 3 selector; native-held conflict tests; auth/facade registration). Wrote `results/part3-orchestration/2026-08-17/P3-WP01/iteration-001/review.yaml`, `review.md`, `review.json`. No product edit by reviewer. No commit. No push. Next: Composer 2.5.
- **2026-08-17**: P3-WP01 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP01/work-package.yaml` and `baseline.json`. Verdict **VALID** (ok to launch Grok 4.5 High read-only). Parent `9a553e49a2` and nested `0bd67ad5` reproduced; gitlink SHA consistent. Classified `results/`, nested Luna logs, `tests/lib/`, `doc/deep-research-report (1).md`, and expected uncommitted §16 tracker edits. Nested dirty tests re-verified as autocrlf stat dirt (blobs match HEAD). No product/MCP/GUI implementation. No Grok launch. No commit. No push. Next: Grok 4.5 High read-only on P3-WP01.
- **2026-08-17**: P3-WP00 final gate **PASS**. Docs consistency PASS; `git diff --check` exit 0. Commit `9a553e49a2` (`9a553e49a24d73b1af547c2fb75554b99fcb4e82`) contains only `doc/part3-gui-collaboration-stress-design.md` and `doc/part3-orchestrated-review-fix-test-plan.md`. Pushed to `origin/fix/change-aware-save-mcp-autonomy`. Nested gitlink held at `0bd67ad5`. Composer idle. Next: P3-WP01 baseline.
- **2026-08-17**: P3-WP00 Grok 4.5 High re-review (after whitespace) Done. Verdict **NO_ISSUES**. Complete current ADR+plan reviewed; prior pre-whitespace `NO_ISSUES` not treated as covering these files. GRK-P3-001..009 still PASS; header Status/Date/branch/checkpoints/architecture-source/scope meaning unchanged; plan still augments ADR; ADR contracts intact (whitespace pass did not alter ADR architecture). `git diff --check` exit 0 observed. Wrote `results/part3-orchestration/2026-08-17/P3-WP00/iteration-001/re-review-after-whitespace.yaml`. Next: Main Agent re-run docs/`git diff --check`/commit+push gate. No Composer. No commit by reviewer. Parent still `2c4c16bb`.
- **2026-08-17**: Plan header whitespace strip **Done**. Converted L3–L7 markdown two-space hard breaks to a short list (no trailing spaces; LF kept). `git diff --check -- doc/part3-orchestrated-review-fix-test-plan.md doc/part3-gui-collaboration-stress-design.md` exit 0. No commit. No push. Parent still `2c4c16bb`. Next: Grok 4.5 High re-review of the complete current ADR+plan diff (plan changed after prior `NO_ISSUES`). Not P3-WP01.
- **2026-08-17**: P3-WP00 final gate **BLOCKED**. Docs consistency PASS (ADR states every §13 P3-WP00 bullet; plan remains an augmentation; “three trust domains” absent; caller `actor_id` API absent except the prohibition; pause-checkbox fallback prohibited). `git diff --check` FAIL: `doc/part3-orchestrated-review-fix-test-plan.md` lines 3, 4, 5, 6, 7 trailing whitespace. ADR `--check` clean. Commit candidate would have been only the two `doc/part3-*` files; exclusions (`results/`, `tests/lib/`, nested gitlink, `doc/deep-research-report (1).md`) held. No commit. No push. Parent still `2c4c16bb`. Next: Main Agent strips those five trailing spaces (no Composer); Grok re-review; re-run gate. Not P3-WP01.
- **2026-08-17**: P3-WP00 Grok 4.5 High re-review Done. Verdict **NO_ISSUES**. All GRK-P3-001..009 PASS against current ADR; no new blocking findings. Wrote `results/part3-orchestration/2026-08-17/P3-WP00/iteration-001/re-review.yaml`; updated `review.md` / `review.json`. Next: Main Agent docs consistency / `git diff --check` / commit gate. No Composer relaunch. No commit by reviewer.
- **2026-08-17**: P3-WP00 Composer 2.5 iteration-001 Done (docs-only). Resolved GRK-P3-001..009 in `doc/part3-gui-collaboration-stress-design.md`. Added §0.1 orchestration loop, §3.1 document identity, §3.2 undo/redo safety, §2.2 exactly-once, §10.1 non-blocking geometry compatibility; rewrote §1 actor/credential boundaries and RemoteAgentDriver child-process isolation; removed pause-checkbox fallback and caller `actor_id`; bound checked-edit methods to authenticated session. Wrote `results/part3-orchestration/2026-08-17/P3-WP00/iteration-001/fix-notes.md`. No product code. No commit. Next: Grok 4.5 High re-review.
- **2026-08-17**: P3-WP00 §3–4 baseline captured. Wrote `results/part3-orchestration/2026-08-17/P3-WP00/work-package.yaml` and `baseline.json`. Verdict **VALID** (ok to launch Grok 4.5 High). Parent `2c4c16bb` and nested `0bd67ad5` reproduced; gitlink SHA consistent (`0bd67ad5-dirty` is nested autocrlf stat dirt, blobs match HEAD). Classified `results/`, nested Luna logs, `tests/lib/`, and `doc/deep-research-report (1).md` on the plan §3/§12.1 exclusion list. Nested dirty tests are known autocrlf dirt, not an invented exclusion. ADR `doc/part3-gui-collaboration-stress-design.md` is missing (searched tree and git history; alternatives recorded). No commit. No review started.
- **2026-08-17**: Orchestration started. Added progression table, next-step, and changelog tracker at the bottom of this plan. No review/fix/test work executed. No commit.
